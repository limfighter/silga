# 다나와 공식 오픈API(openapi.danawa.com) 인증키. 미설정 시 스크래핑
# 방식(services/danawa.py)만 사용 — 아직 이 키를 실제로 쓰는 코드는 없음
# (엔드포인트/응답 형식 조사 전, 실가_인수인계.md #검증-안-된-항목 참조).
# 발급받으면 이 파일을 .env로 복사해서 실제 값 채우기.
# 주의: IP 화이트리스트 필요(대시보드에서 최대 3개 등록) — 고정 IP 환경에서만 동작
DANAWA_OPENAPI_KEY=
