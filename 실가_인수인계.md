# 실가 인수인계 문서
업데이트: 2026-08-23 | 문서 체계: 3파일 분리 (2026-08-03 신설)
※ 2026-08-03 최초 작성 — 목업 2건(랜딩/앱셸) + 데이터소스 후보 조사까지 진행,
  같은 날 이어서 danawa-py 실동작 검증 + 패치까지 완료 (아래 참조)

---

## 오늘(2026-08-23, 이어서) 썩은 목업 2개 삭제 + 죽은 코드 정리 + ±5% 확정

```
사용자 요청: "1+2 묶어서 진행해줘" (할 만한 작업 목록 중 1·2번)
+ 4번(±5% 임계값)에 대해 "어차피 사용자가 정하는거지 뭐 퍼센트 보고"로
  결론이 나와서 미결 항목을 닫음
```

[✓] 1. app-shell-mockup.html / silga-mockup.html 삭제
    → 둘 다 2026-08-01 작성분으로 2026-08-05 디자인 개편(다크·네온 →
      종이/잉크 모노크롬) 이전 톤. 실제로 이번 세션에 통계 목업을 만들려고
      열었다가 못 쓰고 global.css에서 토큰을 직접 가져왔음
    → 문제는 CLAUDE.md와 REFERENCE.md가 이 파일들을 "API 계약의 근거"로
      계속 지목하고 있었다는 것 — 다음 세션이 열어보고 네온 톤을 되살릴
      위험이 실재했다
    → 계약 정본을 REFERENCE #엔드포인트-설계로 명시하고, 거기에 api.ts와
      schemas/*.py가 1:1 대응한다는 삼각 관계로 문구를 바꿈
    → 복원이 필요하면 git show 77ca8ce:<파일명>
    → HISTORY.md와 인수인계의 과거 기록에 남은 언급은 손대지 않음
      (append-only 원칙 — 그 시점엔 사실이었던 서술이다)

[✓] 2. api.ts의 listBuilds 제거 — 호출처 0곳
    → v0.18에서 listBuildStructure/listBuildPrices로 갈라놓고 "기존 방식으로
      남겨둠"이라 적어뒀는데 실제로 아무도 안 썼음
    → 백엔드 GET /builds?with_prices=true(기본값)는 그대로 유지. 프론트만
      안 쓸 뿐이고, REST를 tool처럼 부르는 AI 라우터에겐 한 번에 받는 쪽이
      맞아서 엔드포인트를 없애면 안 된다는 점을 주석으로 남김

[✓] 3. VERDICT_THRESHOLD_PERCENT ±5% — 미결 → 확정(그대로 둠)
    → 사용자 판단: 판정 라벨은 요약일 뿐이고 실제 결정은 화면에 같이 뜨는
      diff_percent를 보고 사람이 한다. 기준가 흔들림은 이동평균 기간
      (설정 탭 7/14/30일)이 이미 보완 장치로 들어가 있음
    → CLAUDE.md와 REFERENCE.md 양쪽에 "검증 안 됐다를 이유로 다시 미결로
      올리지 말 것"까지 명시 — 이 항목이 세션마다 되살아나고 있었음

[✓] 검증: 타입체크 통과, E2E 28건 통과(회귀 없음)

[참고] 루트 *-mockup.html의 위치를 CLAUDE.md에 명문화했다 — 구현 전 화면
    합의용 일회성 산출물이지 디자인 정본이 아니다(정본은 global.css 토큰).
    새 목업을 만들 땐 옛 목업이 아니라 global.css에서 토큰을 가져올 것.
    현재 남은 것은 stats-chart-mockup.html 하나(2026-08-23, 현행 톤)

---

## 오늘(2026-08-23, 이어서) 통계 차트 추세 요약 + 데이터 표 — frontend v0.19

```
사용자 요청: 서드파티 UI/UX 스킬(nextlevelbuilder/ui-ux-pro-max-skill)을
검토하다 나온 결과물. 스킬 자체는 도입 안 함(스킬 7개가 통째로 딸려오고
데이터 대부분이 실가와 무관 — Tailwind/shadcn 전제, 폰트 1934종, 팔레트
192종). 다만 charts.csv 25행 중 "Trend Over Time" 항목이 지적한 두 가지가
실제 결함이어서 그것만 반영했다.
```

[✓] 1. 추세 요약 한 줄 (.ch-summary)
    → Answer.tsx의 "결론 먼저" 규칙을 차트에도 적용.
      "이 구간에서 −9.8% 내렸고, 마지막 관측은 바닥에서 +1.6% 올라온
      자리입니다" + 아래 작은 글씨로 첫 관측 → 마지막 관측 구간 명시
    → 문구 분기 4가지 (끝이 최고가 / 끝이 바닥 / 바닥이 중간 / 바닥이 곧
      첫 관측→중복이라 절 생략). 연결어미도 뒤 절 유무에 따라
      "내렸고," / "내렸습니다"로 갈린다

[✓] 2. y축 금액 라벨 (.ch-yaxis)
    → 격자선 4줄(GRID_Y)에 금액 표시. 지금까지 눈금이 얼마인지 알 수 없었음
    → SVG가 아니라 HTML 절대배치 — 이 SVG는 preserveAspectRatio="none"으로
      가로를 늘려 그려서 <text>를 넣으면 글자까지 늘어난다

[✓] 3. 관측 기록 표 (.ch-table-wrap, <details> 기본 접힘)
    → SVG 하나만 있으면 스크린리더 입장에서 빈 그림 한 장. 홈 판정 축은
      이미 .axis-list로 같은 문제를 풀어놨는데 이 차트에만 없었음
    → 날짜 / 최저가 / 직전 대비(▲▼—). 최저·최고·마지막 배지는 겹쳐 붙인다
      (하나만 고르면 마지막 행이 곧 최고가인 흔한 경우에 "최고"가 사라짐)

[✓] 4. 관측 4건 미만 → 꺾은선 대신 수치 카드 (.ch-sparse)
    → 점 2개를 이으면 뭐든 완벽한 직선이라 추세처럼 보이지만 추세가 아님.
      MIN_POINTS_FOR_LINE = 4 (실측 근거 없는 판단값 — 바꾸려면 상수 하나)

[✓] 5. 코드 버전 문자열 동기화 (또 밀려 있었음)
    → main.py 0.17.0 → 0.19.0, package.json 0.16.0 → 0.19.0.
      v0.18/v0.19를 올리면서 둘 다 안 건드렸다 — 직전 세션에 REFERENCE
      #버전-관리-규칙에 명문화한 바로 그 항목인데 그 다음 변경에서 바로 놓침

[✓] 검증
    → 타입체크 통과, E2E 28건 통과(통계 스텝에 요약·y축·표 검증 4건 추가)
    → 실브라우저: 실데이터 12건 → 요약/라벨/표 정상, 표 기본 접힘 확인
    → 응답 가로채기로 경계 검증: 2건 → 수치 카드, 4건 → 꺾은선
    → 문구 4분기 목데이터로 전부 확인

[미결] 서드파티 스킬 도입 여부 — 현재 "안 함"으로 결론. 통계 화면 차트를
    다시 크게 손보거나 디자인을 재설계할 일이 생기면 charts.csv만 다시 볼 것.
    클론은 세션 컨테이너에만 있었고 리포에는 안 넣었음

---

## 오늘(2026-08-23, 이어서) GPU 칩셋 모델 필터 — backend v0.19 / frontend v0.18

```
사용자 요청: "gpu 들어가서 칩셋 고르면 해당 칩셋의 글카 종류 뜨게 가능한가"
→ 기존 GPU의 chipset은 제조사(NVIDIA/AMD/Intel)뿐이라 RTX 5070 Ti 같은
모델로는 못 좁히고 있었음. 다나와에 해당 필터가 있는지 실측 후 구현.

[✓] 실측 — 다나와 GPU 필터 그룹 38개 중 칩셋 모델 그룹 확인
  → NVIDIA 칩셋(658, 91종) / AMD 칩셋(657, 27종) / 인텔 칩셋(338008, 2종).
    제조사별로 속성코드가 갈리는 건 쿨러 소켓(인텔 6805 / AMD 6806)과 같은 패턴
  → 전체 칩셋(656, 122종)과 제품 시리즈(655, 27종)도 있으나 미채택 —
    전자는 제조사 연동이 안 되고, 후자는 세대 묶음이라 모델보다 거침

[✓] 백엔드 — GPU_CHIP_ATTRIBUTES 딕셔너리 하나로 합침
  → 값 이름이 RTX/RX/ARC로 안 겹쳐서 세 속성코드를 한 딕셔너리에 담을 수
    있었음(쿨러 소켓과 같은 방식). 백엔드는 제조사를 몰라도 됨
  → /search?gpu_chip= 신규. chipset(제조사)과 서로 다른 속성이라 동시 지정
    가능하지만, 모델을 고르면 제조사는 사실상 정해지므로 실사용에선 하나만 씀
  → 실측 120종 중 25종 채택 — NVIDIA RTX 50·40(13), AMD RX 9000·7000(10),
    인텔 ARC B(2). RTX 30 이하·GTX·RX 6000 이하 구형과 워크스테이션 라인
    (AMD W/WX, R9700)은 기존 트리밍 원칙대로 제외

[✓] 프론트 — 의존형 select 기구 신설(이 리포 처음)
  → SpecFilterDef에 dependsOn / optionsBy / emptyPlaceholder 추가.
    지금 쓰는 건 GPU 칩셋 모델 하나뿐이지만 기구 자체는 범용
  → 부모(제조사) 값이 바뀌면 자식(칩셋 모델) 값을 useSpecFilters가 비움 —
    NVIDIA에서 고른 RTX가 AMD로 바꾼 뒤에도 남아 있으면 결과가 0건이 됨
  → 부모 미선택이면 select를 잠그고 "제조사 먼저" 문구
  → useSpecFilters가 optionsFor(def)를 반환하고 세 소비자가 그걸 씀

[✓] 레이아웃 — GPU가 select 4개가 되면서
  → .part-row에 flex-wrap 추가(.stats-picker는 560px라 4개가 한 줄에 안 들어감)
  → 자동완성 드롭다운 top을 52px 고정에서 100%로 — 행이 줄바꿈되면 높이가
    달라지므로 고정값이면 겹침

[✓] 검증 — 값 일치 21→22쌍, 라이브(RTX 5070 Ti 40건 전부 제목 일치,
    RX 9070 XT 21/21), 실브라우저(초기 비활성 → NVIDIA 13종 → AMD 전환 시
    값 초기화 + 10종 교체), E2E 24건 통과(스펙 필터 스텝 확장)

[교훈] E2E에 스텝을 추가할 때 또 같은 함정에 걸림 — 1번 스텝의 검색어
  ("9800X3D")가 남아 있어서 GPU 탭이 0건이었음. 앞서 같은 실수를 기록해뒀는데도
  반복. 이제 스텝 2 시작에서 검색어를 명시적으로 비우도록 고정
[교훈] 백엔드가 떠 있는 상태에서 backend/ppe.db*를 rm 하면 스키마 없는 빈
  파일이 생겨 이후 조회가 전부 "no such table: builds"로 깨짐. DB를 지울 땐
  반드시 서버를 먼저 내릴 것
```

---

## 오늘(2026-08-23, 이어서) 판정 축 단일 핀 라벨 + CI 액션 버전 — frontend v0.17.1

```
미처리 항목 7·9번. 사용자가 "구 VM은 삭제 대신 정지 유지", "브랜치 삭제는
직접 하겠다"고 알려와서 4번은 결정 사항으로, 3번은 대기로 정리.

[✓] 7. 판정 축 — 핀이 하나일 때만 라벨 상시 노출
  → 처음엔 "빌드가 적을 때 라벨을 띄우자"로 생각했는데, 코드에 라벨을 숨긴
    명확한 이유가 이미 적혀 있었음: 적정 밴드에 빌드가 몰리면(그게 정상
    상태다) 라벨이 겹침. 그래서 "적을 때"가 아니라 **겹칠 상대가 아예 없는
    경우(핀 1개)**로만 좁혀서 적용
  → 빌드 1개일 때 축이 "어디 있는지 알 수 없는 눈금 한 줄"이라 섹션 하나를
    쓰고도 정보를 못 주고 있었음(실화면 확인). 이제 "» 단일축 확인 +120.2%"
    처럼 바로 읽힘. 핀도 4px로 굵게, hover 안내 문구는 이때 숨김
  → 2개 이상은 기존 동작(hover/포커스) 그대로 — 겹침 문제를 되살리지 않음

[✓] 9. CI 액션 버전 — v4/v5 → v7
  → 매 실행마다 "Node 20 런타임 deprecation" 경고가 붙던 것
  → 버전을 추측하지 않고 각 릴리스 페이지에서 최신 메이저를 확인:
    checkout v7.0.1 / setup-node v7.0.0 / setup-python v7.0.0 /
    upload-artifact v7.0.1 (2026-08-23 기준). GitHub API는 403(rate limit)
    이라 릴리스 페이지를 직접 봄
  → 메이저 3단계 점프라 깨질 수 있는데, 이 워크플로는 PR을 열면 바로 도니까
    PR CI가 그대로 검증이 됨(실패하면 되돌리는 전제로 올림)

[결정] 4. 구 us-central1 VM(silga-vm) — 삭제하지 않고 정지 유지(사용자 판단,
  나중에 다시 구동 가능성). 정지 중엔 컴퓨팅 과금 없고 디스크만 남는데
  us-central1은 Always Free라 pd-standard 30GB 이하면 무료.
  부수 효과로 "크레딧 소진 전 되돌리기"의 대기 인스턴스가 됨 — deploy/README
  되돌리기 절에 "새로 만들지 말고 이걸 켜라"로 반영
[대기] 3. 브랜치 삭제 — 사용자가 직접 처리하기로 함
```

---

## 오늘(2026-08-23, 이어서) 홈 응답 분리 — backend v0.18 / frontend v0.17

```
미처리 항목 5번(효과 최대, 계약 변경이라 보류해뒀던 것). 사용자가 "쭉 진행"
위임해서 진행.

[문제] 홈/목록이 저장된 빌드를 전부 스크래핑한 뒤에야 화면이 뜸
  → 이름·부품수·저장일은 DB에서 바로 나오는데 가격과 한 응답에 묶여 있어서
    몇 초를 빈 화면으로 기다림. 홈은 집계(판정 분포/총액/부품 수/판정 축)에
    전체 빌드를 쓰는 구조라 빌드가 늘수록 그대로 체감 지연

[✓] 설계 — 요청을 쪼개지 않고 "구조 / 가격" 두 단계로만 나눔
  → 빌드마다 요청을 나누는 안(카드별 호출)은 기각. 브라우저가 동시에 날려서
    매너 크롤링 원칙(동시 병렬 지양)을 깨기 때문. 스크래핑은 서버가 한 요청
    안에서 순차로 도는 지금 방식을 그대로 유지
  → GET /builds?with_prices=false — DB만 읽고 즉시 반환(기본값 true라 기존
    호출부 영향 없음). GET /builds/prices — 스크래핑 부분만
  → 계산 로직은 _build_price_summary()로 뽑아 두 경로가 공유 — 값이 어긋날
    여지를 없앰
  → /builds/prices 라우트는 /builds/{build_id}보다 먼저 정의해야 함
    (뒤에 두면 "prices"가 build_id로 잡혀 422)

[✓] 프론트 — lib/useBuilds.ts 훅 신설, HomePage/BuildListPage가 공유
  → 구조 쿼리 + 가격 쿼리(구조가 온 뒤에만 enabled)를 id로 병합
  → BuildCard에 pricesPending prop — total_price가 null인 건 같아도
    "아직 안 옴"과 "조회 실패"는 전혀 다른 말이라 문구를 나눔
  → invalidateQueries({queryKey:["builds"]})는 prefix 매칭이라 새 키 두 개도
    그대로 무효화됨 — 삭제/생성 후 갱신 로직은 손댈 것 없었음

[✓] 대기 중 "거짓말하는 숫자" 정리 — 실브라우저로 잡음
  → 처음 구현했을 때 대기 상태에서 추적 총액이 "0원", 판정 분포가
    "▲0·—0·▼0 / 판정 불가 3"으로 떴음. 아직 안 온 것뿐인데 0이라고 단정하는
    셈이라 → 대기 중엔 "—" + "가격 조회 중"으로 비움. 목록 탭의 판정별
    개수도 대기 중엔 숨김

[✓] 실측 (빌드 3개 × 부품 3종)
  → 앱 부팅 후 카드 표시 0.06초 / 가격 도착 5.9초 → 5.9초를 먼저 볼 수 있게 됨
  → GET /builds?with_prices=false 자체는 5ms, 다나와 호출 0회
  → 주의: 개발 환경에서 첫 화면까지 12.5초가 찍히는데 이건 Vite dev의 모듈
    컴파일 시간이고 이 변경과 무관함(운영은 nginx 정적 서빙). 네트워크
    타이밍을 찍어서 domcontentloaded 이전 구간임을 확인함

[✓] 검증 — E2E 20건 통과, typecheck, 대기/완료 양쪽 타일·탭 표시 확인,
    pageerror 0건
```

---

## 오늘(2026-08-23, 이어서) 문서·상수 정합성 정리 (앱 동작 변경 없음)

```
사용자가 "찾고 작업 안 한 항목"을 리스트업 요청 → 중요도+간단함 순으로 정렬 →
"쭉 진행하다가 내 역할 필요하면 불러줘"로 위임받아 1·2·8번 처리.

[✓] 배포 문서 VM 이름 — deploy/README.md의 silga-vm 13곳 → silga-vm-seoul
  → 서울 이전 때 인스턴스만 이름을 바꿨는데 문서는 그대로였음. 모든 명령이
    --zone=asia-northeast3-a와 짝인데 그 존엔 silga-vm이 없어서, 복붙하면
    실패하거나 us-central1 구 VM을 건드리게 됨. 실제 배포 안내하다 걸린 건
  → 생성 절에 이름 경위 + "silga-vm으로 바꿔 쓰지 말 것" 경고,
    us-central1 되돌리기 절엔 반대로 "거기선 다른 이름" 주석

[✓] 코드 버전 문자열 — main.py 0.1.0→0.17.0, package.json 0.1.0→0.16.0
  → /openapi.json에 그대로 노출되는 값이라, AI 라우터용 API 스펙에 0.1.0이
    나가고 있었음. REFERENCE 버전 규칙에 "코드 안 문자열도 같이 올린다" 명문화

[✓] bak 파일 규칙 삭제 — 규칙만 있고 .bak 파일은 0개였음. git이 있으니 불필요.
  삭제 사실과 근거를 같은 자리에 남김

[✓] 인수인계 정합성 — "PartRow 죽은 경로" 미결 항목에 [해소됨] 표시

[남은 것 — 사용자 몫]
  · 브랜치 삭제(이번 것 + 과거 11개) — 이 환경 프록시가 삭제 push를 막음
  · us-central1 구 VM(silga-vm) 삭제 — 비용 + 이름 혼동 뿌리
  · VM 가동시간(08~20시) / e2-micro RAM 1GB — 비용 판단

[남은 것 — 내가 이어서]
  · 홈 응답 분리(효과 최대, 계약 변경) → 홈 판정 축 라벨 → CI 액션 버전 →
    CPU 코어수·시리즈 → RAM 클럭·SSD 메모리타입 실측 → GPU 길이 다중구간
```

---

## 오늘(2026-08-23, 이어서) 사이트 개선 1차 — backend v0.17 / frontend v0.16

```
사용자가 "사이트 개선 가능한 점 있나"로 물어서 스크린샷/실측으로 점검 →
계획서를 선택지로 띄워 컨펌받아 진행. 범위는 ①카테고리 필터 버그 +
③검색 40건 상한 해제. ②홈 응답 분리는 계약 변경이라 다음 라운드로 보류.

[버그] 통계·즐겨찾기 부품 검색이 카테고리 필터를 안 타고 있었음
  → PartRow에 category로 "부품"/"검색"을 넘기는데 CATEGORY_LABELS에 없는
    값이라 백엔드가 통째로 무시 → 필터 없이 검색됨
  → 실측: "9800X3D" 무필터 검색 40건의 카테고리 분포가 데스크탑 39 / CPU 1.
    자동완성 상위 8건 중 7건이 완제품 PC라, CPU 가격 추이를 보려다 완제품
    PC를 고르게 되는 상태였음. main.py 주석에 "카테고리 필터가 왜 필요한지
    보여주는 사례"로 적혀 있던 바로 그 케이스인데 이 두 화면만 빠져 있었음
  → 검색 탭에는 이미 적용돼 있어서 오래 안 보였던 것으로 추정

[✓] ① 해결 — PartRow에 카테고리 select + 결과 카테고리 배지
  → categorySelectable prop 신설. true면 .part-cat 라벨 자리가 select로
    바뀌고(전체 + 8개 카테고리) 고른 값이 검색 필터와 스펙 select를 함께 구동.
    StatsPage/FavoritesPage가 이 prop을 넘김
  → 기본값은 "전체" — 기존 동작 보존용. 대신 그 상태에서도 구분이 되도록
    결과에 카테고리 배지를 붙임(SearchResultItem.category 신규). 이 값은
    category_label 필터링에 어차피 읽고 있던 필드라 스크래핑 증가 없음
  → 부수 효과: PartRow의 스펙 필터 렌더링이 죽은 경로였는데 되살아남.
    CPU를 고르면 종류/소켓/내장그래픽 select가 같이 뜸 — 별도 부채로 적어뒀던
    항목이 이걸로 정리됨

[✓] ③ 해결 — 검색 40건 상한
  → danawa.get_product_codes(page=N) + /search?page= (1부터, 1 미만 422)
  → 실측으로 페이지 분리 확인: page1∩page2=0, page2∩page3=0
  → SearchPage를 useInfiniteQuery로 전환 + "더 보기" 버튼. 자동 무한스크롤은
    의도적으로 안 씀(사용자가 누를 때만 다나와에 추가 요청 — 매너 크롤링)
  → 정렬은 페이지별이 아니라 누적 전체 대상. 안 그러면 "낮은가격순"이 페이지
    안에서만 맞는 목록이 됨
  → Answer의 pending 판정을 isFetching→isLoading으로 교체. 안 그러면 더보기를
    누를 때마다 결론 블록 전체가 "불러오는 중"으로 뒤집힘
  → 자동완성(PartRow/PartSearchPanel)은 1페이지 고정 — 상위 몇 건만 쓰는
    화면이라 페이징이 의미 없고 요청만 늘어남

[✓] 검증 — 값 일치 검사 21쌍 / 백엔드 라이브(무필터 카테고리 분포
    데스크탑39·CPU1, category=CPU 1건, page1∩page2=0, page=0→422) /
    typecheck / E2E 신규 스텝 2개(더 보기 누적, 통계 탭 카테고리 필터)

[✓] 작업 중 추가로 드러난 문제 2건 (둘 다 E2E가 잡음)
  → 자동완성 드롭다운이 좁은 컨테이너에서 통째로 안 보임: PartRow가 스펙
    select 개수만큼 드롭다운 right 오프셋을 주는데(16 + 3×158 = 490px),
    .stats-picker는 max-width 560px이라 left:106 + right:490 > 560이 되어
    폭이 음수. 스펙 필터가 죽은 경로였던 동안 숨어 있던 버그 — 이번에 필터가
    되살아나면서 드러남. 드롭다운은 어차피 행 아래(top:52px)라 select를
    가리지 않으므로 오프셋 로직을 통째로 제거(SPEC_FILTER_SLOT_WIDTH 삭제)
  → 검색 결과 배지가 제목을 밀어내 잘림: 검색 탭은 카테고리 탭으로 이미
    좁혀져 있어 결과 카테고리가 전부 같음 = 중복 정보. 배지는 카테고리를
    안 고를 수 있는 PartRow 자동완성에서, 그것도 "전체"일 때만 붙이도록 변경.
    실측으로 규칙이 잘 드러남 — 전체 검색 배지 ['CPU','데스크탑'×7] →
    CPU 선택 시 1건, 배지 없음

[주의] E2E 새 스텝을 처음 넣었을 때 "GPU 탭이면 기본 목록 40건"이라고 전제해서
  실패했음 — 앞 스텝의 검색어("9800X3D")가 남아 있으면 GPU 탭에서 0건이라
  그렇고, 앱 동작은 정상이었음(빈 상태 문구가 맞게 뜸). 스텝을 추가할 때
  앞 스텝이 남긴 상태를 반드시 확인할 것

[보류] ② 홈 응답 분리(구조 즉시 렌더 + 가격은 카드별) — 효과는 제일 크지만
  /builds 계약을 쪼개고 프론트 로딩 상태 설계까지 바뀌어서 별도 라운드
[보류] 홈 판정 축 인라인 라벨 — 취향 영역이라 사용자 판단 대기
```

---

## 오늘(2026-08-23) E2E 스모크 테스트 복구 (앱 코드 변경 없음)

```
사용자 요청: "e2e 스모크 테스트 되살려줘". 앞선 세션에서 이 스크립트가
현행 UI와 안 맞아 첫 스텝부터 실패하는 상태임을 확인해뒀던 건.

[원인] 셀렉터를 화면 문구로 잡아둬서 디자인 개편 때 통째로 썩음
  → `input[placeholder='예: RTX 5070 Ti 16GB']` — 2026-08-05 개편으로
    placeholder가 `{카테고리} 검색어를 입력하세요 (예: ...)`로 바뀜
  → 빌드 생성이 `.part-row` 8개 반복 → 마스터-디테일 패널(.bc-row 클릭 →
    .bc-panel에서 검색)로 바뀌어 스텝 전체가 무효
  → 빌드 상세 부품 행 `.b-row` → `.spec-row`로 이름 바뀜
  → 스크린샷 경로가 `/home/claude/` 하드코딩

[✓] 전면 재작성 (scripts/e2e_smoke_test.py)
  → 셀렉터를 전부 구조 클래스 기준으로 교체(.search-box input,
    .search-result-row, .bc-row, .bc-result-row, .spec-row, .build-card 등).
    문구에 의존하는 곳은 짧고 안 바뀌는 단어만(카테고리명, "12개월")
  → 스텝 9개 / 검증 16건. 스펙 필터 동시 적용(v0.13~v0.16) 스텝을 새로 추가 —
    이번에 넣은 기능이라 회귀 감시 대상이 됨(select 3개, 2개 동시 유지,
    조건 칩 2개, 조건 지우기)
  → pass/fail 집계 + exit code(전부 통과 0 / 실패 1) + 실패 지점 스크린샷
    자동 저장. 예전엔 print만 하고 항상 0으로 끝나서 게이트로 못 씀
  → pageerror 수집 — 화면이 멀쩡해 보여도 콘솔 예외가 있으면 실패 처리
  → 만든 빌드를 끝에서 삭제. 처음엔 반환값으로 id를 넘겼는데 중간 실패 시
    뒷정리가 건너뛰어져 빌드가 남는 걸 실제로 겪어서(실패 1회 후 DB에 잔류),
    생성 즉시 리스트에 적재하고 finally에서 지우도록 고침
  → 환경변수 E2E_BASE/E2E_API/E2E_SHOT_DIR/E2E_CHROMIUM/E2E_KEEP_BUILD.
    스크린샷 기본 경로는 ./e2e-shots(상대), .gitignore에 추가

[✓] 실행 결과 — 16건 전부 통과, exit 0, 실행 후 DB에 잔류 빌드 없음 확인
  → 도중에 실제 결함도 하나 잡음: 빌드 목록에서 `.build-card`를 기다리면
    "새 빌드 만들기" 카드(.build-card.new)가 로딩 중에도 항상 떠 있어서
    조회가 끝나기 전에 통과해버림 → `.build-card:not(.new)`로 수정

[✓] (이어서) CI 신설 — .github/workflows/e2e.yml (리포 최초 워크플로)
  → 사용자 결정: "PR 때만 확인하는 게 맞다" → 트리거를 pull_request의
    opened/reopened/ready_for_review + workflow_dispatch로 잡음. 매 push마다
    돌리지 않는 이유는 이 테스트가 실제 다나와를 수십 회 긁기 때문(매너
    크롤링 원칙)
  → 순서: npm ci → 타입체크(빠르고 결정적이라 먼저) → 백엔드 의존성 →
    playwright chromium → 서버 2개 기동 대기 → E2E → 실패 시 서버 로그 출력
    + 스크린샷 아티팩트 업로드(7일 보관)
  → **실전 검증 완료(PR #33)**: GitHub 러너에서 다나와가 정상 응답하고
    16건 전부 통과. 해외 데이터센터 IP 차단을 걱정했는데 문제 없었음
  → 러너에서 전체 96초로 끝남 — 로컬(2~4분)보다 훨씬 빠른 건 개발 환경이
    에이전트 프록시를 거쳐 요청당 1.7~2.1초씩 먹기 때문. 러너는 다나와에
    직접 붙어서 실제 응답 속도가 나옴(인수인계에 기록된 VM 실측 0.54초와
    같은 맥락)
[참고] 이 환경에서는 playwright가 기대하는 chromium 빌드 번호와 설치본이
  달라서 E2E_CHROMIUM=/opt/pw-browsers/chromium 을 줘야 실행됨
```

---

## 오늘(2026-08-22, 이어진 작업) 다나와 조회 속도 — backend v0.16.1

```
사용자 질문: "다나와에서 받아오는 속도 더 빠르게 못하나?" → 측정 후 옵션
제시 → "1번(lxml+SoupStrainer)은 바로 적용", "①(/build/compare 2N 호출)은
냅둬", "②가격 캐시는 안 함 — 가격 변동이 잦아서 하루에 여러 번 키는 게
아니면 체감 없음" + "홈 로드할 때 전체가 돌아서 빌드가 많으면 더 걸린다"
는 추가 제보.

[측정] 시간이 어디서 가는가
  → 상품 상세(152KB): 네트워크 1.7~2.1s vs 파싱 0.06s — 네트워크가 97%.
    (이 환경은 에이전트 프록시 경유라 부풀려진 값. VM 실측은 부품당 0.54s)
  → 검색 페이지(2.4MB): 파싱만 html.parser로 1.50s. 이건 순수 CPU라
    프록시와 무관하고 VM에서도 그대로 나는 비용 — e2-micro 공유코어면 더 큼
  → gzip은 이미 적용돼 있음(Content-Encoding: gzip). 손댈 것 없음
  → requests.Session keep-alive는 이 환경에서 -2%(노이즈). 프록시가 TLS를
    재종단해서 의미 없음 — VM에서는 다를 수 있으나 여기선 측정 불가라 미적용

[✓] lxml + SoupStrainer (danawa.py)
  → 파싱 1.50s → lxml 0.96s → +SoupStrainer 0.45s (약 3.3배)
  → get_product_codes가 2.4MB 문서 전체를 파싱하고 상품목록 div 하나만
    쓰고 버리던 것을 그 div만 파싱하도록 좁힘. 가격(min_price_{code} input)이
    li 밖에 있어서 잘릴까 봐 따로 확인 — 같은 div 안이라 정상 동작
  → 파서 교체는 깨진 HTML 해석이 달라질 수 있는 게 위험 요소라, 변경 전
    모듈을 따로 로드해두고 검색 페이지 6종 × 상세 페이지 2종을 전수 대조 —
    출력 완전 동일 확인 후 교체(A/B 스크립트는 스크래치패드, 리포 미포함)
  → requirements.txt에 lxml>=5.0 추가 (배포 시 pip install 필요 — deploy.sh가
    이미 requirements.txt를 설치하므로 별도 조치는 없음)

[✓] 홈 로드 — 요청 단위 중복 제거 (list_builds)
  → 원인: 홈이 집계(판정 분포/총액/부품 수/판정 축)에 전체 빌드를 쓰는데,
    list_builds가 빌드마다 따로 _compute_estimate를 돌려서 같은 부품이 여러
    빌드에 있으면 그 수만큼 다나와를 중복 조회하고 있었음
  → price_memo/ma_memo를 빌드 전체에 걸쳐 공유하도록 수정. 캐시가 아니라
    응답 하나를 만드는 동안만 사는 중복 제거(TTL·프로세스 공유 없음)라
    "가격 축적 금지" 원칙과 무관 — REFERENCE.md에 경계를 명시해둠
  → 실패(None, None)도 memo에 적재 — 다나와가 죽었을 때 같은 코드로 timeout을
    반복해서 맞는 게 더 나쁨
  → 부수 효과: 한 응답 안의 모든 빌드가 같은 시점 가격을 보게 됨(일관성 개선)
  → 실측(빌드 5개 × 부품 8개, 4종 공유): 다나와 호출 80회 → 48회(40% 감소),
    응답 JSON 완전 동일. 감소폭은 빌드 간 부품 공유 정도에 비례

[✓] 검증 — 라이브 /search 40건, /search+필터 9건, /product 상세(제목·최저가·
    카테고리 브레드크럼) 전부 정상. API 계약 변경 없음(응답 동일)이라
    세 번째 자리 v0.16→v0.16.1

[결정] ① /build/compare·/builds/{id}의 부품당 2회 호출(get_product +
  get_price_variance)은 그대로 두기로 함(사용자 판단)
[결정] ② 가격 캐시/저장은 도입하지 않음 — 가격 변동이 잦아 하루에 여러 번
  켜는 게 아니면 체감이 크지 않다는 사용자 판단

[주의/발견] 로컬 main이 PR #31 머지 이후 갱신되지 않아 한때 머지 전 코드
  위에 작업이 얹힐 뻔했음(git log로 danawa.py 이력이 안 맞는 걸 보고 발견,
  origin/main으로 리셋 후 재적용). 머지 후 새 작업 시작 전 fetch 확인할 것
[미결] PR #31 브랜치가 원격에 남아 있음 — 이 환경 git 프록시가 삭제 push를
  계속 끊어서(sideband disconnect) 지우지 못함. 과거 세션 브랜치 11개도 동일

---

## 오늘(2026-08-22, 새 세션) 스펙 필터 다중 선택 + 5개 카테고리 필터 확장 — backend v0.16 / frontend v0.15.3

```
사용자 요청: "백엔드 딕셔너리랑 프론트 옵션 값 불일치 없는지 검사해봐" →
"필터 종류를 늘리려고 다나와 필터구조 보면서" → 세션 도중 사용자가 danawa.com
프록시를 다시 열어줌(2026-08-07에 이어 두 번째) → "우선 cpu부터 천천히 필터
대조해가면서 찾아보자" → CPU 대조 중 다중결합 가능이 발견돼 "다중선택부터
구현해줘"로 방향 확정.

[✓] 백엔드/프론트 필터 값 불일치 검사 — 이상 없음
    → 4개 축 전부 대조: ①CATEGORY_LABELS/CATEGORY_DEFAULT_QUERY/CATEGORIES
      키 8/8 ②스펙 옵션 값 13쌍(백엔드 dict 키 ↔ 프론트 옵션 배열) ③
      CATEGORY_SPEC_FILTERS 배선 ④SearchSpecParams ↔ api.search() 전송 ↔
      백엔드 search() 파라미터명. 불일치 0건, 미사용/누락 dict도 없음
    → 검사 스크립트는 스크래치패드에서만 사용, 리포에 커밋 안 함

[✓] CPU 필터 사이드바 재실측 — 현행 코드 이상 없음
    → CPU_SOCKET_ATTRIBUTES 4개 코드 전부 라이브에서 유효, 다나와 라벨도 일치.
      미채택 22개(워크스테이션/서버/구형 소켓)도 기존 기록과 정확히 일치
    → CPU 필터 그룹은 총 28개 확인. 확장 후보로 CPU 종류(속성코드 357316,
      34개), 내장그래픽(15570, 탑재/미탑재), 코어 수(45, 21개), CPU 시리즈
      (357313, 43개)의 값 코드까지 확보해뒀으나 이번엔 구현 안 함

[★] **"다중 attribute 결합 불가" 제약이 오판이었음이 밝혀짐 — 콤마 조인이 됨**
    → 규칙: 서로 다른 속성코드를 콤마로 이으면 **AND**, 같은 속성코드의 값
      여러 개를 이으면 **OR**. 다나와 필터 UI(그룹 안 다중선택=OR, 그룹 간=AND)
      와 동일한 의미론
    → 검증 3건: ①소켓AM5+라이젠7 = 두 단독 결과의 교집합과 정확히 일치(9건,
      A=38 B=28 합집합 57이라 OR이면 40이 나왔어야 함) ②3개 결합(AM5+라이젠7+
      내장그래픽 미탑재) = 1건, (A∩B)∩C와 일치 ③소켓AM4+6코어 = 반환 20건
      전부 두 조건 동시 만족(스펙 텍스트 직접 대조. "6코어"가 아니라
      "헥사(6) 코어"로 표기돼 있어서 문자열 매칭이 오탐을 냄 — 주의)
    → 다른 조인 방식은 전부 실패 재확인: attribute= 파라미터 반복(뒷값만),
      파이프·공백 조인(첫값만), searchAttributeValue[](아예 무시됨)
    → **2026-08-06 "결합 불가" 결론이 왜 틀렸나**: 그때는 GPU 길이(속성 682)
      두 구간을 묶어 합집합을 기대한 케이스였음. 같은 속성끼리는 실제로 OR이
      맞는데 결과가 페이지 상한 40건에 잘려서 "정체불명 집합"으로 보였던 것 —
      즉 그 당시 코드는 제대로 동작하고 있었음

[✓] 부수 발견 — 검색 결과 페이지당 40건이 상한
    → limit=90을 붙여도 40건 그대로. 이 상한 때문에 "결과 집합 비교"로 필터
      동작을 판정하면 계속 오판이 남 — 양쪽이 40에 걸리면 비교 자체가 무효.
      앞으로 필터 실측할 땐 결과가 40 미만인 좁은 질의를 쓰거나 스펙 텍스트를
      직접 대조할 것

[✓] backend v0.12 → v0.13 — 스펙 파라미터 동시 적용
    → main.py::search()의 `A or B or C` 체이닝을 리스트 수집 + 콤마 조인으로
      교체. 쿼리파라미터명/응답 구조는 그대로라 필드 계약 변경은 아니지만,
      지금까지 조용히 무시되던 조합이 적용되는 동작 변경 + 문서화된 우선순위
      규칙이 사라진 것이라 두 번째 자리
    → 검증: ①네트워크 차단(get_product_codes 몽키패치)하고 8개 카테고리 ×
      15조합의 attribute 조립 문자열 전수 확인 15/15 통과 ②라이브 엔드투엔드 —
      "RTX 5070 Ti"는 전량 16GB라 memory_gb=8 + length=300~309mm를 같이 걸면
      AND면 0건, 뒷값만 적용이면 25건이어야 하는데 실제 0건으로 AND 확정

[✓] frontend v0.14.4 → v0.15 — select 상호배타 해제
    → lib/specFilters.ts에 useSpecFilters() 훅 신설. SearchPage /
      PartSearchPanel / PartRow에 3벌로 복붙돼 있던 상태 로직(memoryGb만
      Number 캐스팅하는 예외 포함)을 한 곳으로 모음. 상태를
      `{key,value}|null` 슬롯 하나 → SearchSpecParams 객체로 바꾼 게 전부라
      select 마크업·CSS·개수는 그대로
    → 카테고리 전환 시 조건 초기화를 훅의 useEffect로 옮김(전엔 SearchPage만
      수동으로 했음)
    → UI 추가분(작음): ①Answer의 .because 줄에 현재 걸린 조건 표시
      (`.cond` 헤어라인 박스, .because가 이미 모노스페이스라 글꼴은 물려받음)
      ②"조건 지우기" 버튼(.spec-clear) — 스펙 필터 줄과 0건 화면 두 곳
      ③.search-spec-filters에 flex-wrap 추가(필터가 늘면 넘침)
      ④결과가 40건이면 헤드라인을 "40건+"로 표기 + 좁히라는 안내
    → 새 컴포넌트 없음, 디자인 토큰/선 두께 규칙 그대로 준수

[해소됨 → 2026-08-23 카테고리 select 추가로 되살아남] PartRow의 스펙 필터 렌더링 코드는 당시 죽은 경로 — 이 컴포넌트를
  쓰는 StatsPage/FavoritesPage가 category로 "부품"/"검색"을 넘겨서
  CATEGORY_SPEC_FILTERS 조회가 항상 undefined임(SPEC_FILTER_SLOT_WIDTH
  오프셋 계산도 같이 죽어 있음). 이번엔 다른 화면과 같은 훅을 쓰도록 맞춰만
  두고 삭제는 안 함 — 지울지 여부는 별도 판단 필요
[✓] (이어서) backend v0.14 / frontend v0.15.1 — CPU 종류 필터 추가
    → CPU_TYPE_ATTRIBUTES 신규(속성코드 357316) + /search에 cpu_type
      쿼리파라미터 신설. 실측 34종 중 11종 채택(코어 울트라9/7/5,
      코어i9/i7/i5/i3, 라이젠9/7/5/3) — 워크스테이션·서버(스레드리퍼/제온/
      EPYC/코어X), 기업용 PRO 라인, 구형 단종(코어2듀오·페넘·FX 등),
      저가 사무용(펜티엄·셀러론), 분류 불명("프로세서")은 기존 트리밍
      원칙대로 제외
    → 프론트는 CPU 탭 select가 1개(소켓) → 2개(종류·소켓)로 늘어남.
      등급이 소켓보다 실구매 기준에 가까워서 종류를 앞에 배치. 컴포넌트
      코드는 손 안 댐(범용 렌더링 + useSpecFilters가 그대로 흡수)
    → 검증: 조립 문자열 4케이스 통과, 라이브에서 라이젠7 단독 28건 /
      AM5 단독 38건 / 둘 다 9건(전부 라이젠7 + AM5), 코어i5 단독 40건 전부
      코어i5. 실브라우저에서 select 2개 동시 유지 + 조건 칩 2개 + 헤드라인
      "CPU 9건" 확인, pageerror 0건
    → 새 쿼리파라미터 신설이라 backend 두 번째 자리 v0.13→v0.14.
      프론트는 select 추가라 기존 선례(SSD formfactor·GPU length 추가 때와
      동일)대로 세 번째 자리 v0.15→v0.15.1

[✓] (이어서) backend v0.15 / frontend v0.15.2 — CPU 내장그래픽 필터 추가
    → CPU_IGPU_ATTRIBUTES 신규(속성코드 15570) + /search에 igpu 파라미터.
      값이 탑재/미탑재 2종뿐이라 트리밍 없이 그대로 채택
    → 조립 견적에서 의미가 큰 축 — 별도 GPU를 안 사는 구성이면 "탑재"가 필수
      조건이고, GPU를 따로 살 거면 인텔 F 모델처럼 내장그래픽이 빠진 대신
      싼 물건을 "미탑재"로 골라낼 수 있음
    → select에서 고르고 나면 "탑재"만 남아 무슨 탑재인지 모르게 되므로
      formatOption으로 "내장그래픽 탑재/미탑재"로 표기(조건 칩에도 동일 적용)
    → 검증: 조립 문자열 4케이스 통과. 라이브에서 탑재 40건 / 미탑재 40건이
      겹침 0건, 인텔 F 모델은 미탑재 쪽에만 12건(탑재 쪽 0건). 3중 AND
      (AM5+라이젠7+미탑재)는 9건 → 1건(8700F)으로 좁혀짐
    → 실브라우저: CPU 탭 select 3개 한 줄에 들어감, 3개 값 동시 유지,
      조건 칩 3개, 헤드라인 "CPU 1건", pageerror 0건
    → 새 쿼리파라미터라 backend 두 번째 자리 v0.14→v0.15, 프론트는 select
      추가라 세 번째 자리 v0.15.1→v0.15.2

[✓] (이어서, 사용자가 "자동으로 쭉 진행" 승인) backend v0.16 / frontend
    v0.15.3 — RAM/SSD/메인보드/파워/케이스 5개 카테고리에 필터 6종 추가
    → 먼저 5개 카테고리 필터 사이드바를 실측(RAM 13그룹 / SSD 28 / 메인보드
      81 / 파워 36 / 케이스 43). 상품목록 잡음도 같이 검사했는데 category
      사후필터가 이미 잘 걸러내고 있어서(노트북 RAM 40건 중 1건) 쿨러처럼
      "제품분류" 필터를 따로 둘 필요는 없다고 판단 — 실구매 축만 채택
    → 채택: RAM 총용량(282, 5종) + 램개수(279, 3종), SSD 용량(310085, 5종),
      메인보드 세부 칩셋(504, 84종 중 19종), 파워 80PLUS 인증(13033, 6종
      전량), 케이스 크기(974, 12종 중 4종)
    → 파라미터: capacity(RAM/SSD 공유), ram_count, efficiency, case_size
      신설 + 기존 chipset을 메인보드까지 확장(GPU=제조사, 메인보드=칩셋 모델).
      formfactor/socket처럼 "같은 이름 + 카테고리별 딕셔너리" 선례 그대로
    → 카테고리당 select는 최대 3개 유지(메인보드/RAM/SSD 3, 파워/케이스 2).
      SSD는 용량이 가격을 가장 크게 가르므로 인터페이스보다 앞으로, 케이스는
      크기를 지원폼팩터보다 앞으로 재배치
    → **[정정] RAM 메모리 용량(282)은 모듈 1개당이 아니라 패키지 총 용량**
      — 처음엔 모듈당으로 적었는데, capacity=8GB + ram_count=4개가 4x8GB
      킷이 아니라 4x2GB DDR2/DDR3 구형 패키지 17건을 반환하는 것으로 확인해
      주석/파라미터 설명/select 안내문구를 전부 정정
    → 검증: 값 일치 검사 21쌍 전부 일치 / 조립 문자열 9케이스 통과(카테고리
      교차 오적용 2건 포함) / 라이브에서 값별 결과가 전부 배타적(SSD 2TB vs
      525GB 겹침 0, RAM 8GB vs 32GB 0, 1개 vs 2개 0, 미들타워 vs 빅타워 0),
      파워 800~899W+골드는 40건 전부 두 조건 만족(스펙 텍스트 대조) /
      실브라우저에서 5개 카테고리 전부 select 개수·값 유지·조건칩 확인,
      pageerror 0건
    → 참고: RAM 32GB 킷이 60~90만원대로 나오는데 필터 오류가 아니라 실제
      시세임(무필터 DDR5 목록에도 같은 상품이 같은 가격) — 2026년 메모리
      가격 급등 반영
    → 새 쿼리파라미터 4개라 backend 두 번째 자리 v0.15→v0.16, 프론트는
      select 추가라 세 번째 자리 v0.15.2→v0.15.3

[미결/다음] CPU 나머지 확장 후보(코어 수 45, CPU 시리즈 357313) 값 코드는
  확보했으나 미구현. 다른 카테고리(RAM 용량, SSD 용량,
  메인보드 칩셋, 파워 80PLUS, 케이스 크기 등)는 아직 실측 안 함
[미결/다음] GPU 길이(682) 같은 속성 다중 구간 OR은 재실측 안 함 — 지금도
  미지원으로 둠
```

---

## 오늘(2026-08-15, 새 세션) 배포 리전/비용 최적화 — 서울 이전 + 스케줄 확정

```
사용자 요청: "무료 리전이랑 저장소에서 굴리고 있는데 남는 크레딧이 있다면
하루에 300원정도로 굴릴수 있나" → 이후 대화로 실측 GCP 요금 청구서(CSV)를
같이 보며 구체적인 구성으로 좁혀감. 코드 변경 없음, 배포 문서(deploy/,
REFERENCE.md #기술-스택)만 갱신.

[발견] deploy/README.md의 "임시 외부 IP는 무료" 서술이 틀렸음 — 2024-02-01
부터 GCP는 static/ephemeral 구분 없이 실행 중인 표준 VM의 외부 IPv4에
시간당 $0.005 과금(월 1시간만 무료). us-central1 무료 티어에 있어도 이
비용(24시간 기준 약 170원/일)은 항상 발생 — 문서 정정 완료.

[검토 순서, 반려된 대안 포함]
  1. "서울 Spot으로 예산 맞추기" → 사용자가 실제 청구서(8/1~8/15 CSV,
     Compute Engine 100,071원 등)를 제공 → 계정이 IAP+트레이딩봇과
     크레딧을 공유 중이며 이미 하루 4천~1만원대 소모 중임을 확인.
     "300~500원 예산"이 실가 단독이 아니라 공유 크레딧 풀 안의 여유임을
     재확인
  2. 초기 앱 설치 인그레스 스파이크(8/7~8/8)를 빼고 평일/주말 실측
     재계산(평일 3~6천, 주말 8천 → 정상 소진 약 5,500원/일) → 크레딧
     소진 시점 약 10월 초중순으로 재추정(콘솔에서 실제 잔액 확인 필요,
     이 세션은 GCP 인증 없어 직접 조회 불가)
  3. 예산 300~500원 역산 → 서울 온디맨드 24시간(~577원)은 예산 초과,
     서울 Spot 24시간(~264원)은 예산 안이나 선점 리스크, "08~20시
     온디맨드"(~310원)가 선점 리스크 없이 예산 안에 드는 지점으로 확정
  4. Cloud Run(요청 시 자동 기동, "콜드스타트") 검토 → 콜드스타트
     자체는 1~4초로 빠르지만, SQLite WAL 모드(REFERENCE.md 기술 스택
     확정 사항)가 GCS FUSE 마운트의 파일 락킹과 안 맞고, Cloud SQL로
     가면 DB 비용만으로 예산 초과 + VPC 커넥터 등 구조 복잡화 →
     REFERENCE.md #기술-스택에 이미 있던 "Cloud Run 기각" 근거와 동일
     결론 재확인, 채택 안 함
  5. "REST로 DB 파일 받아왔다 flush" 절충안도 검토 → GCS FUSE보다는
     안전하나 동시 인스턴스 대응(max-instances=1 강제), SIGTERM 유예
     10초 내 flush 실패 시 데이터 유실 가능성, 콜드스타트마다 다운로드
     추가 등 잔여 리스크 + 신규 코드 필요 → 과설계로 판단, 기각

[✓] 확정 구성 (deploy/README.md, REFERENCE.md #기술-스택 반영 완료)
  → 리전: us-central1(무료 티어) → asia-northeast3(서울, 유료). GCP 90일
    $300 체험 크레딧으로 운용 — Always Free는 크레딧을 안 깎지만 서울은
    전부 크레딧 소모 대상이라 "크레딧 있는 동안만" 임시 배치로 취급
  → 인스턴스: e2-micro(RAM 1GB) 유지 — e2-small(RAM 2GB, OOM 근본 해결)은
    24시간 상시 기준 예산 초과(571~741원+)라 보류
  → 가동: GCP 내장 리소스 정책(instance-schedule)으로 08:00~20:00(KST)
    자동 on/off + 필요시 `gcloud compute instances start/stop`으로 수동
    오버라이드
  → 외부 IP: 고정(static)으로 승격 — ephemeral IP는 스케줄로 매일 껐다
    켤 때마다 다른 주소가 배정될 수 있어서(도메인 없이 IP 직접 접속하는
    구성이라 문제됨). 단 GCP는 인스턴스에 붙은 고정 IP를 VM이 꺼져있어도
    "사용 중" 요금(24시간분)으로 매기므로, 컴퓨팅·디스크는 스케줄 12시간분,
    IP만 24시간분으로 따로 계산해야 함 — 합산 시 **약 395원/일**(예산
    300~500원 안, 상단 여유 105원). 애초 스케줄만으로 계산한 310원/일은
    IP 재배정 문제를 놓친 수치였음(사용자가 지적해서 발견, 정정 완료)
  → 스왑 파일 2GB 추가 — 2026-08-08 OOM 장애(RAM 1GB 한계로 SSH까지
    먹통, 아래 8/8 섹션 참조) 재발 방지용, 리전/스케줄과 무관하게 무료라
    같이 반영
  → 디스크 30GB(무료 한도) → 20GB로 축소 (서울은 무료 혜택이 없어 최대치를
    쓸 이유가 없음)

[미결] 크레딧 실제 잔액/만료일은 이 세션에서 직접 조회 불가(GCP 인증
  없음) — 사용자가 콘솔 결제→개요에서 확인 필요. 소진 임박 시
  deploy/README.md "9. 비용 확인 + 크레딧 소진 전 되돌리기" 절차대로
  us-central1로 되돌릴 것 (다음 세션 진입 시 아직 안 했으면 우선 확인)

[미결] RAM 1GB(e2-micro) 유지가 확정이라 OOM 근본 해결은 여전히 안 된
  상태 — 스왑으로 완화만 함. 크레딧이 넉넉해지거나(잔액 확인 후) 예산이
  늘어나면 e2-small 재검토 여지 있음
```

---

## 오늘(2026-08-16, 이어진 세션) 서울 리전 실제 마이그레이션 실행 완료

```
위 계획대로 사용자가 Cloud Shell에서 명령을 하나씩 실행, 세션이 옆에서
안내하며 실시간으로 진행. 결과: 서울(asia-northeast3) 신규 VM
`silga-vm-seoul` 정상 기동, 기존 데이터(products 34/builds 5/
build_items 40/favorites 1) 전량 이전 검증 완료, 사이트 정상 접속 확인.

[발견] 진행 중 deploy/README.md 원본 절차의 버그 3건 실제로 재현 —
  전부 문서에 반영 완료(아래) 다음 배포 때부터는 안 겪을 것
  1. `useradd -r -m -d /opt/silga`가 `/etc/skel` 숨김파일로 홈을
     채워서 이어지는 `git clone /opt/silga`가 "디렉토리가 비어있지
     않다"며 실패 — 클론 전 `find ... -exec rm -rf {} \;`로 정리 필요
  2. nodesource 스크립트 실행 후 `apt install nodejs`가 의도한 v20이
     아니라 Ubuntu 기본 저장소의 구버전(v12, npm 없음)을 설치하는
     경우 있음 — 설치 후 `node -v`로 반드시 검증, v20 아니면
     `apt remove` 후 재설치
  3. `/opt/silga`가 `silga` 전용 홈이라 다른 로그인 계정으로 `cd` 자체가
     막힘 — 백엔드/프론트 셋업은 `sudo -u silga bash -c "cd ... && ..."`로
     한 번에 묶어야 함(개별 명령으로 나누면 `cd`만 실패하고 나머지가
     엉뚱한 디렉토리에서 실행됨)
  4. 같은 이유로 nginx(www-data)가 `/opt/silga/frontend/dist`까지
     못 들어가 500 에러(`stat() ... Permission denied`, "rewrite or
     internal redirection cycle") — `backend/`는 그대로 잠그고
     `frontend/dist`만 `chmod o+x`/`o+rX`로 딱 필요한 만큼 열어야 함

[발견] DB 이전 시 SQLite WAL 모드 함정 실제로 걸림 — `ppe.db` 파일
  하나만(4096바이트, 사실상 빈 스키마) 옮겼더니 데이터가 안 보였음.
  실제 데이터(865KB)는 `ppe.db-wal`에 있었음 — 세 파일(`ppe.db`,
  `-wal`, `-shm`)을 항상 같이 옮겨야 함(권한 위해 백엔드 서비스
  잠깐 정지 후 `/tmp`로 복사 → scp → 원래 위치로 이동 순서). "되돌리기"
  절차에도 이 순서로 반영 완료

[✓] 사이트 정상 접속 확인 — 고정 IP(34.22.94.135)로 `http://` 접속,
  기존 빌드 5개/즐겨찾기 1개 화면에 그대로 보임 확인
[✓] 부품 1개당 다나와 상세조회 실측 — 0.54초(nginx 안 거치고 백엔드
  직접 호출). us-central1 시절 실측치는 없어서 정량 비교는 안 되지만,
  해외 왕복 지연(보통 300~500ms) 없이 이 정도면 부품 8종 빌드도
  스크래핑만 5초 안팎 — nginx 180초 타임아웃에 여유

[결정 2026-08-23] 기존 us-central1 VM(`silga-vm`)은 **삭제하지 않고 정지
    상태로 유지** — 나중에 다시 구동할 가능성이 있어서(사용자 판단).
    정지 상태면 컴퓨팅 과금은 없고 외부 IP도 ephemeral이면 해제되므로,
    남는 비용은 부팅 디스크뿐. us-central1은 Always Free 리전이라
    pd-standard 30GB 이하면 디스크도 무료 — 타입 확인은
    `gcloud compute disks describe silga-vm --zone=us-central1-a
    --format='get(type,sizeGb)'` (pd-balanced면 과금되므로 확인 권장).
    부수 효과로 "크레딧 소진 전 되돌리기" 시나리오의 대기 인스턴스가 되므로,
    되돌릴 때 새로 만들 필요 없이 이걸 켜면 됨(단 코드는 오래됐으니
    deploy.sh부터 돌릴 것)
[구 기록] 기존 us-central1 VM(`silga-vm`)은 아직 안 지움 — 서울 VM
  안정성 며칠 더 지켜본 뒤 삭제 권장(삭제 명령은
  `gcloud compute instances delete silga-vm --zone=us-central1-a`)
[미결] 인스턴스 이름을 서울 VM만 `silga-vm-seoul`로 다르게 지어서
  두 리전에 동시에 떠 있는 동안 구분함 — 기존 VM 삭제 후 이름
  통일할지(예: `silga-vm`로 재명명은 GCP에서 인스턴스 이름 변경이
  안 되므로 재생성 필요) 여부는 사용자 판단 필요, 급하지 않음
```

---

## 오늘(2026-08-13, 새 세션) 견적서 인쇄 여백 비율 조정

```
사용자 요청: "견적서 인쇄 버튼 누르면 여백이 좀 많은데 테스트 시안 여러개
뽑아서 좋은 비율 찾아줘"

[방법] 실제 스크래핑 없이(다나와 예의 크롤링 원칙상 목데이터가 맞는 선택) 목
빌드(부품 8종, 실제 다나와 상품명 길이)를 Playwright route 인터셉트로
프론트에 주입 → Chromium headless(page.pdf)로 5개 @page margin 후보
(12/10/10+8/8/6mm)를 실제 렌더링해 물리 치수(mm)까지 비교. 결과를 아티팩트로
정리해 사용자에게 공유 후 확인받음.

[발견] margin을 좁혀도 "종이가 많이 남는다"는 인상 자체는 해결 안 됨 —
부품 8종 내용은 12mm 기준으로도 페이지 하단에 40mm 이상을 못 채운다. 내용
높이가 margin과 거의 무관하게 고정이라, margin을 좁힐수록 인쇄 가능
영역만 넓어져 죽은 공간이 오히려 커짐(12mm→40.6mm 빈 공간, 6mm→52.6mm).
상세 수치·근거는 REFERENCE.md #디자인-토큰 "인쇄" 항목 참조.

[✓] frontend v0.14.3 → v0.14.4 (CSS만, 세 번째 자리)
  → global.css @page margin: 12mm(균등) → 10mm 8mm(상하/좌우)
  → 좌우를 상하보다 좁혀 .spec-row 폭도 소폭 확보하는 효과 겸함
  → 6mm 균등안은 가정용 프린터 비인쇄 하한선(4~5mm 안팎)에 근접해 테두리
    잘림 위험으로 보류
  → PR #28 (커밋 352ad39) 병합 완료

[미결] 페이지 하단 죽은 공간은 margin과 별개 문제로 남음 — 부품 수가 적은
  빌드(예: 3~4종)일수록 체감이 더 클 수 있어 다음에 확인 필요. 손대는
  방향(내용 밀도를 올릴지, 여백을 인쇄물 관례로 그냥 둘지)은 사용자 확인
  후 진행하기로 함, 이번 세션에서는 손대지 않음.

[절차 노트] 이 PR의 커밋 메시지가 "style:"로 나갔음 — REFERENCE.md
  #버전-관리-규칙의 허용 타입(feat/fix/refactor/docs/chore)에 없는 값이라
  다음부터는 "fix:"를 쓸 것(이미 squash 병합된 main 이력이라 되돌리지 않음).
  또한 커밋 메시지 자체에 버전 표기("frontend vX.Y.Z")를 안 넣었음 — 버전
  변경 사실은 이 문서와 HISTORY.md에 사후 기록으로 보완.
```

---

## 오늘(2026-08-13, 이어진 세션) 견적서 인쇄 1장 압축

```
사용자 요청: "견적서를 뽑을 때 여러 장으로 보이게 돼서 불편한데 비율 조정해서
나오게 할 수 있어?"

[!] 먼저 확인한 것: CSS로 브라우저 인쇄 배율(%)을 지정하는 표준 방법은 없음.
    (인쇄 대화상자의 "배율"은 사용자 조작 영역이고, body{zoom}은 렌더러별로
    레이아웃이 깨짐) → 배율이 아니라 종이용 치수를 따로 주는 방향으로 감

[✓] frontend v0.14.1 → v0.14.2 (CSS만, 세 번째 자리)
  → @media print 안에서 폰트/패딩을 다시 지정해 압축. 기준은 "화면 =
    훑어보는 대시보드, 종이 = 숫자를 확인하는 명세서"
  → 정보를 지운 게 아니라 중복만 걷어냄:
    .prop-bar(비중 바) — %가 .spec-desc에 텍스트로 이미 있음
    .spec-price em(만원 보조) — 원 단위와 중복
    .st-s(스탯 보조 줄) — 같은 내용이 .because와 부품 행에도 있어 3중복
  → .strip 4칸 한 줄 강제 — A4 내용 폭(약 703px)이 화면용 860px
    브레이크포인트에 걸려 2×2로 접히면서 세로를 더 먹고 있었음
  → @page margin 14mm → 12mm

[검증] Playwright page.pdf(format="A4")로 실제 페이지 수를 측정
  → 부품 8종 빌드: 3장 → 1장, 부품 6종 빌드: 2장 → 1장
  → 내용 높이 실측 1133px → 977px (1페이지 = 약 1055px), 여유 78px ≈
    부품 2행이라 10종까지는 1장 유지될 것으로 봄
  → 화면 스크린샷으로는 페이지 분할이 안 보이므로 치수를 또 바꾸게 되면
    반드시 page.pdf()로 다시 잴 것
  → npm run build 통과. 화면(@media print 밖) 스타일은 안 건드림

[✓] (같은 세션, 이어서) 실사용 보고 "용지 2장으로 뜨네, 밑에 총 금액이
    내려와서" → frontend v0.14.2 → v0.14.3
  → 1차 압축의 여유가 78px밖에 안 됐던 게 문제. 브라우저 인쇄 대화상자의
    머리글·바닥글이 켜져 있으면 @page margin과 무관하게 위아래 공간을 더
    먹는데, 그게 78px을 넘어서면 합계 블록만 다음 장으로 떨어짐 —
    CSS로 막을 수 없는 영역이라 여유로 흡수해야 함
  → 게이지 바(.gauge-track/.gauge-labels) 종이에서 제거(약 61px). 바로 위
    배지와 결론 줄이 같은 값을 이미 두 번 말하고 있어 정지된 그림 하나
    때문에 장이 넘어가는 게 손해가 큼
  → 결론 h2 17→15.5px, 부품 행 패딩 축소 + 카테고리/가격 열을 줄여
    상품명 폭 확보(실제 다나와 상품명은 40자 안팎이라 폭이 모자라면 행이
    2줄로 접힘 — 짧은 더미 이름으로 재다가 이걸 놓쳤음)
  → .sum에 break-before:avoid — 그래도 넘칠 때 합계만 홀로 떨어지지 않게
  → @page에 size:A4 명시
  → 실측: 889px(여유 166px ≈ 부품 4~5행). 상품명을 실제 길이로 바꿔
    다시 측정함. 부품 8종/6종 모두 1장
```

---

## 오늘(2026-08-13, 새 세션) C안 목업 전면 적용

```
사용자가 직접 짠 목업(cff878ad-silgaCfullmockup.html, 8화면 전체 + [C] 주석으로
변경분 표시)을 리뷰 요청 → 리뷰 후 "1~10 적용, 홈 판정축은 개편안 고려,
검색 견적 카트는 넣는 걸로, 셸 교체도 진행, 나머지 두 개는 자율" 지시로 구현.

※ 리뷰 중 내가 낸 오판 정정: "검색 견적 카트는 신규 기능"이라고 했는데
  실제로는 2026-08-06에 이미 구현돼 있었음(SearchPage.tsx + global.css의
  .cart-panel 계열). 목업이 더한 건 진행 틱 + 러닝 소계 두 개뿐이었음.

[✓] 백엔드 v0.11 → v0.12 — BuildSummary에 diff_percent 추가
  → list_builds가 이미 `verdict, _ = calc_verdict(...)`로 계산하고 버리던
    값을 그대로 실어 보내기만 함(main.py) — 추가 스크래핑 0. 카드에 판정
    근거(기준가 대비 증감률)를 보여주려면 이 값이 목록 응답에 필요했음.
    계약에 필드가 늘어난 것이라 두 번째 자리.

[✓] 프론트 v0.13.1 → v0.14 — 전 화면 결론 블록(.answer) 도입
  → 기존 화면 헤더(.section-label "DASHBOARD" + <h2>홈</h2>)는 화면 이름만
    되풀이해서 정보량이 0이었음. 그 자리를 "그래서 결론이 뭔데" 한 문장으로
    교체(홈/빌드목록/상세/생성/검색/즐겨찾기/최근기록/통계/설정 9화면).
  → components/Answer.tsx 신규 — kick/headline/because/actions + state
    3종("ready"/"pending"/"failed"). state를 둔 이유: 데이터 소스가 예고 없이
    DOM이 바뀌는 스크래퍼라 "결론이 아직 없음/낼 수 없음"이 예외가 아니라
    정상 상태 중 하나임. 목업에는 이 상태가 아예 없어서(.status-line,
    .empty-state가 CSS만 있고 8화면 어디에도 안 쓰임) 직접 설계해 넣음 —
    최상단 4px 룰은 어느 상태에서나 유지하고(문서 구조가 깜빡이면 안 됨)
    <mark>의 잉크 채움만 밑줄로 낮춤.
  → components/BuildCard.tsx 신규 — 홈/빌드목록에 30줄씩 복붙돼 있던 카드를
    추출(.bc-diff 줄이 붙으면서 중복이 부담스러워짐). lib/format.ts 신규 —
    won()/manwon()/shortDate()/verdictGlyph()/signedPercent().

[✓] 룰 두께 토큰 --rule-1/2/3 (1px/2px/4px)
  → 1px/2px가 자리마다 근거 없이 섞여 있던 걸 의미 단위로 고정
    (4px=문서 구분, 2px=블록 경계, 1px=행 구분). 기계적 치환 후 목업 자신이
    어긴 리터럴 4곳(.gauge-marker, .axis-pin 등)도 같이 정리.

[✓] 셸 교체 — 사이드바 접힘 폐기 + 탑바 페이퍼 톤
  → .hamburger/.sidebar.expanded/.brand-mark/.nav-item .label 페이드 전부
    제거하고 항상 펼친 상태로 고정. 탭이 7개 고정이라 아이콘만 남으면
    "즐겨찾기/최근기록"처럼 뜻이 겹치는 항목을 글리프로 구분해야 해서,
    접었을 때 얻는 164px보다 잃는 게 큼.
  → 탑바는 잉크+블러 → 페이퍼+헤어라인. 바로 아래 .answer의 4px 잉크 룰과
    두 겹으로 겹쳐 상단이 무거웠음 — 위계를 .answer에 넘김. .seal/.crumb
    계열은 .tb-word/.tb-div/.tb-here로 교체. 탑바 현재 위치도
    "빌드 / 새 빌드", "빌드 / #12"로 세분화.

[✓] 인쇄(견적서) 배경 소실 버그 수정 — 이번 변경 중 유일한 실제 버그
  → .answer h2 mark / .sum / .diff-badge.fill / .bc-tag / .gauge-track /
    .stack-bar가 전부 "잉크 배경 + 페이퍼 글자"인데 브라우저는 기본적으로
    배경을 인쇄하지 않음 → 흰 종이에 흰 글자로 결론만 사라짐.
    @media print에 print-color-adjust:exact 추가. Playwright
    emulate_media(media="print")로 실측 확인.

[✓] 그 밖에 적용한 것
  → 상세: 스택 바 + 범례(채움/빗금/세선/외곽선 4패턴, aria-label 포함),
    그룹 합계(.grp .amt), 차액 배지 2종 분리(원 차액은 실측 합계 기준,
    증감률은 판정 기준가 기준 — 기준이 달라 한 배지에 붙이면 오해됨),
    게이지 마커를 페이퍼 심지+잉크 테두리로 교체(잉크 구간에서 묻히던 문제),
    라벨 가장자리 접기(data-edge, ±30% 클램프로 양끝에 붙을 때 잘림 방지)
  → 빌드 생성: .bc-row를 flex → 4열 그리드(88px/1fr/118px/16px)로. 행마다
    가격이 끝나는 위치가 달라 세로 비교가 안 됐음. .bc-shell 좌우 비중도
    뒤집음(목록 가변 / 패널 330px 고정) — 정작 긴 건 왼쪽 상품명이었음.
  → 검색: 카트에 진행 틱 8칸 + 러닝 소계(.cart-sum) 추가, 카운트를 "2 / 8"
    표기로, 결과 행 가격을 고정 슬롯 우측 정렬(min-width:96px)
  → 빌드 목록: 판정 필터 탭(전체/고가/적정/저가) — 이미 받아온 목록을 거르는
    순수 클라이언트 필터라 탭을 눌러도 다나와를 다시 부르지 않음
  → 설정: .form-shell → .set-list 2열 목록(항목 설명이 길어서), 최근기록
    비우기/API 서버 행 추가
  → 죽은 CSS 정리: .build-header/.detail-head/.verdict/.verdict-tag/
    .home-actions/.form-shell/.breakdown/.b-row (전부 TSX에서 미사용 확인 후 제거)

[✓] (같은 세션, 이어서) 홈 판정 축 추가 — frontend v0.14 → v0.14.1
  → 개편안 3안(A 축 아래 리스트 / B hover·포커스 시에만 라벨 / C 표시 개수
    제한)을 제시했고 사용자가 B 채택
  → lib/verdictScale.ts 신규로 상세 게이지와 좌표계를 공유(±30%, 적정 ±5%),
    components/VerdictAxis.tsx 신규
  → 클램프 밖 빌드는 «/» 글리프, low confidence는 파선 눈금,
    인쇄는 축 아래 정렬 목록(.axis-list)으로 폴백
  → 검증 중 잡은 것: 핀 히트 영역이 2px라 짚을 수 없었음(18px로 확대),
    적정 밴드 대비가 없어 안 보였음(잉크 트랙 + 페이퍼 밴드로 통일)
  → .page max-width 980px(목업 값): 1100px 유지. 검색이 1fr+300px 2단이고
    빌드 생성도 1fr+330px이라 980에서는 상품명이 대부분 말줄임됨. 상세만
    980이 유리한데 그건 전체 폭을 줄일 이유가 못 됨.

[검증]
  → 다나와가 이 샌드박스에서 막혀 있어(503) danawa 모듈만 고정 데이터로
    갈아끼운 스텁 서버로 happy path를 스크린샷 확인(9화면 + 인쇄 미디어).
    스텁 스크립트는 스크래치패드에만 두고 커밋 안 함. 확인 중 실제 버그
    2건을 잡음: (1) 상세 .because의 "기준가"에 compareValue를 넣어서
    market_price가 있는 빌드에서 판매가와 같은 숫자가 두 번 찍힘 →
    verdict_basis_price로 교정, (2) 게이지 마커 말풍선이 위 .deltas 배지와
    겹침 → .gauge-track margin-top 22px→50px.
  → npm run typecheck 통과, npm run build 통과, Playwright pageerror 0건.
  → 라이브 다나와 검증은 못 함(샌드박스 네트워크 정책) — 로컬에서 실데이터로
    한 번 더 확인 필요.
```

---

## 오늘(2026-08-08, 이어진 세션) 빌드 삭제 기능 추가
```
사용자 요청: "빌드 삭제 버튼 빌드 우측 상단에 x로 두고 삭제 확인 문구 받고
삭제하는거 넣자" → 상세 화면에 구현해서 "머지할까요?" 물었더니 "다 추가해줘"로
목록/홈 화면까지 확장 지시. 저장된 빌드를 지울 방법이 그동안 아예 없었음(DB에
계속 쌓이기만 함) — DELETE 엔드포인트 자체가 처음부터 없었음. backend
v0.10.1 → v0.11(신규 엔드포인트, 계약 추가라 두 번째 자리), frontend
v0.12.1 → v0.13(상세 화면) → v0.13.1(목록/홈 확장, 아래 참조).

[✓] DELETE /builds/{id} 신규 (backend/app/main.py)
  → 204 No Content, 없는 id면 404 — DELETE /favorites/{code}와 동일한
    패턴(조회→404 처리→db.delete→commit)
  → Build.items가 이미 cascade="all, delete-orphan"으로 관계 설정돼
    있어서(models/build.py) BuildItem을 따로 지울 필요 없이 db.delete(build)
    한 번으로 정리됨 — 실측: 삭제 후 GET /builds/{id} 404, 재삭제도 404
    확인
  → products 캐시 테이블은 안 건드림(다른 빌드/즐겨찾기가 같은 상품코드를
    참조할 수 있어서 — DB 스키마 설계상 원래도 그렇게 두기로 한 캐시라
    별도 정리 로직 불필요)

[✓] 프론트 — BuildDetailPage.tsx 우측 상단 "×" 버튼
  → 기존 "견적서 인쇄" 버튼 옆에 배치(같은 marginLeft:auto 그룹이라 자동으로
    같이 우측 정렬됨). 클릭 시 `window.confirm("\"{빌드명}\" 빌드를
    삭제할까요? 되돌릴 수 없습니다.")`로 확인 문구 — 별도 모달 컴포넌트를
    새로 만들지 않고 네이티브 confirm 사용(1인 개인 도구 규모에 과설계
    방지 원칙 그대로 적용). 확인 시에만 api.deleteBuild(id) 호출 →
    성공하면 빌드 목록(/build)으로 이동 + builds 쿼리 무효화
  → 버튼 스타일(.btn-delete)은 accent color 없이 hover 시 잉크 반전으로만
    "되돌릴 수 없는 조작"을 표시 — 프로젝트의 모노크롬 디자인 원칙(상태는
    색이 아니라 기호/반전으로 구분) 그대로 따름, 빨간색 등 신규 accent
    색상 도입 안 함
  → 실측: Playwright로 취소 시나리오(다이얼로그 dismiss → 페이지 그대로,
    빌드 안 지워짐)와 삭제 시나리오(accept → /build로 이동, 목록에서
    사라짐) 둘 다 확인. npm run typecheck 통과, pageerror 0건

[✓] (같은 세션, 바로 이어서) 사용자가 "다 추가해줘"로 확장 지시 — 빌드
    목록/홈 화면 카드에도 동일 패턴 추가. frontend v0.13 → v0.13.1(백엔드
    변경 없음, 새 컴포넌트/훅 추가라 세 번째 자리)
  → `lib/useDeleteBuild.ts` 신규 — confirm+mutate+쿼리무효화 로직을
    상세/목록/홈 3곳이 공유하는 훅으로 추출(3곳에서 쓰게 되면서 그제서야
    추출 — 2곳까지는 각자 구현이었어도 됐을 텐데 3번째부터 중복이 부담돼서
    분리). 성공 후 동작(상세는 목록으로 navigate, 목록/홈은 그 자리에서
    그리드만 갱신)이 화면마다 달라서 `onSuccess` 콜백으로 위임하는 구조
  → `BuildDetailPage.tsx`는 기존 인라인 useMutation을 이 훅으로 리팩터
    (동작 변화 없음, 코드만 정리)
  → `BuildListPage.tsx`/`HomePage.tsx`의 빌드 카드(`<Link className=
    "build-card">`) 안에 `.bc-delete`(24×24, 카드 좌상단 판정 배지와
    대칭되는 우상단 위치) 버튼 추가 — 카드 전체가 Link라서 버튼
    onClick에서 `e.preventDefault()`+`e.stopPropagation()` 필수(안 하면
    삭제 확인창 대신 상세 페이지로 이동해버림, 실제로 이 실수를 했다가
    Playwright 검증 중 발견하지는 않았고 처음부터 인지하고 방지 코드 넣음)
  → 실측: 목록 화면에서 카드 × 클릭 → confirm accept → 그 카드만 사라지고
    나머지는 그대로, URL도 안 바뀜(목록에 계속 머묾) 확인. 홈 화면
    "최근 빌드" 카드에서도 동일하게 확인. npm run typecheck 통과,
    pageerror 0건
  → 검증 중 실수 하나: 처음 Playwright 스크립트가 삭제 직후 고정
    `time.sleep(0.8)`만 걸고 DOM을 읽어서 "삭제가 반영 안 된 것처럼"
    보였음 — 실제로는 삭제 자체(백엔드 curl 조회로 확인)는 정상이었고
    쿼리 무효화→리페치→리렌더 사이클이 0.8초보다 오래 걸린 것뿐이었음.
    `wait_for_selector(..., state="detached")`로 바꿔서 재검증 후 정상
    확인 — 다음에도 이런 비동기 UI 검증은 고정 sleep 대신 조건 대기 쓸 것

다음에 이어갈 것:
  → 커밋/push 완료 여부 확인 필요(다음 세션에서 git log로 확인)
  → 배포 서버 재배포 필요 — deploy/README.md "이후 업데이트" 절차
  → SSH 먹통(OOM 추정) 재발 방지용 스왑 파일 설정 여부는 여전히 미확인
    (2026-08-08 앞선 섹션 참조, 사용자가 직접 확인하기로 함)
```

---

## 오늘(2026-08-08, 이어진 세션) 판정(verdict) 비교 판매가 선택화
```
직전 GCP 배포 정상화 확인 도중 사이트가 통째로 응답 없어짐(SSH까지 안 됨)
→ e2-micro RAM 1GB 한계로 인한 OOM 추정, 사용자가 `gcloud compute instances
reset`으로 강제 재부팅해서 복구(코드 변경 없음, 배포 스크립트/구조 문제
아니고 VM 리소스 한계 — 스왑 파일 설정 권장했으나 이 세션에서 확인은 못 함,
다음 세션에서 `dmesg | grep -i oom` 결과와 스왑 설정 여부 확인 필요).

복구 확인 후 사용자 피드백: "빌드에 비교할 판매가가 쓸모없다 — 판매가를
알아야 비교하는데, 이동평균 기준 가격으로 빌드 비교를 넣어야 될듯". 실제로
기존 코드를 보니 `market_price`(비교 판매가) 입력을 안 하면 verdict 자체가
아예 계산 안 되고(list_builds/get_build_detail 둘 다 `if build.market_price
and ...`로 게이팅) 프론트 판정 게이지 섹션 전체가 렌더링 안 되는 구조였음 —
완제품 시세를 모르면(대부분의 경우) 이 도구의 핵심 기능인 "적정가 판정"
자체를 못 쓰는 셈. backend v0.10 → v0.10.1(세 번째 자리 — 응답 필드 구조는
안 바뀌고 verdict가 더 자주 non-null이 되는 동작 변경), frontend v0.12 →
v0.12.1.

[✓] market_price 없어도 항상 판정되도록 변경
  → `list_builds()`/`get_build_detail()` 둘 다 게이팅 조건에서
    `build.market_price and`를 제거(`if total_price and items:`만 남김).
    calc_verdict 호출 시 두 번째 인자를 `build.market_price or total_price`로
    변경 — market_price 입력했으면 기존과 동일(그 값과 비교), 없으면 즉시가
    (total_price)를 대신 넣어서 "이동평균 기준가 대비 지금이 비싼지"로 자동
    판정됨. `market_price` 응답 필드 자체는 여전히 사용자가 입력한 값
    그대로(None이면 None) — 내부 비교값으로만 total_price를 대신 쓰는 것이라
    필드 의미 안 바뀜(CLAUDE.md "응답 필드명 임의 변경 금지" 규칙 준수)
  → POST /build/compare, GET /product/{code}/compare는 이번 범위 밖 —
    이 둘은 market_price가 필수인 "일회성 비교 도구"라 원래도 사용자가
    구체적 비교 대상 가격을 갖고 쓰는 용도(REFERENCE.md 참조), 이번 피드백은
    "저장한 빌드"(POST/GET /builds 계열) 한정이라 판단해 안 건드림
  → 실측 검증: market_price 없이 빌드 생성 → GET /builds/{id} 응답에서
    실제로 verdict="고가"(총액 690,660원 vs 14일 이동평균 643,513원, +7.3%)
    확인. GET /builds(목록)도 동일하게 verdict 채워짐 확인

[✓] 프론트 BuildDetailPage.tsx — 판정 게이지 섹션이 diff_percent 존재
    여부로만 렌더링되도록 변경(기존엔 `data.market_price != null` 게이팅이라
    market_price 없으면 게이지 섹션 자체가 안 보였음)
  → market_price 있으면 기존과 동일("비교 판매가" 라벨 + 그 값), 없으면
    우측 비교값을 "최근 {ma_window}일 평균가"(`verdict_basis_price`)로
    표시 — 실측합계(즉시가, 좌측 고정)와 짝을 이루는 값이 상황에 따라
    market_price ↔ verdict_basis_price로 바뀌는 구조(`compareLabel`/
    `compareValue` 지역변수로 분기)
  → 차액(원) 배지 계산도 `Math.abs(data.market_price - data.total_price)`
    (market_price 없으면 NaN 나던 버그)에서 `Math.abs(compareValue -
    data.total_price)`로 일반화 — 두 모드 다 정확한 차액 표시
  → 상단 통계 스트립 "판정" 항목 문구도 "N% vs 판매가" 고정에서 market_price
    유무에 따라 "판매가"/"평균가"로 갈리도록 수정, "비교 판매가 미입력"
    문구는 verdict가 진짜 null인 경우(가격 조회 자체 실패)로 의미 변경
  → BuildCreatePage.tsx의 "비교할 판매가" 필드 힌트 문구도 "비워두면
    이동평균과 자동 비교됩니다" 쪽으로 갱신(필드가 여전히 쓸모 있어 보이게)
  → 실측 검증: market_price 없이 만든 빌드 상세 화면을 Playwright로 확인 —
    게이지 카드 정상 렌더링, 비교 라벨 "최근 14일 평균가", 배지 "▲
    47,147원 · +7.3%", "고가 판정" 태그까지 전부 정상 표시. npm run
    typecheck 통과, pageerror 0건

다음에 이어갈 것:
  → SSH 먹통(OOM 추정) 재발 방지용 스왑 파일 설정 여부 다음 세션에서 확인
    필요(`free -h`, `swapon --show`) — 이 세션은 사용자가 직접 VM에서
    확인하기로 하고 결과 공유 전에 다른 작업(이 항목)으로 넘어감
  → 커밋/push 완료 여부 확인 필요(다음 세션에서 git log로 확인)
  → 배포 서버 재배포 필요(코드 변경 있음) — `deploy/README.md` "이후
    업데이트" 절차
```

---

## 오늘(2026-08-08) 새 빌드 만들기 화면 마스터-디테일 재설계
```
직전 세션(검색 기본 목록+썸네일)의 사용자 코멘트 "다듬기만 하면 되겠다"에
이어 "새 빌드 만들기 창이 너무 사용하기 불편하다"는 피드백. 화면을 다른
Claude 세션(claude.ai 디자인 도구)으로 떼어가서 재설계해오겠다고 해서,
BuildCreatePage/PartRow/specFilters/관련 CSS+API 타입을 하나의 마크다운
파일로 정리해서 전달함(이후 대화에서 사용자가 완성된 .dc 목업 HTML을
다시 가져와서 그대로 구현). frontend v0.11 → v0.12(API 계약 변경 없는
구조 개편 — 두 번째 자리, 화면 구조가 실질적으로 바뀜).

[✓] 마스터-디테일 레이아웃으로 전면 재구성
  → 기존: 부품 8종이 세로로 나열된 행 각각이 자기 검색창+자동완성+스펙
    select를 인라인으로 들고 있던 구조(PartRow 8개 반복 렌더링)
  → 신규: 왼쪽엔 카테고리 8개 목록만(이름/선택된 부품명·가격/화살표),
    오른쪽엔 고른 카테고리 하나에 대한 검색 패널(스펙 필터+검색창+결과
    목록, 44×44 썸네일 포함) — 2단(`bc-shell`) 레이아웃
  → PartSearchPanel.tsx 신규 컴포넌트(BuildCreatePage 전용) — 기존
    PartRow.tsx는 건드리지 않고 그대로 둠. **이유**: PartRow가
    StatsPage(통계 탭 부품 조회)/FavoritesPage(즐겨찾기 추가)/구 SearchPage
    코드 경로에서도 단일 검색+자동완성 위젯으로 재사용 중이라(grep으로
    사용처 확인), 마스터-디테일 구조를 그 컴포넌트 자체에 넣으면 관계
    없는 화면 3곳이 다 깨짐 — 새 컴포넌트로 완전히 분리한 게 핵심 설계
    판단
  → 부품 선택 시 자동으로 다음 미선택 카테고리로 이동(전부 채워지면
    패널이 placeholder로 복귀) — `handlePick()`이 CATEGORIES 순서로 다음
    빈 칸을 찾아 activeCategory를 갱신
  → 패널을 카테고리 변경마다 `key={activeCategory}`로 강제 리마운트해서
    검색어/스펙필터 내부 state가 자동으로 초기화되게 함(별도 useEffect
    동기화 코드 없이 React 기본 동작으로 해결)
  → 오른쪽 패널도 검색어 없이 카테고리 진입 즉시 기본 목록이 뜸(직전
    세션에 구현한 `/search` q 선택화 기능을 그대로 재사용 — 이 화면이
    그 기능의 두 번째 소비처가 됨) + 검색 결과 화면과 동일한 패턴으로
    썸네일(`img` 필드) 표시
  → `.build-summary`(하단 러닝 총액 바)는 이 페이지 전용 클래스라 안전하게
    수정 — 새 목업이 `position:sticky` 없이 정적 블록으로 그려서 그대로
    반영(레이아웃이 2단으로 바뀌면서 세로 길이가 짧아져 sticky 필요성이
    줄었다고 판단됨, 목업 원안 그대로 따름)
  → CSS는 전부 새 `bc-*` 클래스로 분리 신설(`bc-shell`/`bc-left`/`bc-row`/
    `bc-panel`/`bc-result-row` 등) — 기존 `.part-row`/`.part-input`/
    `.autocomplete-*`(PartRow.tsx 전용) 클래스는 그대로 둠. `.part-cat`
    (카테고리 라벨 스타일)만 순수 프레젠테이션이라 새 행에서도 재사용

[검증] 실제 danawa 라이브 데이터로 Vite dev + Playwright 확인 —
  카테고리 행 클릭 시 검색어 없이 기본 목록(40건) 자동 로딩, 썸네일 40/40
  DOM 반영, 부품 선택 후 자동으로 다음 카테고리(GPU)로 전환 + 선택된
  부품명·가격이 왼쪽 행에 반영되는 것까지 확인. 닫기 버튼으로 placeholder
  복귀도 확인. npm run typecheck 통과, pageerror 0건. 이미지 픽셀 렌더링
  육안 확인은 이 세션도 동일한 한계(브라우저가 아웃바운드 프록시를 못 탐)로
  스킵 — DOM에 올바른 img src가 들어가는 것까지만 확인

[참고 — 구현하며 발견한, 목업에 없어서 옮기지 않은 것]
  → 사용자가 준 .dc 목업 스크립트에 `clearPart(cat, e)` 함수가 정의는
    돼 있는데 실제 렌더링 템플릿 어디에도 연결이 안 돼 있었음(죽은 코드로
    보임) — 이미 선택된 부품 행을 클릭해도 그냥 재검색 패널이 열릴 뿐,
    선택을 지우기만 하는 별도 버튼/동작은 없음. 목업 그대로 구현해서
    이 프로젝트도 동일 — "선택된 부품 지우기"가 필요해지면 별도 요청
    필요(현재는 재선택으로만 변경 가능, 즉 다른 부품을 골라 덮어쓰는
    방식)

다음에 이어갈 것:
  → 커밋/push 완료 여부 확인 필요(다음 세션에서 git log로 확인)
  → 사용자가 실제 화면에서 눈으로 확인 후 피드백 예정
```

---

## 오늘(2026-08-07, 새 세션) 검색 기본 목록 자동 로딩 + 썸네일 이미지
```
GCP 배포 실사용 검증 완료 세션 이후, 사용자가 "다듬기" 대상으로 기능면을
지목 — (1) 검색 화면이 검색 버튼을 눌러야만 결과가 뜨는 게 불편, 필터 없이도
카테고리별 기본 목록이 그냥 보였으면 함 (2) 상품 이미지도 보여줄 수 있는지.
둘 다 구현 완료. backend v0.9.1 → v0.10 (계약 변경: /search q 선택화 + img
필드 추가), frontend v0.10.2 → v0.11.

[✓] /search?q= 선택화 — 생략 시 category 기본 키워드로 대신 검색
  → CATEGORY_DEFAULT_QUERY 신규(main.py) — CATEGORY_LABELS(사후 필터용
    라벨)와 별도로, 다나와 검색창에 실제로 넣을 키워드를 카테고리별로 정의
    (예: 쿨러는 label이 "쿨러/튜닝"이지만 검색어로는 "쿨러"만 써야 함).
    8개 전부 라이브로 실측 — category_label 사후 필터까지 거쳐도 전부
    37~40건씩 나오는 것 확인(2026-08-07)
  → q 생략 + category 지정 시 CATEGORY_DEFAULT_QUERY[category]로 검색,
    q도 category도 없으면 400. q를 명시하면 기존과 동일하게 그대로 사용
    (하위 호환 — PartRow 자동완성 등 기존 호출부 변경 불필요)
  → 프론트(SearchPage.tsx): `enabled: query.length > 0` 제거 → 항상
    fetch(query 비어있으면 api.search(undefined, category, spec) 호출).
    카테고리 탭 전환 시에도 자동으로 새 기본 목록 로딩됨. 기존
    "검색어를 입력하세요" 빈 상태 문구는 제거하고, q가 빈 상태로 결과가
    있을 때만 "검색어 없이 {category} 기본 목록을 보여주는 중" 캡션 표시

[✓] 검색 결과 썸네일 이미지 — SearchResultItem.img 신규 필드
  → danawa.py::get_product_codes()가 상품 li의 div.thumb_image img에서
    src 파싱 추가. **실측 중 발견**: 위쪽 소수(~10%)만 즉시 로딩이라 src에
    실제 URL이 있고, 나머지 대다수는 lazyload(class="lazyload")라 src는
    noImg 플레이스홀더 gif 고정값이고 실제 URL은 data-src 속성에 있음 —
    data-src 우선 사용으로 수정(처음엔 src만 봐서 CPU 40건 중 4건만
    이미지가 잡히는 버그였다가 이걸로 40/40 확인). 둘 다 없거나 noImg
    플레이스홀더뿐이면 img=None(진짜로 이미지 없는 상품, 실측 소수 존재)
  → SearchResultItem 스키마에 img 필드 추가, /search 응답에 그대로 포함
  → 프론트: SearchPage.tsx 결과 행에 40×40 썸네일(.thumb, hairline 보더,
    object-fit:contain) 추가, img 없으면 빈 플레이스홀더 박스(.thumb-empty)
    렌더링. PartRow(빌드 생성 화면 자동완성)는 이번 범위에서 제외(사용자가
    검색 화면만 지목함)

[동작특성/검증 한계] 이 세션 환경은 브라우저(Playwright Chromium)가 아웃바운드
  프록시를 못 타서 img.danuri.io 등 외부 이미지 요청이 응답 없이 그냥
  걸림(fonts.googleapis.com 등도 동일 증상) — DOM에 올바른 img src가
  박히는 것까지는 Playwright로 확인했으나(CPU/GPU 전환 각각 40/40 썸네일
  엘리먼트 존재 확인), 실제 픽셀 렌더링까지는 이 환경에서 못 봄. curl로는
  이미지 URL 자체가 200으로 정상 응답하는 것 확인함 — 실제 배포
  서버(GCP VM, 직접 인터넷 접근)나 사용자 로컬 브라우저에서는 문제 없이
  뜰 것으로 판단되지만, 다음 세션(또는 사용자가 직접) 눈으로 최종 확인 필요
  → /search 자체는 curl로 CPU/GPU 등 8개 카테고리 전부 기본 목록 정상
    반환 확인(각 37~40건), q 명시 검색도 기존과 동일하게 동작 확인
  → npm run typecheck 통과

다음에 이어갈 것:
  → **위 이미지 렌더링 최종 확인 필요** — 사용자가 실제 화면(로컬 dev 또는
    배포 서버)에서 썸네일이 실제로 보이는지 확인 후 문제 있으면 알려주기
  → PartRow(빌드 생성 화면) 자동완성에도 썸네일을 넣을지는 미정 — 이번엔
    검색 화면만 요청받아 그쪽만 구현함, 필요해지면 다음에 확장
  → 커밋/push 완료 여부 확인 필요(다음 세션에서 git log로 확인)
```

---

## 오늘(2026-08-07, 이어진 세션) GCP 배포 실사용 검증 완료
```
아래 "GCP 배포 준비" 섹션에서 만든 deploy/ 설정으로 사용자가 실제
GCP e2-micro VM에 처음으로 배포 진행. 이 세션엔 GCP 인증이 없어 설계만
검토했었는데, 실제 VM에서 돌려보니 예상대로 몇 군데 막혔음 — 전부
그 자리에서 고쳐서 최종적으로 **실사용 정상 확인**(검색/빌드저장/
히스토리 조회 다 됨). backend는 이 세션 동안 v0.9 → 커밋 여러 개 누적(버전
번호 자체는 유지, 전부 세 번째 자리급 버그수정/배포 스크립트 수정).

[✓] 배포 절차 실행 중 겪은 문제 4건 + 해결 (시간순)
  1. **`deploy/`가 main에 안 올라가 있었음** — GCP 배포 준비 커밋들을
     PR로 안 올리고 브랜치에만 둔 채로 있다가, 사용자가 `git clone`했을
     때 `deploy/` 자체가 없어서 systemd 등록 단계에서 파일을 못 찾음.
     PR #16으로 뒤늦게 머지 — **이제부턴 작업하면서 커밋 쌓이는 대로
     바로바로 PR 만들어서 머지할 것, 몰아뒀다가 나중에 하지 말 것**
     (이번 세션에 실제로 발생한 실수라 강조)
  2. **`git clone` "destination path already exists" 반복** —
     `useradd -m -d /opt/silga`가 스켈레톤 파일(.bashrc 등)을 자동으로
     넣어놔서 매번 안 비어있다고 거부당함. 최종적으로는 디렉토리를
     통째로 삭제(`sudo rm -rf /opt/silga`) 후 `mkdir`+`chown`으로
     새로 만들고 클론하는 방식으로 정착(`/opt`는 root 소유라 silga
     유저가 직접 mkdir 못 하는 것도 같이 확인)
  3. **`deploy.sh`를 `sudo -u silga`로 실행하면 안 되는 구조적 버그** —
     스크립트 내부 `sudo systemctl restart`가 로그인 불가 시스템 계정
     `silga`로 다시 sudo 승격을 시도하다가 막힘. `deploy.sh`를 **root로
     실행**(`sudo ./deploy.sh`)하고 파일 작업만 내부에서 `sudo -u silga`로
     낮추는 구조로 재작성(PR #18)
  4. **실사용 버그 2건, 실제 배포 후에야 드러남**:
     - `PriceHistory.min`/`max` 스키마가 `str`인데 `get_price_variance()`가
       int를 그대로 반환해서 pydantic ValidationError(500) — 데이터가
       있는 기간(3/6/12개월)에서만 크래시가 드러남(데이터 없는 기간은
       그 전에 TypeError→404로 먼저 빠져서 안 걸림). `str()` 캐스팅
       추가로 해결(PR #17)
     - `danawa.py`의 `requests.get()` 3곳 전부 **timeout 미설정** —
       재배포 시 이전 프로세스가 graceful shutdown에 90초 넘게 응답
       없어 SIGKILL 강제 종료되는 현상으로 발견(다나와 쪽 요청이 어딘가
       멈춰있었던 것으로 추정). 셋 다 `timeout=20` 추가(PR #19).
       `requests.Timeout`이 `requests.RequestException`의 서브클래스라
       기존 503/부품별 fallback 처리에 자동으로 걸림 — 별도 예외 처리
       코드 추가 불필요했음
  → nginx `/api/` 프록시 타임아웃도 60초→180초로 미리 늘려둠(빌드 저장이
    부품마다 danawa.get_product() 순차 호출이라 부품 많으면 오래 걸림,
    PR #17에 포함)

[✓] 최종 검증 (사용자 실브라우저로 직접 확인)
  → 검색(GET /search) 정상
  → 빌드 저장(POST /builds) 정상
  → 히스토리 3개월 이상 조회(GET /product/{code}/history?months=3|6|12) 정상
  → "깔끔하게 잘 된다"로 사용자 확인 — 실가가 처음으로 로컬이 아닌
    실제 서버(GCP e2-micro, 34.56.49.73)에서 돌아가는 상태가 됨

다음에 이어갈 것:
  → 사용자가 "다듬기만 하면 되겠다"고 함 — 구체적으로 뭘 다듬을지는
    다음 세션에서 물어볼 것(디자인 디테일일 수도, 성능일 수도, 명시 안 됨)
  → 배포 자체는 안정화된 걸로 판단 — 앞으로 코드 변경 시 재배포 절차는
    `deploy/README.md` "이후 업데이트" 섹션 그대로 따르면 됨
    (`sudo -u silga git -C /opt/silga pull` → `sudo /opt/silga/deploy/deploy.sh`
    — 설정 파일(nginx/systemd) 자체가 바뀐 경우엔 그 아래 안내대로
    수동 복사도 같이 필요)
  → 남은 보류 항목: APScheduler 배치 설계, 알림 기능(Flutter 이후),
    다나와 공식 API 후속 확인(로그인 후 실제 문서 찾기, 낮은 우선순위) —
    전부 우선순위 없음
```

---

## 오늘(2026-08-07, 이어진 세션) GCP 배포 준비
```
다나와 공식API 되돌림(커밋 6dca486) 이후, 사용자가 "슬슬 웹 종결하고 서버
붙일까?"로 배포 논의 시작 → GCP e2-micro 무료 티어 선택("자주 다뤄봐서"
익숙함이 이유). 이 세션 환경엔 GCP 인증이 없어서(gcloud 미설치) 실제 VM
생성/배포는 사용자가 직접 gcloud로 진행해야 함 — 이번 세션에서는 배포에
필요한 설정 파일 + 절차 문서만 준비함. 아직 커밋/push 전(다음 "커밋하자"
지시 대기 — 이 문서 자체가 그 확인 시점 이전에 쓰여짐).

[✓] 배포 구성 결정 (사용자 확인 완료)
  → 프론트(정적 빌드)+백엔드(FastAPI) 같은 VM 하나에서 서빙 — nginx가
    frontend/dist 정적 서빙 + `/api/`만 로컬 uvicorn(127.0.0.1:8000)으로
    리버스 프록시. 프론트를 Vercel 등 별도 호스팅에 두는 안은 기각(VM
    하나로 통일 선택)
  → 도메인/HTTPS 없이 VM 외부 IP로 HTTP 바로 접속(도메인 생기면 그때
    certbot 추가하기로)
  → 이 구성의 부수효과: 프론트가 `/api/...`로 같은 오리진 호출을 하게
    되어 **프로덕션에서는 CORS가 아예 발동 안 함** — 오랫동안 보류 중이던
    "배포 시 CORS 좁히기" 항목이 이걸로 사실상 해소됨(코드 변경은 안 함,
    로컬 dev는 여전히 크로스 오리진이라 `allow_origins=["*"]` 유지 필요.
    아래 "수정 예정 사항" CORS 항목 참조)

[✓] deploy/ 디렉토리 신규 — 4개 파일
  → `deploy/README.md`: gcloud VM 생성부터 systemd/nginx 셋업까지 전체
    절차(구체적 명령어 포함). 무료 티어 조건(e2-micro는 us-west1/
    us-central1/us-east1 리전 한정 무료)도 명시
  → `deploy/silga-backend.service`: uvicorn을 systemd로 관리(전용 유저
    `silga`로 실행, 127.0.0.1:8000만 바인딩 — 외부 노출은 nginx 통해서만)
  → `deploy/nginx-silga.conf`: 정적 서빙 + `/api/` 리버스 프록시 + SPA
    라우팅(존재하지 않는 경로는 index.html로) 설정
  → `deploy/deploy.sh`: 이후 코드 갱신 시 재배포용(git pull + 백엔드
    의존성 재설치 + 프론트 재빌드 + 서비스 재시작 한 번에)
  → README.md에 "## 배포" 섹션 추가해서 deploy/README.md로 연결.
    REFERENCE.md #기술-스택 배포 항목도 "미정"에서 확정으로 갱신(구조
    결정이라 REFERENCE.md도 같이 수정 — CLAUDE.md 규칙)

[동작특성/주의] 이 세션은 GCP 인증이 없어서(gcloud 커맨드 자체가 없음)
  실제 VM 생성이나 배포 스크립트 실행을 검증하지 못함 — 파일 내용은
  설계상 정합성만 확인했고, 실제 VM에서 첫 실행 시 오탈자/권한 문제 등이
  나올 수 있음. 사용자가 직접 VM에서 실행해보고 문제 있으면 다음 세션에
  피드백 필요

다음에 이어갈 것:
  → 커밋/push 여부 확인 필요 (다음 세션에서 git log로 확인)
  → 사용자가 실제로 GCP VM 만들어서 `deploy/README.md`대로 진행 → 문제
    생기면 그 내용 가지고 디버깅
  → 남은 보류 항목: APScheduler 배치 설계, 알림 기능(Flutter 이후) —
    둘 다 우선순위 없음
```

---

## 오늘(2026-08-07, 이어진 세션) 다나와 공식 오픈API 최종 결론: 채택 안 함
```
GPU 길이 필터 작업 뒤이어, 사용자가 실제로 openapi.danawa.com에서 API 키를
발급받아 공유(이후 폐기 예정 키였음)해서 공식 API 채택 가능성을 깊게
조사함. 결론: 두 갈래 다 우리 용도엔 안 맞음 — 준비해뒀던 코드/설정도
전부 되돌림.

[✓] "다나와 공식 오픈API" 정체 최종 확인 — 2개의 서로 다른 서비스였음
  → **구버전(api.danawa.com, 2012년 출시)**: 카테고리별 상품목록/뉴스/
    장터 조회 등을 제공하던 공개 개발자 API. ERPia(전자상거래 ERP 업체)가
    2019-05-20에 올린 고객 공지사항에서 "다나와 OPEN API 서비스를
    종료했습니다"를 직접 확인 — 즉 공식적으로 종료된 지 7년 된 서비스.
    지금 이 도메인은 DNS 실패/502로 완전히 죽어있음(여러 세션에 걸쳐
    반복 확인)
  → **신버전(openapi.danawa.com, 사용자가 키 발급받은 것)**: 검색엔진에
    관련 페이지가 전혀 인덱싱 안 돼 있음(공개 마케팅 대상이 아니라는
    신호) + 대시보드 안내 문구가 "허용된 IP를 통해서만 **상품관리**가
    가능합니다" — 이건 쿠팡/스마트스토어/G마켓 등에서 흔히 보이는
    "입점 판매자가 자기 상품을 등록/관리하는 API"와 동일한 패턴(IP
    화이트리스트 + "상품관리" 용어 + 판매자 대상). 즉 제3자가 전체
    상품 가격을 조회하는 공개 API가 아니라 **판매자 전용 포털**로 추정
  → 로그인해서 본 API 가이드 페이지 자체도 "charset은 UTF-8로 입력하셔야
    합니다" 에러만 뜸(고장 상태), img.danawa.com에 남아있는 정적 가이드
    페이지(2012년 구버전용으로 보임)도 표에 에러메시지가 잘못된 칸에
    들어가 있는 등 미완성 템플릿이 그대로 방치돼 있는 상태 확인
  → 종합: 구버전은 폐기, 신버전은 용도 자체가 다름(추정) + 문서도 고장—
    양쪽 다 danawa-py 스크래핑을 대체할 근거가 안 됨. 이 항목 완전히
    종결, 앞으로 다시 검토할 필요 없음(도메인이 살아나거나 사용자가
    명확한 API 문서를 직접 확보하지 않는 한)

[✓] 준비해뒀던 코드/설정 되돌림 (사용자 지시 — "아까 만든 경로 파기하자")
  → backend/app/main.py: `load_dotenv()` 호출 + `DANAWA_OPENAPI_KEY` 로드
    코드 제거(이전 커밋 4455929에서 추가했던 것)
  → backend/requirements.txt: `python-dotenv` 의존성 제거
  → backend/.env.example: git rm (커밋 25c9aaf에서 추가됐던 것)
  → backend/.env(로컬 전용, 커밋된 적 없음): 파일 자체 삭제 — 사용자가
    "보여주려고 꺼낸 것, 어차피 폐기할 키"라고 밝힌 실제 API 키가 들어있던
    파일이라 완전히 지움
  → 결과: 다나와 데이터소스는 이전과 동일하게 scraping(services/danawa.py)
    단일 경로로 원복. 코드베이스에 이 조사의 흔적은 문서(이 파일 +
    HISTORY.md)에만 남음

다음에 이어갈 것:
  → 다나와 공식 API 항목은 완전히 닫힘 — "수정 예정 사항" 목록에서도 제거
  → 남은 보류 항목: CORS 좁히기(배포 시), APScheduler 배치 설계, 알림
    기능(Flutter 이후) — 전부 우선순위 없음, 다음에 필요할 때 진행
```

---

## 오늘(2026-08-07) GPU 길이 스펙 필터 추가
```
PR #12(SSD 폼팩터 필터)까지 머지 완료된 상태로 세션 시작. 직전 세션에서
GPU 길이·쿨러 냉각방식 둘 다 "실효성 낮다"는 이유로 미채택했었는데
(2026-08-06 세션 참조), 이 세션 도중 이 환경의 danawa.com 접근 차단이
풀림(사용자가 프록시 정책을 변경한 것으로 보임 — "그거 내가 풀었는데") →
"실효성 문제였지 되면 넣어달라"는 사용자 지시로 GPU 길이만 다시 검토해서
구현. backend v0.9 / frontend v0.10.2.

[✓] GPU_LENGTH_ATTRIBUTES 신규(속성코드 682 = 가로(길이))
  → danawa.com이 열려 있는 동안 직접 라이브로 실측+검증(사용자가 HTML을
    붙여줄 필요 없이 이 세션에서 스크래퍼로 직접 확인한 첫 사례)
  → 직전 세션에서 발견했던 우려("가로(길이)" 필터가 지금까지 쓴 다른
    스펙 필터와 달리 다나와 "상세검색" UI 소속이라 값 형식이
    "{카테고리seq}|{속성seq}|{값seq}|OR"로 다름)를 라이브 요청으로 직접
    풀어봄 — 카테고리seq를 버리고 하이픈으로 바꾼 "682-3993-OR" 형식이
    기존 attribute= 파라미터에 그대로 먹히는 것 확인(140~149mm 필터 →
    GT1030/GT730 등 초소형 카드만, 360mm~ 필터 → RTX 5080/5090 등
    초대형 카드만 반환되는 것으로 실측 검증)
  → 여러 10mm 구간을 하나로 묶는 결합(예: "300mm 이상")은 콤마/파이프/
    attribute 파라미터 반복까지 다 시도했으나 전부 실패 확인 — 기존에
    이미 알려진 "다중 attribute 결합 규칙 미검증" 제약이 라이브 실측으로
    다시 확인된 것. 그래서 PSU_WATTAGE_ATTRIBUTES처럼 다나와가 주는
    10mm 단위 구간(전체 23개)을 그대로 개별 옵션으로 노출하는 방식 채택
  → 23개 전부 넣지 않고 현재 시장에 몰려 있는 실사용 범위(190~369mm)에서
    7개만 채택 — 190~199mm, 260~269/280~289/300~309/320~329/340~349mm,
    360mm~. 140~189mm(GT710/GT730급 구형 로우프로파일 업무용 카드)는
    개인 게이밍 조립 범위 밖이라 기존 트리밍 원칙대로 제외
  → GPU는 이제 chipset > memory_gb > length 우선순위(main.py::search()의
    `A or B or C` 체이닝) — chipset이 기존에도 memory_gb보다 우선이었고,
    length는 새로 추가된 세 번째 옵션이라 가장 낮은 우선순위로 둠. 실측:
    chipset=AMD + length=360mm~ 동시 지정 시 chipset 결과(라데온만) 반환
    확인
  → 값 하나에 실제 "몇 mm 이하만 장착 가능한 케이스" 같은 정확한 이하/이상
    필터는 아니고 근사치(10mm 폭 개별 구간) 참고용이라는 한계는 REFERENCE.md
    표에 명시해둠
  → 프론트: specFilters.ts GPU 항목에 length select 추가(GPU_LENGTH_OPTIONS)
    — GPU가 이제 select 3개(제조사/용량/길이), 기존 specDefs 배열 기반
    범용 렌더링이라 컴포넌트 코드 변경 없이 자동 확장됨(자동완성 오프셋도
    자동 대응, SSD 2-스펙 확장 때와 동일한 패턴)
  → 검증: npm run typecheck 통과. **이번엔 mock이 아니라 실제 danawa
    라이브 데이터로 백엔드(curl)+프론트(Vite+Playwright) 전체 흐름 검증**
    — GPU 탭에서 length=360mm~ 선택 후 검색 → 화면에 RTX 5080/5090만
    뜨고 RTX 5060은 안 뜨는 것 실브라우저로 확인(스크린샷). pageerror 0건

다음에 이어갈 것:
  → 쿨러 냉각 방식(공랭/수랭)은 이번엔 재검토 대상에서 제외됨(사용자가
    GPU 길이만 지목) — 필요해지면 이 세션에서 확인한 것처럼 danawa 접근이
    열려 있는 동안 직접 라이브로 실측하는 게 제일 빠름(속성코드 315758은
    이미 알고 있음, main.py 주석 참조)
  → danawa.com 접근이 이 세션 한정으로 열린 건지, 앞으로도 계속 열려있을
    건지 불확실 — 다음 세션에서 막혀 있으면 다시 사용자가 HTML 붙여주는
    방식으로 돌아가면 됨(어느 쪽이든 대응 가능하도록 코드/문서 특별히
    바꾼 것 없음)
  → 그 외 미결 사항은 아래 2026-08-06 섹션들 및 "수정 예정 사항" 목록
    참조(CORS 좁히기/APScheduler/다나와 공식 API 생사 확인 등, 전부 보류 중
    — 특히 다나와 공식 API 확인은 접근이 열린 지금이 적기일 수 있음)
```

---

## 오늘(2026-08-06) SSD 폼팩터 스펙 필터 추가
```
PR #11(검색 UX 개편)이 이미 main에 머지된 상태로 세션 시작(브랜치
claude/new-session-xw491j, 커밋/push 확인 완료 — 아래 직전 섹션 "다음에
이어갈 것"의 확인 항목 해소됨). 사용자가 "스펙 필터 확장 → SSD 폼팩터"를
선택해서 진행. backend v0.8 / frontend v0.10.1.

[✓] SSD_FORMFACTOR_ATTRIBUTES 신규(속성코드 14695 = 폼팩터). 사용자가 다나와
  "SSD" 검색결과 #prodArea outerHTML을 통째로 제공해서 실측(기존 패턴 재사용)
  → 실측 8종(M.2 2280/2242/2230/22110, 6.4cm(2.5형), Mini SATA(mSATA),
    PCIe 카드, 기타) 중 개인 조립 PC 범위 밖인 M.2 22110(서버/엔터프라이즈),
    mSATA(구형 노트북용), PCIe 카드(소수 애드인 카드 규격), 기타(불명확)는
    기존 트리밍 원칙대로 제외 — M.2 2280/2242/2230 + 2.5인치(SATA) 4종만 채택
  → /search의 `formfactor` 파라미터를 재사용(기존엔 메인보드/케이스 전용) —
    카테고리별로 완전히 다른 속성코드/딕셔너리를 쓰는 기존 방식(메인보드
    506 vs 케이스 6196) 그대로, SSD도 자기 딕셔너리(SSD_FORMFACTOR_ATTRIBUTES)로
    분리 추가. 새 쿼리파라미터 신설 아님 — 기존 파라미터의 적용 category만 확장
  → SSD는 이제 interface(기존)+formfactor(신규) 스펙 파라미터 2개 보유 —
    동시 지정 시 interface 우선 적용(`SSD_INTERFACE_ATTRIBUTES.get(interface)
    or SSD_FORMFACTOR_ATTRIBUTES.get(formfactor)`, main.py::search()).
    우선순위 기준은 명확한 원칙이 있다기보단 SSD 세대/성능을 더 직접
    나타내는 쪽을 우선한 결정(다른 2-스펙 카테고리와 마찬가지로 임의 결정)
  → 프론트: frontend/src/lib/specFilters.ts의 SSD 항목에 formfactor select
    추가(SSD_FORMFACTOR_OPTIONS). PartRow/SearchPage 둘 다 specDefs 배열
    기반 범용 렌더링이라 컴포넌트 코드 변경 없이 select 2개로 자동 확장됨
    (자동완성 드롭다운 우측 오프셋도 specDefs.length 기반이라 자동 대응 —
    GPU/메인보드/쿨러가 이미 2-스펙이라 검증된 패턴 재사용)
  → 검증: npm run typecheck 통과. mock 백엔드(danawa 함수 monkeypatch,
    attribute값별로 다른 픽스처 반환) + Vite dev + Playwright로 실측 —
    (1) /search?category=SSD&formfactor=... 쿼리 자체가 백엔드에서 올바르게
    필터링되는지 curl로 4종 전부 확인 + interface 동시 지정 시 interface
    우선 확인 + 다른 category에서 무시되는지 확인 (2) SearchPage에서 SSD
    탭 select 2개 렌더링 → 폼팩터 select로 "M.2 2280" 선택 → 검색 결과가
    실제로 필터링된 데이터만 나오는지 화면에서 확인 (3) BuildCreatePage
    (PartRow)의 SSD 행도 select 2개 정상 렌더링 + 자동완성 드롭다운이
    select들과 안 겹치는지 스크린샷으로 확인. pageerror 0건

다음에 이어갈 것:
  → [x] 스펙 필터 확장은 여기서 종료 확정 (2026-08-06, 같은 세션 이어서) —
    GPU 길이, 쿨러 냉각 방식(공랭/수랭) 둘 다 사용자가 "안 쓸 것 같다"고
    판단해서 보류 아닌 미채택으로 확정. 이유는 아래 참조. 스펙 필터
    작업은 이 시점 기준 완전히 마무리된 상태 — 다음에 또 "스펙 필터
    확장할까?" 검토 안 해도 됨(새로 강하게 필요해지면 그때 재논의)
    → GPU 길이: 사용자가 제공한 다나와 "그래픽카드" 검색결과 #prodArea를
      실측해보니, 지금까지 쓴 모든 스펙 필터(RepOption 방식,
      "{속성코드}-{값코드}-OR")와 달리 "가로(길이)" 필터는 다른 UI
      체계인 "상세검색"(searchAttributeValue[] 방식,
      "{카테고리seq}|{속성seq}|{값seq}|OR", 예: "753|682|3993|OR")라
      우리 danawa.py의 attribute= 파라미터가 이 형식도 받아주는지 실증된
      적이 없었음. 값도 10mm 단위로 23개(140~149mm~360mm 이상)나 쪼개져
      있어 다중 결합 미검증인 채로는 "300mm 이상"처럼 뭉뚱그린 옵션도 못
      만듦 — 검증 부담 대비 실효성이 낮다고 판단해 사용자가 직접 미채택
      결정. 카테고리 nav의 "길이 300mm 미만"(cate=11355105) 서브카테고리
      링크는 고정 URL이라 select 필터로 못 씀
    → 쿨러 냉각 방식: 애초 2026-08-05 세션에서도 "검색어로 대체 가능"
      이유로 미채택했던 항목 — 이번에 다시 검토했으나 재확인 없이 사용자가
      그대로 미채택 재확정. main.py 주석에 속성코드(315758)까지는 남아있어
      나중에 필요해지면 값별 코드만 다시 실측하면 됨
  → 그 외 미결 사항은 그 아래 2026-08-06 섹션("검색 UX 개편") 및
    "수정 예정 사항" 목록 참조(CORS 좁히기/APScheduler 등, 전부 보류 중)
```

---

## 오늘(2026-08-06) 검색 UX 개편 + 빌드 카드 배지 재배치
```
직전 세션(디자인 다듬기, v0.9.2)에서 컴포넌트 구조 변경이 필요해 보류했던
디자인 패치노트 2건을 사용자 확인 후 처리(frontend v0.10).

[✓] 빌드 카드 판정 배지 좌상단 겹침 배치
  → 기존: 카드 안쪽 상단에 인라인으로 놓인 태그(margin-bottom으로 제목과
    분리). 변경: 카드 hairline 위쪽에 살짝 걸치는 탭 형태로 이동
    (.build-card{position:relative}, .bc-tag{position:absolute;
    top:-13px;left:-1px}). 고가/저가는 잉크 완전 채움, 적정은 아웃라인만
  → 사용자가 "카드 유지 + 배지만 좌상단 겹침" 선택 — 홈/빌드목록이 공유하는
    .build-card 그리드 자체는 안 건드림(리스트형 전환 선택지는 기각)

[✓] 검색 화면(SearchPage) 카테고리+견적 카트 플로우로 전면 재구성
  → 사용자가 "전체 적용 — 새 검색+카트 플로우로" 선택. 기존엔 카테고리
    없이 키워드만으로 검색하는 단순 목록이었음(스펙 필터 자체가 아예
    없었음 — PartRow에만 있었음)
  → 새 구조: 카테고리 탭 8개(CATEGORIES 공유) → 검색창+스펙필터(카테고리별
    CATEGORY_SPEC_FILTERS 재사용) → 정렬탭(인기순 API순서 그대로/가격순은
    프론트 재정렬) → 결과 리스트(행마다 "담기" 버튼) → 우측 견적 카트
    패널(카테고리별 슬롯 8개, 채워진 슬롯 클릭시 제거, 빌드 이름 입력 +
    "이 구성으로 빌드 만들기" 버튼)
  → "빌드 만들기" 버튼은 기존 POST /builds(api.createBuild) 그대로 재사용
    — 새 엔드포인트 없음, BuildCreatePage와 마찬가지로 성공 시 새 빌드
    상세로 navigate
  → CATEGORIES + CATEGORY_SPEC_FILTERS(스펙 select 정의)가 기존엔
    PartRow.tsx 안에 갇혀 있었는데, SearchPage도 똑같은 정의가 필요해져서
    frontend/src/lib/specFilters.ts로 추출 — PartRow/BuildCreatePage/
    SearchPage 3곳이 이제 이 파일 하나를 단일 소스로 공유(main.py 주석의
    "키가 일치해야 함" 경고를 코드 레벨로 강제하게 됨)
  → 검증: npx tsc --noEmit 통과, mock 백엔드+Playwright로 카테고리 전환
    (CPU→GPU→쿨러, 스펙필터 자동 교체 확인)/스펙필터 지정 검색/담기→카트
    반영/다른 카테고리 담아도 기존 카트 유지/정렬 탭 전환/실제
    저장→빌드상세 이동까지 end-to-end 확인(CPU+GPU 담아 저장 → 총액
    162.8만원 = 62.9+99.9 정확히 일치). 8개 라우트 전부 pageerror 0건,
    BuildCreatePage(8행)·StatsPage(1행) part-row 개수로 specFilters.ts
    추출이 기존 두 화면을 안 깨뜨렸는지도 확인

[동작특성] 검색 화면 카트와 빌드생성(BuildCreatePage) 화면은 완전히
  독립된 두 진입 경로 — 상태를 공유하지 않음(둘 다 각자 로컬 state).
  검색에서 담다가 새로고침하면 카트 날아감(BuildCreatePage의 PartRow
  선택과 동일한 특성, 원래도 그랬음 — 새로 생긴 문제 아님)

다음에 이어갈 것:
  → 커밋/push 완료 여부 확인 필요(다음 세션에서 git log로 확인)
  → 디자인 패치노트 나머지 항목(제품 종류/버튼 disabled 스타일 등)은
    이미 v0.9.2에서 처리 완료, 이번 v0.10으로 패치노트 전체 항목 소진
```

---

## 오늘(2026-08-05) 쿨러 스펙 필터 추가
```
스펙 필터 확장의 마지막 남은 카테고리(쿨러) 완료 — 이걸로 8개 카테고리 중
스펙 필터가 없는 곳은 없어짐. backend v0.7 / frontend v0.9.1.

채택: 제품 종류(5종) + 지원 소켓(4종), select 2개(기존 GPU·메인보드와 동일).
  → COOLER_TYPE_ATTRIBUTES: CPU 쿨러 / 시스템 쿨러 / VGA 쿨러 /
    M.2 SSD 쿨러 / 써멀그리스 (속성코드 687). 다나와 "쿨러/튜닝" 카테고리엔
    케이스팬·써멀그리스·조명기기·VGA 지지대가 다 섞여 있어서 category
    필터만으론 CPU 쿨러가 안 걸러짐 — 이 카테고리에 특히 필요했던 필터
  → COOLER_SOCKET_ATTRIBUTES: AM5/AM4/LGA1851/LGA1700. 쿨러는 소켓 필터가
    인텔(6805)/AMD(6806) 두 그룹으로 갈려 있어 값에 따라 속성코드까지
    달라지는데, socket 파라미터 하나 + 기존 4개 값 계약은 그대로 유지하려고
    한 딕셔너리로 합침. 프론트도 SOCKET_OPTIONS 그대로 재사용
  → AM5 코드가 CPU/메인보드/쿨러 셋 다 다름(41-/500-/6806-) — 카테고리 간
    attribute 재사용 불가 원칙 재확인
  → 미채택: 냉각 방식(공랭/수랭, 속성코드 315758). 검색어로 대체 가능하고,
    어차피 같은 카테고리 스펙끼리는 상호배타라 3개를 넣어도 동시 사용 불가
  → 상세(실측값 전체·제외 근거·검증 3단계)는 실가_HISTORY.md 마지막 항목 참조

이번 세션 환경도 danawa.com 프록시 차단 지속(CONNECT 403) — 사용자가 "CPU
쿨러" 검색 결과 #prodArea outerHTML을 통째로 제공해서 진행. 필터 사이드바 +
상품목록 40건이 다 들어있어서 attribute 코드 확보(체크박스 480개 파싱)와
카테고리 사후 필터링 검증을 한 파일로 같이 할 수 있었음 — 다음에 스펙을 더
확장할 때도 이 형태(#prodArea outerHTML)로 받는 게 제일 효율적임

다음에 이어갈 것:
  → 스펙 필터는 여기서 일단락. 더 확장한다면 쿨러 냉각 방식(위 미채택),
    SSD 폼팩터, GPU 길이 정도가 후보 — 매번 그 카테고리 필터 사이드바
    실측 필요
  → 부수 발견(미수정): 케이스 행 select placeholder "지원 폼팩터 전체"가
    116px 폭에서 "지원 폼팩터 전"으로 잘림. PR #7 때부터 있던 것,
    기능 영향 없음
```

---

## 오늘(2026-08-05) 프론트 디자인 전면 교체 + 스펙시트 고도화
```
2차(스펙시트 고도화, 커밋 대기 중) — 1차 교체는 색/폰트/보더만 바꿨고
참조 디자인의 구조적 요소가 대부분 미사용이었어서, 그걸 실제로 이식하고
빌드 사이트(PCPartPicker) 관례를 얹음:
  → 빌드 상세를 실제 견적서 구조로 재구성: 4칸 스탯 스트립 / 큰 실측 합계 /
    실측↔판매가 대비 + 차액 배지 / 부품을 기능 그룹(연산부·그래픽·
    메모리저장·섀시전원)으로 묶은 스펙 행 + 총액 대비 비중 바 / 소계
  → 이동평균 무효로 즉시가 대체된 부품을 행 단위로 표시(기존엔 빌드
    전체 신뢰도 한 줄로만 알 수 있었음)
  → 빌드 생성 화면에 sticky 요약 바("구성 N/8" + 진행 틱 + 러닝 총액) —
    이걸 위해 SelectedPart에 raw price 필드 추가
  → 가격 정렬용 tabular-nums 전면 적용, 인쇄(견적서 출력) 스타일 추가
  → 미채택: 소비전력 추정(PCPartPicker 핵심 기능이나 부품별 TDP 데이터가
    스크래퍼에 없음 — 하려면 백엔드 작업 필요)
  → 상세는 실가_HISTORY.md 2026-08-05 마지막 항목 참조

1차(커밋 db61895, push 완료) — PR #7(카테고리·스펙 필터) 머지 확인 후,
사용자가 제공한 참조 디자인(에디토리얼 빌드 명세서 HTML, 종이/잉크 톤)으로
프론트 전체 톤을 교체함. 브랜치 claude/danawa-category-spec-filters-a04sod
(PR은 사용자가 직접 관리 중, 이 세션에서 PR 생성 안 함).

이번 교체 요약(자세한 팔레트/규칙은 REFERENCE.md #디자인-토큰 참조):
  → 이전 다크+시안/마젠타/앰버 네온 톤(Black Han Sans+JetBrains Mono) 폐기,
    종이(paper)/잉크(ink) 모노크롬 에디토리얼로 전면 교체 — accent color
    없음, 상태 구분은 ▲(고가)/▼(저가)/—(적정가) 기호로
  → 폰트: Pretendard(헤드라인 포함 전체) + IBM Plex Mono(데이터/코드).
    index.html의 구글폰트 링크 교체 완료(Black Han Sans 제거,
    IBM Plex Mono 추가)
  → border-radius/box-shadow 글로우 전면 제거, hairline(1px) 보더로만
    위계 표현. frontend/src/styles/global.css 전체를 다시 씀(546줄 규모
    전면 재작성, 클래스명은 유지해서 TSX는 대부분 안 건드림)
  → 빌드 상세(BuildDetailPage.tsx)의 판정 게이지를 SVG 반원+니들에서
    flat 수평 바(.gauge-track/.gauge-zone/.gauge-marker)로 재설계.
    diff_percent를 ±30%로 클램프해 바 위 0~100% 위치에 매핑,
    VERDICT_THRESHOLD_PERCENT(±5%, services/verdict.py)를 음영 구간으로
    표시. needleAngle() → markerPosition()으로 함수도 교체
  → 통계 탭(StatsPage.tsx) 가격 히스토리 차트: 글로우 필터+그라디언트
    라인 제거, flat ink 라인+옅은 회색 fill로 교체
  → 실측 발견 버그: build-grid에 쓴 "그리드 라인 트릭"(컨테이너
    background+gap:1px)이 아이템 개수가 3의 배수가 아닐 때 빈 셀에
    컨테이너 배경색이 그대로 노출되는 문제 발견 → 개별 카드에 자체
    border를 주는 방식으로 수정. flex-column 리스트(search-results,
    recent-list)는 이 문제 없어서 트릭 그대로 유지 — REFERENCE.md에
    이 함정 기록해둠
  → 검증: mock danawa 함수 + 임시 SQLite로 실제 빌드/즐겨찾기/히스토리
    데이터를 채워서 Playwright로 9개 화면(홈/검색/빌드목록/빌드생성/
    빌드상세(게이지 포함)/즐겨찾기/최근기록/통계(차트 포함)/설정) 전부
    실브라우저 확인. npm run typecheck 통과

다음에 이어갈 것:
  → 사용자가 Codespaces에서 직접 눈으로 확인 예정 — 이상 있으면 후속 조치
  → [완료] 쿨러 스펙 필터는 2026-08-05 이어진 세션에서 구현 완료
    (맨 위 섹션 참조)

※ 이 섹션의 "커밋 대기 중"은 이후 해소됨 — 2차(스펙시트)까지 커밋 6c226bf로
  들어갔고 PR #9로 머지 완료
```

---

## 오늘(2026-08-05) /search 카테고리·스펙 필터 확장 — PR #7
```
/search에 카테고리 필터 + 카테고리별 스펙 필터 구현 완료. 브랜치
claude/danawa-scraper-filters-bryp6q, PR #6(카테고리 필터+GPU 메모리+CPU
소켓+폴리시 2건, 커밋 eae25c4~16e5ef5)은 이미 머지·종료됨. 그 이후
GPU 칩셋 제조사(1624454)까지 커밋+push 완료, 이어서 진행한 메인보드
소켓/폼팩터·RAM 규격·케이스 지원폼팩터·파워 정격출력·SSD 인터페이스
6종은 아직 커밋 전(다음 "커밋하자" 지시 대기) — 상세 이력은
실가_HISTORY.md 2026-08-05 항목들(여러 개로 나뉘어 append됨) 참조,
여기는 최종 상태만 요약.

핵심 구현 방식(재구현/확장 시 반드시 참고):
  → category 필터: 요청 URL에 새 파라미터 추가하는 방식이 아님. 상품 li
    안의 input#productItem_categoryInfo_{code} 값(예: "PC 주요
    부품_그래픽카드")을 응답 파싱 단계에서 대조하는 사후 필터링
    (danawa.get_product_codes(category_label=...)). CATEGORY_LABELS 8개
    라벨 전부 실제 상품 li HTML로 직접 검증 완료(간접 검증 남은 것 없음)
  → 스펙 필터(memory_gb/chipset/socket/formfactor/ram_type/wattage/
    interface): 다나와 상세검색 필터 체크박스 클릭 시 URL에 실제로 붙는
    "{속성코드}-{값코드}-OR"(케이스만 -AND, 단일값 선택엔 동작 차이 없음)
    문자열을 danawa.get_product_codes(attribute=...)로 요청 URL에 그대로
    전달 — 이건 다나와 서버측 필터라 사후 필터링 아님
  → **카테고리 간 attribute 코드는 절대 재사용 불가** — 같은 물리적
    스펙(예: AM5 소켓)이라도 카테고리마다 다나와 내부 코드 자체가 다름
    (CPU 카테고리 AM5=41-801631-OR, 메인보드 카테고리 AM5=500-801682-OR).
    새 카테고리/스펙으로 확장할 때마다 매번 그 카테고리 자신의 필터
    사이드바에서 다시 확보해야 함(대표 검색어로 검색 → 필터 체크박스
    아무거나 클릭 → 바뀐 URL의 attribute= 값 확인, 또는 페이지 HTML의
    체크박스 value 속성 직접 읽기)
  → 같은 카테고리 안에 스펙 필터가 2개인 경우(GPU: memory_gb+chipset,
    메인보드: socket+formfactor) **동시 적용 불가** — get_product_codes의
    attribute 인자가 값 하나만 받음(다중 attribute 결합 규칙 미검증).
    백엔드는 `A.get(x) or B.get(y)` 체이닝으로 우선순위 처리, 프론트
    (PartRow)는 같은 카테고리의 스펙 select를 상호 배타로 구현(하나
    고르면 다른 하나 자동 해제) — 자세한 우선순위는 REFERENCE.md
    #엔드포인트-설계 표 참조
  → 데이터 트리밍 원칙: 워크스테이션/서버/구형/데이터센터급 값(예: GPU
    94~96GB, CPU/메인보드 sWRX8·sTRX4·SP3 등 구형 소켓)은 실측은 됐어도
    개인 조립 PC 범위 밖이라 매핑 딕셔너리에서 의도적으로 제외 — 신규
    스펙 추가 시에도 같은 원칙 적용

현재 CATEGORY_LABELS(카테고리 필터, 8개 전부) + 스펙 필터 매핑
(backend/app/main.py):
  GPU      → memory_gb(GPU_MEMORY_ATTRIBUTES, 11개), chipset(GPU_CHIPSET_ATTRIBUTES, NVIDIA/AMD/Intel)
  CPU      → socket(CPU_SOCKET_ATTRIBUTES, AM5/AM4/LGA1851/LGA1700)
  메인보드 → socket(MAINBOARD_SOCKET_ATTRIBUTES, 값은 CPU와 다름), formfactor(MAINBOARD_FORMFACTOR_ATTRIBUTES, ATX/M-ATX/ITX/E-ATX)
  케이스   → formfactor(CASE_FORMFACTOR_ATTRIBUTES, "장착 가능한 보드 크기" 의미 — 메인보드 폼팩터와 라벨은 같지만 코드는 다름)
  RAM      → ram_type(RAM_TYPE_ATTRIBUTES, DDR5/DDR4)
  파워     → wattage(PSU_WATTAGE_ATTRIBUTES, "450W~499W"~"1000W~1299W" 7구간)
  SSD      → interface(SSD_INTERFACE_ATTRIBUTES, SATA3/PCIe3.0x4/PCIe4.0x4/PCIe5.0x4)
  쿨러     → 카테고리 필터만(스펙 필터 없음)

프론트(frontend/src/components/PartRow.tsx): CATEGORY_SPEC_FILTERS 설정
객체로 카테고리→스펙 select 목록을 선언적으로 관리(select 반복 렌더링,
상호배타는 단일 specValue 상태로 자연스럽게 보장됨 — 여러 개의 boolean
필드 대신 {key, value} 하나만 유지). api.ts::api.search(q, category?,
spec?)의 spec은 SearchSpecParams 객체(URLSearchParams로 쿼리 조립) —
포지셔널 인자가 계속 늘어나는 걸 막으려고 이번에 리팩터함. select 폭은
116px(긴 라벨 "800W~899W" 대응), 자동완성 드롭다운은 그 행에 보이는
select 개수(specDefs.length) 기반 인라인 스타일로 겹침 방지.

검증 방식(전부 이 패턴 반복 — 다음에도 재사용): ① 사용자가 제공한 실제
danawa 상품목록/필터 HTML을 requests.get mock 응답으로 고정해 백엔드
필터링 로직 오프라인 검증 ② FastAPI TestClient로 쿼리파라미터 조합별
attribute 전달 경로 확인 ③ mock 백엔드(danawa 함수 monkeypatch) +
Vite dev 서버를 Playwright(사전 설치 Chromium)로 실제 조작해서 UI까지
확인 — 이 과정에서 실제 CSS 버그 2건 발견+수정함(select appearance
리셋 누락으로 폭 무시되던 문제, 자동완성 드롭다운이 select를 덮던 문제)

다음에 이어갈 것:
  → [완료] 위 6종 커밋+push 완료(ab60a3e, f31c295), PR #7 생성·머지 완료.
    이후 PR #8(UI 톤 다듬기)·PR #9(디자인 교체+스펙시트)까지 머지됨
  → [완료] 쿨러 지원 소켓 — 2026-08-05 이어진 세션에서 제품 종류와 함께
    구현 완료(맨 위 섹션 참조)
  → 더 확장하려면(예: SSD 폼팩터) 매번 그 카테고리 필터 사이드바 실측
    필요 — 이 환경은 danawa.com 프록시 차단 지속이라 사용자가 로컬
    브라우저에서 HTML 복사해서 전달하는 방식 계속 필요
```

---

## 오늘(2026-08-04) 세션 마무리
```
PR #3 main 머지 완료 (2026-08-04) — 아래 1~5번이 main에 반영됨. 6~7번은
같은 날 이어서 작업, 아직 커밋/PR 전(다음 "커밋 하자"/"PR 하자" 지시 대기).
브랜치 claude/frontend-moving-average-integration-qkazx9는 머지된 main
기준으로 리셋해둠(과거 커밋 위에 새로 쌓지 않도록).

이번 세션에 완료(1~5는 PR #3, 커밋 순서대로):
  1. verdict 이동평균 프론트 연동 — 설정 탭 드롭다운 + localStorage (v0.2)
  2. _fetch_lowest_price danawa 연결 장애 처리 누락 수정 (v0.4.2)
  3. category/cash_price 필드 스크래퍼 구현 (v0.4.3)
  4. 통계 탭 가격 히스토리 차트 구현 (frontend v0.3)
  5. 홈 탭 최근빌드 대시보드 구현 (frontend v0.4)
  6. 최근기록 탭 — 최근 조회한 부품 구현 (frontend v0.5) — 커밋/push 대기
  7. 즐겨찾기 탭 — 관심 상품 북마크 구현, favorites DB 테이블 신규
     (backend v0.4.4, frontend v0.6, 아래 "우선순위 낮음" 섹션 참조) —
     커밋/push 대기

사이드바 7탭 전부 실데이터 연동 완료 — PlaceholderPage 컴포넌트 자체를
삭제함(더 이상 아무 데서도 안 씀).

이어서(같은 날, v13) 완료:
  → frontend .env 기반 API_BASE 문서화 완료(.env.example 신규 +
    README.md 보강, "현재 구현 상태" 표도 스테일해서 같이 최신화)
  → scripts/e2e_smoke_test.py에 홈/통계/최근기록/즐겨찾기 4개 화면 스텝
    추가 완료(기존 4스텝 + 신규 8스텝, 총 12스텝) — mock 백엔드 +
    Playwright로 전체 흐름 실제 실행해서 통과 확인(아래 "우선순위 높음"
    섹션 v13 참조)
  → 커밋/push 대기 (다음 "커밋 하자" 지시 대기)

남은 것(순서 무관, 모두 낮은 난이도/보류):
  → 배포 시 CORS allow_origins=["*"] 좁히기는 실제 배포 계획이 생길 때까지
    계속 보류(인수인계.md에 명시된 기존 결정 유지)

이 세션 환경 특이사항 (다음 세션에도 반복될 수 있음):
  → 프록시 정책이 danawa.com 전체를 막고 있어(curl/WebFetch 둘 다 403)
    라이브 다나와 검증이 이 환경에서는 불가능함 — 사용자가 로컬 브라우저에서
    HTML을 직접 복사해서 붙여넣어주는 방식으로 실측 진행함(category/
    cash_price 구현 때 실제로 이렇게 함). 같은 상황이면 이 방법 재사용
  → 프론트 기능 검증은 danawa.* 함수를 python mock으로 교체한 로컬
    백엔드 + Playwright 조합으로 진행 (이번 세션 통계/홈/최근기록/즐겨찾기
    탭 검증에 전부 사용, 재사용 가능한 패턴)
  → 즐겨찾기 탭 구현 중 이 mock+Playwright 검증 과정에서 실제 프론트
    버그 하나 발견+수정함(PartRow를 selected=null 고정으로 재사용하면
    두 번째 검색부터 자동완성이 안 뜸 — 아래 "결정 완료"/우선순위 낮음
    섹션 참조). mock 검증이 단순 확인 이상으로 실제로 버그를 잡아낸 사례
```

---

## 읽기 순서 안내
```
① 실가_인수인계.md (이 파일)  — 세션 시작 시 필수. 현재 상태/수정예정/미결사항
② 실가_REFERENCE.md           — 규칙/구조/절차. 작업과 관련된 섹션만 필요 시 확인
③ 실가_HISTORY.md             — 변경 이력 (append-only). 특정 시점 이력 추적 시에만 확인

문서 갱신 규칙:
  코드 작성/수정 시 → 이 파일(상태/수정예정) 갱신 + HISTORY.md append
  REFERENCE.md는 규칙/구조 자체가 바뀔 때만
```

---

## 파일별 최신 버전
```
app-shell-mockup.html    v0 (정적 목업, 유지 — 프론트 실 구현체(frontend/)가
                             이 디자인을 그대로 이식해서 대체함)
danawa_patched.py        편입 완료 → backend/app/services/danawa.py (v0.2).
                             get_price_variance 원본과 통합된 단일 모듈.
                             (git 리포: 커밋 4개, v0.2~v0.4 + docs)
backend/                  v0.19 (2026-08-23. GPU 칩셋 모델 필터 —
                             GPU_CHIP_ATTRIBUTES(실측 120종 중 현행 25종) +
                             /search gpu_chip 파라미터. chipset이 제조사라면
                             이건 모델. 새 파라미터라 두 번째 자리 v0.18→v0.19)
                          v0.18 (2026-08-23. 홈 응답 분리 — /builds에
                             with_prices 파라미터 + /builds/prices 신설.
                             구조는 DB만 읽어 즉시, 가격은 따로 받아 채움.
                             새 엔드포인트라 두 번째 자리 v0.17→v0.18)
                          v0.17 (2026-08-23. /search에 page 파라미터 신설
                             (다나와 40건 상한 해제) + SearchResultItem에
                             category 필드 추가(결과가 부품 단품인지 완제품
                             PC인지 구분). 새 파라미터 + 응답 필드 추가라
                             두 번째 자리 v0.16.1→v0.17)
                          v0.16.1 (2026-08-22, 같은 세션 이어서. 다나와 조회
                             속도 — danawa.py 파서를 html.parser→lxml +
                             SoupStrainer(상품목록 div만 파싱)로 교체해 검색
                             페이지 파싱 1.50s→0.45s. list_builds에 요청 단위
                             중복 제거(price_memo/ma_memo) 도입해 빌드 간
                             공유 부품 재조회 제거(실측 80회→48회). 응답
                             동일이라 세 번째 자리 v0.16→v0.16.1)
                          v0.16 (2026-08-22, 같은 세션 이어서. RAM/SSD/메인보드/
                             파워/케이스 스펙 필터 6종 추가 — capacity(RAM
                             총용량·SSD 용량 구간 공유) / ram_count /
                             efficiency(파워 80PLUS) / case_size 신설 +
                             chipset을 메인보드 세부 칩셋(19종)까지 확장.
                             새 쿼리파라미터 4개라 두 번째 자리 v0.15→v0.16)
                          v0.15 (2026-08-22, 같은 세션 이어서. CPU 내장그래픽
                             스펙 필터 신규 — CPU_IGPU_ATTRIBUTES(속성코드
                             15570, 탑재/미탑재) + /search igpu 파라미터.
                             socket/cpu_type과 3중 동시 지정 가능. 새
                             쿼리파라미터라 두 번째 자리 v0.14→v0.15)
                          v0.14 (2026-08-22, 같은 세션 이어서. CPU 종류 스펙
                             필터 신규 — CPU_TYPE_ATTRIBUTES(속성코드 357316,
                             34종 중 11종 채택) + /search cpu_type 파라미터.
                             socket과 동시 지정 가능(v0.13 AND 결합 활용).
                             새 쿼리파라미터 신설이라 두 번째 자리
                             v0.13→v0.14)
                          v0.13 (2026-08-22, 새 세션. /search의 같은 category
                             스펙 파라미터를 전부 AND로 동시 적용 — `A or B`
                             체이닝을 콤마 조인으로 교체. 다나와가 서로 다른
                             속성코드의 콤마 조인을 AND로 처리한다는 걸 라이브
                             실측으로 확인, 기존 "결합 불가" 제약이 오판이었음.
                             파라미터명/응답 구조는 그대로지만 무시되던 조합이
                             적용되는 동작 변경이라 두 번째 자리 v0.12→v0.13)
                          v0.12 (2026-08-13, 새 세션. BuildSummary에
                             diff_percent 필드 추가 — list_builds가 이미
                             calc_verdict로 계산하고 버리던 값을 실어 보냄,
                             추가 스크래핑 없음. 계약 필드 추가라 두 번째
                             자리 v0.11→v0.12)
                          v0.11 (2026-08-08, 이어진 세션. DELETE /builds/{id}
                             신규 — 빌드 삭제 엔드포인트. 새 엔드포인트
                             추가라 두 번째 자리 v0.10.1→v0.11)
                          v0.10.1 (2026-08-08. 저장된 빌드(GET /builds,
                             GET /builds/{id})의 verdict 계산에서 market_price
                             필수 게이팅 제거 — 없으면 즉시가를 이동평균
                             기준가와 비교해서 자동 판정. 응답 필드 구조는
                             안 바뀌고(verdict 등 기존 필드 그대로) non-null
                             빈도만 늘어난 동작 변경이라 세 번째 자리
                             v0.10→v0.10.1)
                          v0.10 (2026-08-07, 새 세션. /search q 파라미터
                             선택화(생략 시 CATEGORY_DEFAULT_QUERY로 대체) +
                             SearchResultItem.img 필드 신규 — 검색 버튼 없이
                             카테고리 기본 목록 + 상품 이미지 요청 대응.
                             응답 구조/파라미터 필수여부 변경이라 두 번째
                             자리 v0.9.1→v0.10)
                          v0.9.1 (2026-08-07. GCP e2-micro 실배포 중 발견된
                             버그 2건 수정 — PriceHistory.min/max int→str
                             캐스팅 누락(pydantic ValidationError), danawa.py
                             requests.get() 3곳 timeout 누락(재배포 시 프로세스
                             graceful shutdown 실패로 발견). API 계약 변경
                             없는 버그수정이라 세 번째 자리 v0.9→v0.9.1)
                          v0.9 (2026-08-07. GPU_LENGTH_ATTRIBUTES 신규 —
                             /search에 length 파라미터(GPU 전용) 추가.
                             danawa.com 접근이 이 세션 한정으로 열려서
                             라이브 실측+검증까지 완료. 새 쿼리파라미터
                             신설이라 두 번째 자리 v0.8→v0.9)
                          v0.8 (2026-08-06. SSD_FORMFACTOR_ATTRIBUTES 신규 —
                             기존 formfactor 파라미터를 SSD category에도
                             적용(M.2 2280/2242/2230/2.5인치). 새 파라미터
                             신설은 아니지만 파라미터의 유효 적용범위가
                             늘어난 계약 변경이라 두 번째 자리 v0.7→v0.8)
                          v0.7 (2026-08-05. v0.6에서 쿨러 스펙 필터 2종
                             (cooler_type 신규 파라미터 + 쿨러 전용 socket
                             매핑) 추가 — 쿼리파라미터 신설이라 계약 변경,
                             두 번째 자리 v0.6→v0.7. 아래는 v0.6까지의 내역)
                          v0.6 (danawa 연결 장애 처리 결함 수정 + category·
                             cash_price 필드 스크래퍼 구현 + favorites
                             테이블/엔드포인트 신규 + /search 카테고리 필터
                             + 카테고리별 스펙 필터 7종(GPU 메모리/칩셋,
                             CPU·메인보드 소켓, 메인보드·케이스 폼팩터,
                             RAM 규격, 파워 출력, SSD 인터페이스) 신규 —
                             2026-08-05, API 계약 변경 누적이라 두 번째
                             자리 v0.5→v0.6 재차 올림)
frontend/                 v0.18 (2026-08-23. GPU 칩셋 모델 select 추가 +
                             의존형 select 기구 신설(dependsOn/optionsBy) —
                             제조사를 고르면 그 제조사 칩셋만 뜨고, 제조사를
                             바꾸면 선택이 초기화됨. .part-row flex-wrap과
                             드롭다운 top:100%도 같이. 두 번째 자리
                             v0.17.1→v0.18)
                          v0.17.1 (2026-08-23. 판정 축에서 핀이 하나뿐일 때만
                             라벨 상시 노출 — 2개 이상은 겹침 때문에 기존
                             hover 유지. CSS/렌더 소폭이라 세 번째 자리)
                          v0.17 (2026-08-23. 홈/목록 2단계 로딩 —
                             lib/useBuilds.ts 훅 신설, BuildCard에
                             pricesPending. 대기 중 집계를 0이 아니라
                             "—"로 비움. 두 번째 자리 v0.16→v0.17)
                          v0.16 (2026-08-23. 통계·즐겨찾기 부품 검색에 카테고리
                             select 추가(카테고리 필터가 통째로 무시되던 버그
                             수정, PartRow의 죽어 있던 스펙 필터도 같이 살아남)
                             + 검색 결과 카테고리 배지 + SearchPage
                             useInfiniteQuery 전환과 "더 보기" 버튼.
                             기능 추가라 두 번째 자리 v0.15.3→v0.16)
                          v0.15.3 (2026-08-22, 같은 세션 이어서. 5개 카테고리에
                             스펙 select 6개 추가(메인보드 칩셋, RAM 용량·
                             개수, SSD 용량, 파워 80PLUS, 케이스 크기).
                             SSD는 용량을, 케이스는 크기를 맨 앞으로 재배치.
                             컴포넌트 코드 변경 없음, 세 번째 자리
                             v0.15.2→v0.15.3)
                          v0.15.2 (2026-08-22, 같은 세션 이어서. CPU 스펙 필터에
                             igpu select 추가(종류·소켓·내장그래픽 3개로).
                             formatOption으로 "내장그래픽 탑재/미탑재" 표기.
                             세 번째 자리 v0.15.1→v0.15.2)
                          v0.15.1 (2026-08-22, 같은 세션 이어서. CPU 스펙 필터에
                             cpuType select 추가(종류·소켓 2개로). 컴포넌트
                             코드 변경 없이 specFilters.ts 정의만 늘림 —
                             SSD formfactor·GPU length 추가 때와 같은 패턴이라
                             세 번째 자리 v0.15→v0.15.1)
                          v0.15 (2026-08-22, 새 세션. 스펙 필터 select 상호배타
                             해제 — lib/specFilters.ts에 useSpecFilters() 훅
                             신설해 3벌 복붙 상태 로직을 통합, 슬롯 하나에서
                             SearchSpecParams 객체로 교체. 결론 블록에 활성
                             조건 표시 + "조건 지우기" 버튼 + 40건 상한 표기
                             추가. 백엔드 v0.13 대응이라 두 번째 자리
                             v0.14.4→v0.15)
                          v0.14.4 (2026-08-13, 새 세션. 견적서 인쇄 @page
                             margin 12mm 균등 → 10mm 8mm(상하/좌우) — Chromium
                             실렌더링으로 5개 후보 비교 후 채택. 페이지 하단
                             죽은 공간(40mm+)은 margin과 무관한 별개 문제로
                             확인만 하고 미착수. CSS만이라 세 번째 자리
                             v0.14.3→v0.14.4)
                          v0.14.3 (2026-08-13, 이어진 세션. 견적서 인쇄 여유
                             확보 — 1차 압축은 여유 78px뿐이라 인쇄 머리글·
                             바닥글이 켜진 환경에서 합계가 다음 장으로 떨어졌음.
                             게이지 바를 종이에서 빼고 889px로. 세 번째 자리)
                          v0.14.2 (2026-08-13, 이어진 세션. 견적서 인쇄를
                             A4 1장으로 압축 — @media print 전용 치수.
                             부품 8종 기준 3장→1장. CSS만이라 세 번째 자리
                             v0.14.1→v0.14.2)
                          v0.14.1 (2026-08-13, 같은 세션. 홈 판정 축(.axis)
                             추가 — 라벨은 hover/포커스한 핀 하나만(개편안 B).
                             lib/verdictScale.ts로 상세 게이지와 좌표계 공유,
                             components/VerdictAxis.tsx 신규. 화면 추가 없는
                             컴포넌트 추가라 세 번째 자리 v0.14→v0.14.1)
                          v0.14 (2026-08-13, 새 세션. 사용자 제공 C안 목업
                             전면 적용 — 전 화면 결론 블록(.answer, 로딩/실패
                             변형 포함), --rule-1/2/3 두께 토큰, 사이드바 접힘
                             폐기 + 탑바 페이퍼 톤, 인쇄 배경 소실 수정,
                             상세 스택바/범례, 빌드생성 4열 행, 카트 진행 틱 +
                             러닝 소계, 빌드 목록 판정 필터. components/
                             Answer.tsx·BuildCard.tsx, lib/format.ts 신규.
                             화면 구조 전면 변경이라 두 번째 자리 v0.13.1→v0.14)
                          v0.13.1 (2026-08-08, 이어진 세션. 빌드 삭제 "×"
                             버튼을 BuildListPage/HomePage 카드에도 확장
                             (사용자 "다 추가해줘" 지시). lib/useDeleteBuild.ts
                             신규 훅으로 confirm+mutate+쿼리무효화 로직을
                             3화면이 공유하도록 리팩터(BuildDetailPage도
                             이 훅으로 정리). 백엔드 변경 없는 프론트
                             전용 확장이라 세 번째 자리 v0.13→v0.13.1)
                          v0.13 (2026-08-08, 이어진 세션. BuildDetailPage
                             우측 상단에 빌드 삭제 "×" 버튼 추가 —
                             window.confirm 확인 후 DELETE /builds/{id}
                             호출, 성공 시 목록으로 이동. api.deleteBuild
                             신규 함수라 두 번째 자리 v0.12.1→v0.13)
                          v0.12.1 (2026-08-08, 이어진 세션. BuildDetailPage
                             판정 게이지가 market_price 없어도(즉시가 vs
                             이동평균으로) 항상 뜨도록 수정 + BuildCreatePage
                             "비교할 판매가" 힌트 문구 갱신. 백엔드 v0.10.1
                             동작 변경에 맞춘 대응이라 세 번째 자리
                             v0.12→v0.12.1)
                          v0.12 (2026-08-08. 새 빌드 만들기 화면을 마스터
                             (부품 목록)-디테일(검색 패널) 2단 레이아웃으로
                             전면 재구성 — PartSearchPanel.tsx 신규(BuildCreatePage
                             전용, PartRow.tsx는 다른 화면 3곳이 재사용 중이라
                             건드리지 않음). 사용자가 다른 Claude 세션에서
                             디자인해온 것을 그대로 구현. API 계약 변경은
                             없지만 화면 구조가 실질적으로 바뀌어 두 번째
                             자리 v0.11→v0.12)
                          v0.11 (2026-08-07, 새 세션. 검색 화면이 검색 버튼
                             없이 카테고리 기본 목록을 자동으로 보여주도록
                             변경(useQuery enabled 조건 제거) + 검색 결과에
                             40×40 썸네일 이미지 추가. 백엔드 계약 변경에
                             맞춘 동작 변경이라 두 번째 자리 v0.10.2→v0.11)
                          v0.10.2 (2026-08-07. GPU 스펙 필터에 length select
                             추가(specFilters.ts GPU 항목에 항목 하나 추가한
                             것뿐 — 컴포넌트 로직 무변경, SSD formfactor 추가
                             때와 동일 패턴). 세 번째 자리)
                          v0.10.1 (2026-08-06. SSD 스펙 필터에 formfactor
                             select 추가(specFilters.ts SSD 항목에 항목 하나
                             추가한 것뿐 — PartRow/SearchPage 컴포넌트 로직은
                             무변경, 둘 다 specDefs 배열 기반 범용 렌더링이라
                             자동 확장됨). 세 번째 자리)
                          v0.10 (2026-08-06. 보류했던 디자인 패치노트 2건
                             처리 — (1) 빌드 카드 판정 배지를 상단 인라인
                             태그에서 카드 좌상단 hairline에 걸치는 탭
                             형태로 재배치(.build-card position:relative +
                             .bc-tag absolute, 카드 그리드 구조는 유지),
                             (2) 검색 화면을 카테고리 탭 + 스펙필터 +
                             정렬탭 + 견적 카트 2단 레이아웃으로 전면
                             재구성(SearchPage.tsx 재작성) — 검색하며 여러
                             카테고리를 담아 그 자리에서 빌드로 저장하는
                             새 진입 경로 신설(기존 POST /builds 그대로
                             재사용, 새 엔드포인트 없음). PartRow/
                             BuildCreatePage와 공유하던 CATEGORIES·
                             CATEGORY_SPEC_FILTERS를 lib/specFilters.ts로
                             추출해 3곳(PartRow/BuildCreatePage/SearchPage)
                             단일 소스화. 화면 구성이 실질적으로 바뀌어
                             두 번째 자리 올림 — 백엔드 API 계약 변경 없음.
                             상세는 실가_HISTORY.md 참조)
                          v0.9.2 (2026-08-06. 사용자가 claude.ai에서 다듬어온
                             디자인 패치노트 반영 — 버튼 위계(secondary 추가),
                             .part-spec-filter 116→144px 잘림 수정, 게이지
                             트랙 잉크 반전+퍼센트 라벨, .sum 잉크 반전,
                             부품 그룹 2px 보더 컨테이너, 탑바 인장+브레드크럼.
                             API 계약 변경 없음, CSS/마크업만 — 세 번째 자리)
                          v0.9.1 (2026-08-05. 쿨러 스펙 필터 select 2개 추가.
                             CATEGORY_SPEC_FILTERS 설정 객체에 항목 하나
                             추가한 것뿐이라 컴포넌트 로직 무변경 —
                             세 번째 자리)
                          v0.9 (2026-08-05. v0.8.1에서 다크네온→종이/잉크
                             모노크롬 에디토리얼로 톤 전면 교체, 이어서
                             v0.9에서 빌드 상세를 견적서 구조로 재구성 +
                             빌드 생성 sticky 러닝 총액 + 인쇄 스타일 추가.
                             화면 구성 자체가 바뀌어 두 번째 자리 올림
                             — 백엔드 API 계약 변경은 없음)
```
**GitHub 원격 리포 연동 완료** — origin이 `limfighter/silga`로 설정돼 있고
이번 세션 작업 전부 push까지 끝남(브랜치:
`claude/frontend-moving-average-integration-qkazx9`, PR 아직 미생성).
실행 환경(컨테이너)이 세션마다 리셋돼도 git 리포에 커밋+push만 해두면
다음 세션에서 그대로 이어받을 수 있음 — 예전엔 zip 업로드가 필요했지만
더 이상 해당 없음.

---

## 현재까지 확정된 사항 (요약 — 상세는 REFERENCE.md)
```
- 개발 순서: 웹 먼저 → API 명세 확정 → Flutter 이식 (디버깅 편의 이유로 결정)
- 백엔드: FastAPI
- 데이터소스 1순위: danawa-py 기반, 단 원본 그대로는 못 씀 — 자체 패치본
  (danawa_patched.py) 사용 확정. 근거는 아래 "검증 안 된 항목" 결과 참조
- 화면 구조: 사이드바 7탭 확정, 빌드 탭 3단계 흐름(목록/생성/상세) 목업 완료
- API 계약 초안 확정 (REFERENCE.md #엔드포인트-설계) — 프론트 목업이 이 계약 전제로 만들어짐
- 디자인 토큰 확정 (2026-08-05 개정 — 종이/잉크 모노크롬 에디토리얼,
  Pretendard+IBM Plex Mono. 이전 다크+시안/마젠타/앰버 네온+Black Han Sans는
  폐기. 상세는 REFERENCE.md #디자인-토큰 참조)
- 프로젝트 정식 이름 — **실가**로 확정 (2026-08-03, 이전 임시 코드네임: PPE)
```

---

## 검증 안 된 항목 (코드 작성 전 반드시 확인)
```
[x] ★최우선 — danawa-py 실제 호출 테스트 (2026-08-03 완료)
    → 3개 함수 모두 예외 없이 호출은 됐으나, 2개 함수는 다나와 측 HTML/DOM
      구조 변경으로 필드 일부가 비어서 나옴(파싱 실패). 상세는 HISTORY.md
      2026-08-03 v1 항목 참조. 요약:
        - get_product_codes: 검색어 URL 인코딩 누락 + 카테고리별 li 클래스
          불일치(GPU만 우연히 동작, CPU/RAM/SSD 등은 0건)로 패치 필요 → 완료
        - get_product: 최저가 섹션 클래스가 lowest_area → price-summary로
          변경되어 lowest_price/prices가 항상 빈 값으로 나옴 → 패치 완료,
          쇼핑몰명 + variants(정품/벌크/해외구매) 필드까지 확장
        - get_price_variance: 원본 그대로 정상 동작, 패치 불필요
    → 패치본: danawa_patched.py (get_product_codes, get_product 2개 함수만
      재작성 포함. get_price_variance는 원본 danawa.py 그대로 import해서 사용)

[x] danawa_patched.py 실가 프로젝트 정식 경로 편입 → 완료(체크 누락 상태로
    방치돼 있던 걸 2026-08-05 세션에서 발견해서 정정). backend/app/services/
    danawa.py로 vendoring 완료된 지 오래됨 — 파일 상단 도크스트링에 출처
    명시돼 있고, 이후 세션들에서 이 파일에 계속 패치를 쌓아왔음(category_label/
    attribute 인자 등, 실가_HISTORY.md 참조)
    → PyPI 미등록 + setup.py 없음 확인됨 → pip install 불가, 파일 자체를
      프로젝트에 vendoring(복사)하는 방식으로 편입(git submodule 등 X)

[x] 다나와 공식 오픈API(api.danawa.com) 생사 확인 → 2026-08-07, danawa.com
    접근이 일시적으로 열린 틈에 curl로 직접 확인
    → api.danawa.com / developer.danawa.com은 프록시 CONNECT 단계에서
      502 Bad Gateway — 호스트 자체가 죽어있는 것으로 보임(도메인 추정이
      틀렸을 가능성)
    → 대신 openapi.danawa.com이 살아있음 확인(TLS 연결 성공, 302로
      auth.danawa.com/login?url=...openapi.danawa.com 리다이렉트) — 로그인/
      가입이 필요한 개발자 포털로 추정. "가격정보" API가 실제 공개
      범위에 포함되는지, 개인 무료 발급이 가능한지는 로그인 이후만
      확인 가능해서 여전히 사람이 직접 브라우저로 들어가서 가입/확인
      필요 — 죽어있다는 결론은 아니고 "다음 단계(가입)"까지 좁혀진 상태
    → 살아있고 가격 데이터 포함+무료 발급 가능이면 danawa-py 스크래핑보다
      우선 채택 검토 (여전히 유효한 원칙)
    → (같은 날 이어서) openapi.danawa.com/robots.txt 확인 → `Disallow: /`로
      서브도메인 전체가 크롤링 차단 — 로그인 이후 페이지(약관/가이드/발급
      조건)를 스크래핑으로 더 파고드는 건 매너 크롤링 원칙 위반이라 여기서
      멈춤. **가입해서 가격정보 API 공개 여부/무료 발급 가능 여부/요청
      한도 확인하는 건 최종적으로 사람이 브라우저로 직접 로그인해서
      봐야 하는 것으로 확정** — 더 이상 자동화로 좁힐 수 있는 부분 없음

[x] danawa.com robots.txt 상세 확인 → 2026-08-07, 접근 열린 틈에 확인
    → www.danawa.com/robots.txt: 대부분 페이지 허용, /user_report/ /my/
      /member/ /error/ 등 계정·에러 페이지만 제외 — 상품/검색 페이지는
      막혀있지 않음
    → search.danawa.com/robots.txt(우리가 실제로 스크래핑하는 도메인):
      /api_ui/ /classes/ /genfile/ /globalData/ /snippets/ /tpl/
      /mobile/tpl/ 만 Disallow — 우리가 쓰는 /dsearch.php는 안 걸림.
      **`Crawl-delay: 10` 명시** — CLAUDE.md의 "요청 간격 최소 5~10초"
      원칙이 다나와 자체 robots.txt 권고와 정확히 일치하는 것으로 확인
      (지금까지는 추정치였는데 이제 공식 근거가 생김 — 10초 이상으로
      맞추는 게 안전)
```

---

## 결정 완료 (2026-08-03, 같은 세션 내 논의)
```
[x] API 계약(/product/{code}) 응답에 variants 필드 포함 여부 → 포함 확정
    → /product/{code} 응답에 variants(정품/벌크/해외구매 등 유형별 최저가
      +몰수+pcode) 필드로 노출. 단 참고 정보 한정
    → /estimate, /compare(실측 합계·판정 게이지 등 핵심 계산 로직)에는
      variants 절대 미반영 — 해외구매는 통관/배송 지연(3~4주)/AS 불가 등
      국내 구매와 리스크가 근본적으로 달라 판정 로직 오염 우려
    → 프론트(빌드 상세 화면): breakdown 리스트에 "해외구매 참고가 ~원"
      부가 라인으로만 표시, 실측 합계/판정 게이지 계산에서는 제외
    → REFERENCE.md #엔드포인트-설계에 반영 완료 (API 계약 변경, 버전
      두 번째 자리 영향 — 실 구현 시 v0.2 표기)

[x] danawa_patched.py의 prices 리스트 마지막 항목 신뢰도 처리 → 원칙 확정
    → 실측 합계/판정 계산은 항상 lowest_price 필드(다나와 공식 최저가,
      hidden input 기반) 단일 값만 사용
    → prices 리스트(판매처별 가격)는 화면 표시(판매처 리스트 UI) 전용,
      이 리스트에서 직접 min() 계산해서 최저가로 쓰는 코드는 작성 금지
    → 근거: (1) 리스트 끝에 다나와가 최저가 산정에서 제외한 판매처가
      섞여 신뢰도 낮음 (2) 우리가 스크래핑하는 리스트 자체가 상위 일부
      (약 10건)만 잘려 나와 있어 전체 최저가와 다를 수 있음(불완전)
    → REFERENCE.md #엔드포인트-설계 설계 원칙에 반영 완료

[x] 저장소 구조 → backend/ + frontend/ 2개로 확정 (API는 별도 폴더 아님)
    → API는 backend(FastAPI) 안에 포함된 개념, 별도로 안 쪼갬
    → Flutter 이식 시점(Phase 5)에 flutter/ 추가되어 3개 체제로 전환 예정,
      지금은 2개만 만들면 됨
    → REFERENCE.md #저장소-구조에 반영 완료

[x] DB 스키마 → products / builds / build_items 3개 테이블로 확정,
    price_history는 만들지 않기로 결정 (오버엔지니어링 판단)
    → 개별 부품 가격 추이·빌드 총액 히스토리 둘 다 danawa의
      get_price_variance()를 그때그때 호출해서 계산 가능, 별도 축적
      테이블/스케줄러 불필요하다고 판단
    → APScheduler 우선순위는 그대로 낮음 유지(지정가 알림 등 실제 주기
      감시가 필요해질 때 재검토)
    → REFERENCE.md #DB-스키마에 반영 완료

[x] 배포 플랫폼 후보 갱신 → 결정 자체는 여전히 미룸(원격 접근 필요해질
    때 결정), 단 후보군에 GCP(Compute Engine VM / Cloud Run) 추가 검토
    → VM: 지금 스택(FastAPI+SQLite 파일) 무수정 배포 가능, 확장은 수동
    → Cloud Run: SQLite를 GCS FUSE로 마운트해야 해서 단일 인스턴스 고정
      필요 — 결국 SQLite 쓰는 한 서버리스 자동확장 이점을 못 씀. 진짜
      확장 필요해지면 Postgres 이전 + 멀티 인스턴스가 정공법
    → Cloudflare(Workers)는 후보에서 제외 확정 — Python이 Pyodide 경유라
      FastAPI가 그대로 안 올라가고 SQLite도 D1/Durable Objects로 갈아타야
      해서 스택 재설계 수준의 변경이 필요함
    → REFERENCE.md #기술-스택 배포 항목에 반영 완료
```

---

## 결정 완료 (2026-08-04, verdict 이동평균 도입 논의 — 설계만 확정, 구현 전)
```
배경: verdict가 조회 시점의 스팟 스크래핑 값(순간 lowest_price) 하나로만
  판정돼서, 부품이 일시 특가/재고 급변으로 순간적으로 폭락·폭등하면 같은
  빌드인데도 조회 타이밍에 따라 고가↔저가↔적정가로 판정이 오락가락하는
  문제 발견 (예: 평소 10만원 부품이 물량 이슈로 하루 5만원 됐다가 복귀하는 케이스)

[x] estimate_total(총 견적가, /estimate 응답의 total_price)은 즉시가(현재
    최저가) 그대로 유지 — "지금 당장 사면 얼마"라는 원래 의미를 보존하기
    위해 이동평균 도입 대상에서 제외

[x] verdict 판정 "기준가"만 부품별 이동평균으로 계산하도록 분리
    → 기존 get_price_variance()를 재사용(별도 신규 스크래핑 함수 불필요),
      1/3/6/12개월 daily 시계열에서 최근 N일만 슬라이싱해서 평균
    → N(이동평균 기간)은 7일/14일/30일 중 사용자가 선택, 기본값 14일
    → 선택값은 설정 탭에 드롭다운으로 노출, localStorage에 저장(용량/영속성
      문제 없음 확인 — origin당 수 MB 할당되는데 문자열 하나만 저장), DB
      테이블 신설 안 함(과설계 지양 원칙 유지)
    → compare류 엔드포인트는 ma_window 쿼리 파라미터로 받음 (기본 14)

[x] 이동평균 유효성 기준: "엄격" 채택
    → 선택한 기간(N일)만큼 daily 데이터가 정확히 다 있어야 그 부품의
      이동평균이 유효. 예: 30일선인데 신상품이라 20일치 데이터뿐이면 그
      부품은 이동평균 무효 처리 (완화된 "절반 이상이면 계산" 안은 기각)

[x] 부품별 이동평균 무효 시 fallback 규칙 확정 (중요 — 최초안이었던
    "부품 하나라도 무효면 빌드 전체 판정 null" 원칙을 뒤집음)
    → 이동평균 무효인 부품은 그 부품만 즉시가(현재 lowest_price)로 대체해서
      합산 계속 진행. 빌드 전체를 null로 날리지 않음
    → 대신 응답에 신뢰도를 투명하게 노출: verdict_confidence("high"/"low",
      fallback 섞이면 low) + 부품별 어떤 값을 썼는지 breakdown(source:
      "ma" | "current_fallback")
    → 이 방식이 "전체 null" 방식보다 오히려 구현이 단순함(조기 종료 분기
      불필요, 부품마다 이동평균 있으면 그거 없으면 즉시가 골라 합산하면 끝)
      + API 호출도 추가 안 됨(fallback 판단에 필요한 값들을 이미 부품별로
      조회하는 중이라) + "판정 불가" 빈 상태 UI를 거의 안 만들어도 됨
      (전 부품 스크래핑 실패 같은 진짜 예외 상황에서만 null 발생)

[x] verdict 임계값(VERDICT_THRESHOLD_PERCENT, ±5%)은 그대로 유지
    → 고가/저가 비대칭 임계값(예: 고가 +5%, 저가 -10~15%)도 검토했으나
      이번엔 채택 안 하고 대칭 ±5% 유지로 결론

[x] GET /builds(목록) 성능/일관성 대응 — (build_id, ma_window) 키로 TTL
    5분 메모리 캐시 적용 확정 (DB 저장 아님, 프로세스 인메모리 캐시)
    → 이동평균 계산 도입으로 부품당 스크래핑 호출이 2배(get_product +
      get_price_variance)가 되는데, 목록 화면이 저장된 빌드 전체를 매번
      순회 재계산하는 구조(REFERENCE.md 기존 설계)라 체감 지연 우려 발견
    → 캐시로 "목록→상세→목록" 같은 흔한 네비게이션에서 재스크래핑 방지 +
      목록/상세 판정 불일치(캐시 미스 타이밍 차이로 다른 값 뜨는 문제) 방지
    → 캐시 대상은 이동평균 파트(verdict_basis_price 등)만, 즉시가
      total_price는 캐시 안 하고 매번 새로 조회(기존 "가격 자체 축적 금지"
      원칙과 최대한 안 부딪히게)
    → POST /build/compare(저장 전 미리보기, build_id 없음), GET
      /product/{code}/compare(단일 상품, 반복 조회 문제 없음)는 캐시
      대상에서 제외 확정

[x] REFERENCE.md #엔드포인트-설계 스키마 갱신 완료 (verdict_basis_price,
    verdict_basis_price_formatted, verdict_confidence, verdict_basis_breakdown,
    ma_window 필드 반영, "verdict 판정 기준가(basis_price) — 이동평균 도입"
    섹션 신설)

[x] 백엔드 코드 구현 완료 (2026-08-04)
    → services/verdict.py: compute_ma_price() 신규(엄격 원칙 + full_date
      명시 정렬), calc_verdict()는 파라미터명만 basis_price로 정리(로직 동일)
    → main.py: _fetch_ma_price(), _compute_verdict_basis()(fallback+신뢰도),
      _get_cached_verdict_basis()(TTL 5분 캐시) 신규, /product/{code}/compare
      · /build/compare · GET /builds · GET /builds/{id} 4개 엔드포인트에
      ma_window 파라미터 + verdict_basis_* 필드 반영
    → schemas/compare.py: VerdictBasisItem 신규, schemas/estimate.py·build.py에
      필드 추가
    → 개발 중 발견: ma_window를 Literal[7,14,30] 타입으로 쿼리파라미터에
      바로 쓰면 fastapi 0.141/pydantic 2.13 조합에서 쿼리스트링("14")을
      int로 캐스팅 안 하고 422 내버리는 문제 실측 확인 → int + 수동 검증
      (/history의 months 검증과 동일 패턴)으로 변경해서 해결
    → mock으로 이동평균 정상/fallback/캐시 동작 전부 로컬 검증 완료
      (FastAPI TestClient + danawa 함수 patch)
    → [x] 라이브 다나와 검증 완료 (2026-08-04, 사용자 로컬 환경) — 실측
      결과 full_date 포맷은 "YY-MM-DD"(예: "26-05-12")로 정렬 로직은
      문제없었으나, **다나와가 daily가 아니라 주 단위로 데이터를 줌**(1개월치
      조회해도 포인트 4개 안팎)이 확인돼 "정확히 N개 항목 있어야 유효"였던
      원래 엄격 원칙이 항상 실패하는 치명적 문제 발견 → compute_ma_price()를
      "날짜 기준 최근 window일 이내 데이터 + 히스토리가 window일 전체를
      커버해야 유효"로 재설계, _fetch_ma_price도 get_price_variance(code, 1)
      대신 (code, 3)으로 변경(1개월치만으론 30일 커버리지 체크가 부족해서).
      재검증 완료 (실가_HISTORY.md 2026-08-04 v5 참조)
    → [x] 프론트 연동 완료 (같은 날, PR #2 머지 후 이어서) — 설정 탭
      드롭다운 + localStorage(useMaWindow 훅), BuildListPage/
      BuildDetailPage가 이 값을 읽어 GET /builds, GET /builds/{id}에
      ma_window로 실어 보냄. BuildCreatePage는 POST /builds가 애초
      ma_window를 받지 않아(생성 직후엔 verdict가 항상 null) API 호출
      변경은 없고 안내 문구만 추가. SearchPage는 비교(compare) 기능
      자체가 없어 변경 없음 (실가_HISTORY.md 2026-08-04 v6 참조)
    → [x] danawa 연결 장애 처리 결함 수정 완료 (같은 날, 이어서) — 아래
      "수정 예정 사항" 참조
```

---

## 수정 예정 사항

### 🟡 우선순위 높음 — 전부 완료 (2026-08-03 Phase 3~4 세션)
```
[x] 저장소 구조 실제 생성 (backend/ + frontend/) → 완료, git 리포도 형성됨
    (커밋 4개: v0.2 백엔드 스켈레톤 / v0.3 빌드CRUD / v0.4 프론트 / docs)

[x] danawa_patched.py를 backend/app/services/danawa.py로 편입 + 원본
    danawa.py의 get_price_variance 함수와 합쳐서 단일 모듈로 정리 → 완료
    (GitHub MineEric64/danawa-py 원본에서 get_price_variance 이식, 나머지 두
    함수는 패치본 그대로)

[x] FastAPI 스켈레톤 + /search, /product/{code} 두 엔드포인트 구현 → 완료,
    실제 다나와 라이브 호출로 검증 완료

[x] DB 스키마 실제 구현 → 완료
    → products / builds / build_items 3개 테이블, SQLAlchemy 모델 작성 완료
    → SQLite(WAL) 확정. 단, SQLAlchemy의 DateTime(timezone=True)가 SQLite
      다이얼렉트에서 오프셋을 저장/복원하지 못하는 버그 발견 — "타임존 미표기
      금지" 체크리스트 원칙과 충돌해서 KSTDateTime 커스텀 TypeDecorator로
      해결함(timezone_utils.py). 앞으로 datetime 컬럼은 반드시 이걸 사용할 것

[x] /product/{code}/history, /estimate, /compare 엔드포인트 구현 → 완료
    → /compare는 문서 계약(단일상품 기준)과 silga-mockup.html 예시(빌드전체
      기준)가 스펙 불일치였음 → 확인 후 단일상품용(원 계약 유지) + 빌드전체용
      신규(POST /build/compare) 둘 다 구현. #엔드포인트-설계에 반영 완료
    → 빌드 저장/조회 CRUD(POST/GET /builds, GET /builds/{id})도 신규 추가
      — DB 스키마는 있었는데 채워넣는 엔드포인트가 누락돼 있었음
    → /product/{code} 응답에 in_stock 필드 추가(원 계약 외) — "일시 품절"과
      "스크래핑 실패"를 구분 못 하던 문제 발견해서 해결 (실측: PALIT
      RTX5070Ti 76465883이 일시 품절 상태였음)
    → verdict 판정 임계값은 REFERENCE.md에 수치가 없어 ±5%로 가정하고 구현
      (services/verdict.py 상수 분리, 실사용해보고 조정 필요 — 결정 필요 항목 참조)

[x] 검색 탭 프론트 실데이터 연동 → 완료 (Vite+React+TS+TanStack Query 스캐폴딩)

[x] 빌드 탭 "생성" 화면의 부품 검색 입력에 실제 자동완성/드롭다운 연결 → 완료
    → 디바운스 500ms 적용 (매너 크롤링 원칙 고려)
    → 검색 결과 매칭 느슨한 문제(위에 적힌 현상)는 그대로 남아있음 — 카테고리
      필터링은 아직 안 걸었음, 자동완성에서 관련없는 상품이 섞일 수 있음
    → Playwright E2E 스모크 테스트로 검색→생성→상세→목록 전체 흐름 실브라우저
      검증 완료 (scripts/e2e_smoke_test.py)
    → 버그 수정: 자동완성 로딩 placeholder("검색 중...")가 클릭을 가로채는
      레이스 컨디션 발견 → pointer-events: none으로 해결
```

### 🟡 우선순위 높음 (신규 — Phase 3~4 완료 후 생긴 항목)
```
[x] verdict 판정 임계값(±5% 가정값) 확정 논의 → ±5% 그대로 유지하기로 확정
    (2026-08-04). 대신 판정 "기준가" 계산 방식 자체를 이동평균 도입으로
    개선하기로 함 — 설계 확정, 아래 신규 항목 참조

[x] verdict 이동평균 기준가 도입 — 백엔드 구현 완료 (2026-08-04)
    → estimate_total(즉시가)과 verdict 기준가(이동평균) 분리, ma_window
      쿼리파라미터(7/14/30, 기본 14), 부품별 이동평균 무효 시 즉시가
      fallback + verdict_confidence 플래그, GET /builds 목록용 TTL 5분
      메모리 캐시 — 전부 구현+로컬 mock 테스트 완료, 위 "결정 완료" 섹션 참조
    → [x] 라이브 다나와 검증 완료 (사용자 로컬 환경, 2026-08-04) — 다나와가
      주 단위로 데이터를 주는 걸 발견해서 compute_ma_price() 재설계함
      (위 "결정 완료"/실가_HISTORY.md v5 참조)
    → [x] 프론트 연동 완료 (2026-08-04, 같은 날 이어서) — 설정 탭
      ma_window 드롭다운 + localStorage, BuildList/BuildDetail 연동
      (실가_HISTORY.md v6 참조)

[x] _fetch_lowest_price() danawa 연결 장애 처리 누락 → 수정 완료 (2026-08-04,
    프론트 연동 세션 이어서)
    → 원래 결함: main.py의 _fetch_lowest_price()가 danawa.get_product()
      호출을 try/except로 안 감싸서, requests.RequestException(네트워크
      장애)이 나면 CLAUDE.md 원칙(스크래퍼 장애→503, 정상 품절/파싱실패→404)을
      못 지키고 500 unhandled exception으로 죽었음. 이 환경 프록시가
      danawa.com을 막아서 우연히 재현됨(ProxyError) — 실제 운영에서도
      타임아웃/DNS 실패 등 같은 예외 클래스로 재현 가능한 진짜 결함으로 판단
    → 결정: "엔드포인트별 기존 선례 따르기" 채택 — 새 정책을 만들지 않고
      저장소에 이미 있던 두 선례를 그대로 적용
        (a) 단일 상품 조회(GET /product/{code}/compare)는 GET
            /product/{code}(get_product_detail)와 동일하게 즉시 503
        (b) 다중 부품 합산(_compute_estimate 공유 — POST /estimate, POST
            /build/compare, GET /builds, GET /builds/{id})은 POST
            /builds(create_build)·_fetch_ma_price의 기존 부품별 fallback
            관례를 따라 항목별로 삼켜서 그 부품만 title/price=None 처리,
            나머지 부품은 계속 진행
    → 구현: compare_single_product에 try/except RequestException → 503
      추가, _compute_estimate 루프 내부에 try/except RequestException →
      (None, None) 추가 (main.py)
    → 검증: 이 환경(danawa 접근 차단)에서 실측 — GET
      /product/{code}/compare는 503 확인, GET /builds·GET /builds/{id}·
      POST /estimate는 200으로 정상 응답(가격 정보 없는 부품만
      title/price null)하고 500 크래시 없음 확인. POST /build/compare는
      부품 전부 가격 조회 실패 시 기존 로직대로 404("부품 가격을 하나도
      찾지 못해 판정 불가") — 이건 원래 있던 정상 동작이라 회귀 아님

[x] category, cash_price 필드 스크래퍼 구현 → 완료 (2026-08-04)
    → 이 세션 환경은 danawa.com 접근이 프록시 정책으로 막혀 있어 사용자가
      로컬 브라우저에서 실제 상품 페이지 HTML(GPU: PALIT RTX5070Ti
      76465883, CPU: AMD 9800X3D 70531547)을 직접 떠서 붙여넣어줌 — 그
      실측 HTML 기준으로 구현
    → category: div.location_wrap의 중첩 breadcrumb DOM을 직접 파싱하는
      대신, 페이지 하단 인라인 <script>의 oGlobalSetting.sUICategoryName
      값 사용 ("컴퓨터/노트북/조립PC &gt; 주요부품 &gt; 그래픽카드(GPU)"
      형태로 이미 정리된 단일 문자열, HTML 엔티티만 unescape). GPU/CPU
      두 카테고리 페이지 모두에서 동일 패턴 확인
    → cash_price: 애초 가정했던 "카드가/현금가 비교"(노트북 전용 기능)가
      아니라, 쇼핑몰별 최저가 목록 중 현금결제 전용(badge__cash) 판매처의
      최저가였음 — GPU 페이지엔 없었고(해당 상품이 일시 품절), CPU
      페이지엔 있었음(현금최저가 630,000원, 최저가 681,360원과 다른 값).
      og:description 메타태그에 다나와가 이미 계산해 넣어주는
      "현금최저가: N원" 값을 파싱 — 우리가 스크래핑하는 쇼핑몰별 가격
      목록 자체가 상위 약 10건만 잘려 있어 직접 min() 계산하면 절단
      구간 밖의 진짜 최저가를 놓칠 수 있어서, 다나와 계산값을 그대로 신뢰
    → backend/app/services/danawa.py::get_product()에 두 정규식 추가,
      main.py::get_product_detail()/schemas/product.py TODO 주석 제거하고
      실제 값 연결. 값이 없는 상품(현금결제 전용 판매처가 아예 없는 등)은
      필드가 None인 게 정상 — 파싱 실패 아님
    → 정규식 자체는 사용자가 준 실제 HTML 조각으로 직접 검증 완료.
      전체 페이지 단위 재현(mock 서버로 왕복)까지는 안 함 — 조각 검증으로
      충분하다고 판단(패턴이 각 페이지에 정확히 1회씩만 등장하는 것도
      육안 확인함)

[x] 즐겨찾기 탭 — 관심 상품 북마크 구현 완료, 사이드바 7탭 전부 실데이터
    연동 완료 (2026-08-04, 아래 "우선순위 낮음" 섹션 참조)

[x] frontend .env 기반 API_BASE 설정 문서화 → 완료 (2026-08-04)
    → .gitignore에 .env/.env.local 추가, frontend/.env.example 신규,
      README.md .env 섹션 보강 + "현재 구현 상태" 표 최신화(2026-08-03
      v0.4 시점 그대로 방치돼 있던 걸 발견 — 7탭 전부 연동 완료로 정정)

[x] 배포 시 CORS allow_origins=["*"] → 2026-08-07, GCP 배포 구성 확정하며
    사실상 해소됨 — nginx가 프론트 정적파일+`/api/` 리버스 프록시를 같은
    오리진으로 묶어서 서빙하는 구성이라 프로덕션 트래픽은 애초에 크로스
    오리진이 아님(브라우저가 preflight/CORS 검사 자체를 안 함). 로컬
    dev(Vite :5173 → 백엔드 :8000)는 여전히 크로스 오리진이라 `allow_origins=["*"]`
    유지 필요 — 그래서 코드는 안 건드림, "배포 도메인으로 좁히기"라는
    원래 전제 자체가 이 배포 구성에서는 성립 안 해서 항목 종결
    (`실가_REFERENCE.md` #기술-스택 배포 항목, `deploy/README.md` 참조)
```

### 🔵 우선순위 낮음
```
[x] 홈 탭 대시보드 내용 설계+구현 완료 (2026-08-04)
    → 사용자와 논의해서 범위 확정: "최근 빌드 요약 + 빠른 액션"만 채택,
      "관심부품 변동" 후보는 즐겨찾기 기능 자체가 아직 없어서 보여줄
      데이터가 없다는 이유로 이번엔 제외 — 새 백엔드/DB 작업 없이 기존
      GET /builds만으로 끝낼 수 있는 범위로 좁힘
    → frontend/src/pages/HomePage.tsx 신규. 상단에 "새 빌드 만들기"/
      "부품 검색" 바로가기 버튼 2개, 아래에 최근 빌드 4개(created_at
      내림차순 정렬, 클라이언트에서 정렬 — 백엔드가 정렬 순서를 보장 안
      해서) 카드로 표시, 5개 이상이면 "전체보기"로 /build 이동. 빌드가
      하나도 없으면 "새 빌드를 만들어서 가격을 추적해보세요" 빈 상태
    → BuildListPage와 같은 build-card/bc-tag 스타일 재사용(새 CSS는
      버튼 줄(.home-actions) 하나만 추가)
    → Playwright로 빈 상태(빌드 0개)/채워진 상태(5개 만들어서 최근 4개만
      나오는지) 둘 다 실브라우저 검증, 카드 클릭→상세 이동/전체보기→
      목록 이동 둘 다 확인

[x] 통계 탭 — silga-mockup.html #history 섹션의 오실로스코프풍 라인차트를
    앱 셸로 이식 완료 (2026-08-04)
    → frontend/src/pages/StatsPage.tsx 신규. PartRow(빌드 생성 화면과 동일
      컴포넌트) 재사용해서 부품 검색 → 선택하면 GET
      /product/{code}/history?months={1|3|6|12} 호출, 1/3/6/12개월 탭
      전환 가능
    → 기존 목업 SVG(그래디언트 fill + glow filter + 그리드 라인)를
      그대로 이식하되, prices 배열에서 매번 polyline 좌표 계산하도록
      동적화. 최저/최고는 응답의 min/max 필드 그대로 사용(prices에서
      직접 min() 계산 금지 원칙과는 별개 필드라 무관), "현재"는
      검색 시점의 실측 lowest_price(PartRow가 이미 들고 있는 값) 사용 —
      history의 마지막 포인트는 주 단위라 최대 며칠 전 값일 수 있어
      "현재"로 쓰지 않음
    → 차트 범례의 날짜 범위 표시는 prices[].date(포맷 미확정) 대신
      full_date("YY-MM-DD", 실측 확인된 포맷)를 우선 사용해서
      "20YY.MM.DD" 형태로 표시 — date만 쓰면 1년 이상 차이 나는 두
      시점이 같은 월.일로 보여 혼동될 수 있어서(12개월 조회 시 실제
      겪은 문제, 개발 중 mock 데이터로 발견)
    → 이 세션 환경은 danawa.com 접근이 막혀 있어 danawa.get_product_codes/
      get_product/get_price_variance를 mock으로 교체해 검색→선택→차트
      렌더링 전체 흐름을 Playwright로 실브라우저 검증 (1개월/12개월 등
      데이터 포인트 개수 다른 케이스 포함)
    → 버그 하나 발견+수정: 부품 선택창(.stats-picker)에 overflow:hidden을
      넣었더니 자동완성 드롭다운이 컨테이너 아래로 잘려서 안 보이는 문제
      확인 → overflow:hidden 제거로 해결 (빌드 생성 화면의 .part-rows는
      여러 행이 이어져 있어 드롭다운이 컨테이너 안쪽에 들어가는 우연 덕에
      같은 문제가 안 드러났던 것으로 추정 — 그쪽은 아직 안 건드림)

[x] 최근기록 탭 — 최근 조회한 부품 구현 완료 (2026-08-04)
    → 사용자와 논의해서 범위 확정: "최근 조회한 부품(상품)" 채택
      (danawa 자체 헤더의 "최근 본 상품" 기능과 같은 맥락). "최근 검색어"
      /"최근 조회한 빌드"(홈 탭 최근빌드와 거의 같은 내용이 될 것) 후보는
      기각
    → 저장 위치: ma_window 설정과 동일하게 localStorage만 사용
      (silga:recent_products 키) — DB/백엔드 작업 전혀 없음. "기록"이라는
      단어 때문에 서버 저장을 먼저 떠올리기 쉬운데, 이 프로젝트는
      인증/로그인이 없어 "이 브라우저에서"라는 전제가 항상 깔려 있으므로
      서버에 남길 이유가 딱히 없다고 판단
    → 어디서 "조회"로 칠지가 관건이었음 — 이 앱엔 독립된 "상품 상세
      페이지"가 없어서(검색 결과 클릭해도 이동 안 함) 부품을 실제로 들여다
      보는 지점은 두 곳: (1) 통계 탭에서 부품 검색해 가격 히스토리 볼 때,
      (2) 빌드 생성 화면에서 부품을 고를 때. 둘 다 "조회"로 간주해서 두
      지점 모두에서 기록하기로 함(danawa 자체도 의도와 무관하게 방문한
      모든 상품 페이지를 "최근 본 상품"에 넣는 것과 같은 방식)
    → frontend/src/lib/recentProducts.ts 신규 — getRecentProducts/
      addRecentProduct(중복 시 맨 앞으로 이동)/removeRecentProduct/
      clearRecentProducts, 최대 20개 유지
    → StatsPage.tsx·BuildCreatePage.tsx의 PartRow onSelect 콜백에
      addRecentProduct(part) 한 줄씩 추가해서 계측
    → frontend/src/pages/RecentHistoryPage.tsx 신규 — 목록(시간 내림차순),
      개별 삭제(×), 전체 지우기, 빈 상태. 각 행 클릭 시 /stats로
      이동하면서 react-router의 location.state로 해당 부품을 넘겨
      StatsPage가 재검색 없이 바로 그 부품의 차트를 띄우도록 연결
      (StatsPage가 useLocation().state를 초기 선택값으로 사용하도록 확장)
    → 이 세션 환경은 danawa.com이 막혀 있어 mock 백엔드로 서로 다른 부품
      2개를 통계 탭에서 순서대로 선택 → 최근기록에 최신순으로 뜨는지,
      항목 클릭 시 통계 탭에 해당 부품이 바로 로드되는지, 개별 삭제/전체
      삭제 둘 다 Playwright로 실브라우저 검증 완료

[x] 즐겨찾기 탭 — "관심 상품 북마크" 구현 완료 (2026-08-04)
    → favorites DB 테이블 신규(id PK, product_code FK, created_at,
      product_code 유니크 제약) — 가격은 저장 안 함, 다른 테이블과 동일
      원칙(REFERENCE.md #DB-스키마 갱신 완료)
    → 백엔드: POST /favorites(idempotent — 이미 있으면 기존 항목 그대로
      반환, danawa.get_product() 한 번만 호출해서 존재확인+products
      캐시 upsert+응답용 즉시가까지 처리해 중복 스크래핑 방지), GET
      /favorites(목록, 항목별 실시간 최저가, 연결 장애 시 부품별
      fallback), DELETE /favorites/{code}(204, 없으면 404). 단일상품
      조회(POST)는 즉시 503, 다중부품 목록(GET)은 항목별 fallback —
      기존에 확립한 두 관례 그대로 적용
    → 겸사겸사 발견: _cache_product()가 category 파라미터를 아예 안 받아서
      products 테이블의 category 컬럼이 스크래퍼 구현(오늘 앞서 완료) 이후에도
      계속 비어있던 걸 발견 → _cache_product 시그니처에 category 추가,
      create_build/add_favorite 두 호출부 모두 수정. Product 모델의 스테일한
      TODO 주석("스크래퍼 아직 미제공")도 같이 정리
    → 프론트: frontend/src/pages/FavoritesPage.tsx 신규 — 상단에 PartRow로
      검색→선택 즉시 POST /favorites(추가), 아래 목록(recent-list 스타일
      재사용)에서 클릭 시 최근기록과 동일하게 /stats로 이동+즉시 로드,
      개별 제거(DELETE), 빈 상태
    → 버그 발견+수정: PartRow를 selected=null 고정으로 반복 재사용하는
      패턴(즐겨찾기 화면 특유 — "선택 즉시 추가하고 계속 검색 상태 유지")
      에서, PartRow.handlePick()이 선택 시 setFocused(false)를 호출하는데
      즐겨찾기 화면은 selected가 항상 null이라 .part-selected 클릭으로
      focused를 되돌리는 경로 자체가 없어서, 한 번 선택한 뒤 같은 input에
      바로 타이핑해도 자동완성이 다시 안 뜨는 문제 발견(DOM은 계속
      focus돼 있어서 안 보이는 버그였음 — Playwright로 활성 엘리먼트까지
      직접 확인해서 원인 특정). PartRow 공용 컴포넌트 자체는 안 건드리고,
      FavoritesPage에서 선택 성공마다 key를 바꿔 PartRow를 강제
      remount하는 방식으로 우회 해결
    → 검증: mock 백엔드로 POST(idempotent 포함)/GET/DELETE 전부 curl로,
      프론트는 검색→추가(2개, 중복추가 무시 확인)→클릭시 통계 탭 이동+
      로드→제거까지 Playwright로 실브라우저 검증

[x] scripts/e2e_smoke_test.py — 홈/통계/최근기록/즐겨찾기 4개 화면
    스텝 추가 완료 (2026-08-04)
    → 기존 검색→빌드생성→상세→목록(4스텝)에 홈 카드 클릭, 통계 탭
      검색+차트+월 탭 전환, 최근기록 계측 확인+클릭 이동, 즐겨찾기
      추가/제거 8스텝 추가(총 12스텝)
    → 이 환경은 danawa.com 접근이 막혀 있어 스크립트를 라이브로 못
      돌림 — danawa 3개 함수를 mock으로 교체한 로컬 백엔드를 세워
      확장된 스크립트 전체를 실제 Playwright(사전 설치된 Chromium)로
      끝까지 실행해 12스텝 전부 통과 확인. 셀렉터/플로우 순서 결함은
      이 과정에서 걸러졌고, 다나와 실제 HTML 파싱 자체는 기존 검증된
      danawa.py 로직을 그대로 신뢰(이번 검증 범위 아님)
    → 상세는 실가_HISTORY.md 2026-08-04 v13 참조

[ ] 알림(지정가 알림) 기능 — Flutter 이식 이후 FCM으로 적용 예정,
    웹 단계에서는 보류 확정

[ ] APScheduler 크롤링 배치 설계 (몇 시간 주기로 갱신할지 미정)

[x] 프로젝트 정식 이름 확정 → 실가로 확정, PPE 코드네임 일괄 치환 완료 (2026-08-03)
```

---

## 알려진 미결사항 / 동작 특성
```
[미결] 목업 파일 2개 중 실가(silga-mockup.html)는 랜딩형 마케팅 페이지로,
  현재 채택된 앱 셸(app-shell-mockup.html)과 레이아웃 사상이 다름
  → 판정 게이지/breakdown 컴포넌트, 가격 히스토리 오실로스코프 차트는
    앱 셸 쪽(통계 탭, 2026-08-04)으로 이식 완료. 나머지(히어로, API
    터미널 섹션)는 폐기 또는 별도 마케팅 페이지 용도로 보류 (미정)

[동작특성] app-shell-mockup.html은 순수 정적 목업
  → 사이드바 토글/탭 전환/빌드 3단계 흐름 전환만 JS로 동작, 실제 데이터
    fetch 없음. 모든 부품명/가격은 하드코딩된 예시값 (2026-08-01 대화
    시점의 실제 다나와 조사값 재사용)

[동작특성] GTHV 문서 체계와의 차이
  → GTHV는 UTC 통일, 실가는 KST 통일 (다나와 자체가 KST 서비스라 변환
    비용 없애는 게 실익 큼) — 문서 작성 시 혼동 주의
  → GTHV의 "실행 중인 프로세스"/"장애 대응"/"주기적 점검" 섹션은 실가
    REFERENCE.md에 아직 없음 — 배포 이후(Phase 3 이후)에 채울 것,
    현재는 로컬 개발 단계라 해당 없음

[동작특성] danawa-py는 다나와 DOM 구조 변경에 매우 취약함이 실증됨
  → 2026-08-03 검증 시점 기준으로 이미 원본 코드의 셀렉터 2곳이 깨져
    있었음(제작 시점 대비 다나와 개편). 커밋 9개뿐이라는 우려가 실제로
    맞아떨어진 사례 — 향후 다나와가 또 개편하면 재점검 필요. 크롤링 실패
    시 명확한 에러를 내는 방어 로직은 FastAPI 단에 구현 완료
    (requests.RequestException → 503 "데이터 소스 연결 실패",
    상품 없음/파싱 실패 → 404 "상품을 찾을 수 없음", 라이브 검증 완료)
  → 추가로 "일시 품절"(가격 정보 자체가 없는 정상 상태)과 "스크래핑
    실패"를 구분 못 하던 문제도 발견해서 in_stock 필드로 해결함
    (2026-08-03, PALIT RTX5070Ti 76465883 실측 사례 — REFERENCE.md 참조)
```

---

## Phase 로드맵
```
Phase 1: 설계/목업 (완료)
  2026-08-03  기획: 조립PC 적정가 판정 아이디어 도출 (참쉬운PC 사례 분석에서 출발)
  2026-08-03  design: 랜딩형 목업(실가) 제작
  2026-08-03  design: 앱 셸 목업(사이드바 7탭 + 빌드 3단계 흐름) 제작, 최종 채택
  2026-08-03  research: 데이터소스 후보 조사 — danawa-py(비공식 API), 
              sammy310/Danawa-Crawler(대체 크롤러), 다나와 공식 API(미검증)
  2026-08-03  docs: 인수인계 3파일 체계 도입 (GTHV 문서 틀 이식)

Phase 2: 데이터소스 검증 (완료)
  2026-08-03  test: danawa-py 3개 함수 실동작 테스트 완료
  2026-08-03  fix: get_product_codes 패치 (URL 인코딩 + 클래스 매칭 완화)
  2026-08-03  fix: get_product 패치 (price-summary 신 구조 대응, 쇼핑몰명/
              variants 필드 확장) → danawa_patched.py
  2026-08-03  chore: danawa_patched.py를 backend/app/services/danawa.py로
              정식 편입, get_price_variance 원본과 통합

Phase 3: 백엔드 API 구현 (완료)
  2026-08-03  feat: FastAPI 스켈레톤 + /search, /product/{code} 구현+검증
  2026-08-03  feat: SQLite(WAL) DB 스키마 구현 (products/builds/build_items),
              KSTDateTime 커스텀 타입으로 타임존 소실 버그 해결
  2026-08-03  feat: /product/{code}/history, /estimate,
              /product/{code}/compare 구현+검증
  2026-08-03  feat: /compare 스펙 불일치 발견 → POST /build/compare 신규 추가
  2026-08-03  feat: 빌드 저장/조회 CRUD 신규 추가 (POST/GET /builds,
              GET /builds/{id})
  2026-08-03  fix: "일시 품절"과 "스크래핑 실패" 구분 안 되는 문제 발견,
              in_stock 필드로 해결
  2026-08-03  chore: git 리포 형성 (커밋 4개, v0.2~v0.4 + docs)

Phase 4: 프론트 실데이터 연동 (핵심 완료, 마무리 항목 남음) ← 현재
  2026-08-03  feat: Vite+React+TS+React Router+TanStack Query 스캐폴딩
  2026-08-03  feat: 검색 탭, 빌드 목록/생성/상세 실데이터 연동 완료
  2026-08-03  test: Playwright E2E 스모크 테스트로 전체 흐름 실브라우저 검증
  2026-08-03  fix: 자동완성 로딩 placeholder 클릭 가로채기 레이스 컨디션 수정
  2026-08-04  feat: verdict 판정 기준가에 이동평균(7/14/30일 선택) 도입,
              부품별 fallback+신뢰도 플래그, GET /builds용 TTL 캐시 (백엔드만,
              라이브 다나와 미검증 — 아래 참조)
  2026-08-04  fix: 다나와 주 단위 데이터 실측 반영해 compute_ma_price
              재설계(v0.4.1), 라이브 검증 완료, PR #2 main 머지
  2026-08-04  feat: verdict 이동평균 프론트 연동(v0.2) — 설정 탭 드롭다운
              + localStorage, BuildList/BuildDetail이 ma_window 실어 보냄
  2026-08-04  fix: _fetch_lowest_price danawa 연결 장애 처리 누락 수정
              (v0.4.2) — 단일상품은 503, 다중부품 합산은 부품별 fallback
  2026-08-04  feat: category(카테고리 브레드크럼)·cash_price(현금최저가)
              필드 스크래퍼 구현(v0.4.3), 사용자가 준 실제 페이지 HTML로 검증
  2026-08-04  feat: 통계 탭 가격 히스토리 차트 구현(v0.3) — silga-mockup.html
              오실로스코프 차트 이식, PartRow 재사용해서 부품 검색 연동
  2026-08-04  feat: 홈 탭 최근빌드 대시보드 구현(v0.4) — GET /builds 재사용,
              최근 4개 빌드 카드 + 빠른 액션 버튼, 새 백엔드 작업 없음
  2026-08-04  feat: 최근기록 탭 구현(v0.5) — localStorage 기반 최근 조회
              부품 목록, 통계/빌드생성 화면 계측, 새 백엔드 작업 없음
  2026-08-04  feat: 즐겨찾기 탭 구현(backend v0.4.4, frontend v0.6) —
              favorites 테이블/엔드포인트 3개 신규, 사이드바 7탭 전부
              실데이터 연동 완료(PlaceholderPage 삭제)
  [x] Phase 4 목표(프론트 실데이터 연동) 달성 — 7탭 전부 완료
  2026-08-04  docs: frontend .env API_BASE 문서화 보강(.env.example 신규,
              README.md 최신화)
  2026-08-04  test: e2e_smoke_test.py에 홈/통계/최근기록/즐겨찾기
              4개 화면 스텝 추가(v13), mock 백엔드로 12스텝 전체 통과 검증
  2026-08-05  feat: /search 카테고리 필터 + 카테고리별 스펙 필터 7종
              (GPU 메모리/칩셋, CPU·메인보드 소켓, 메인보드·케이스 폼팩터,
              RAM 규격, 파워 출력, SSD 인터페이스) — PR #6, #7 머지
  2026-08-05  feat: 프론트 디자인 전면 교체(다크네온→종이/잉크 에디토리얼)
              + 빌드 상세를 견적서 스펙시트로 재구성 — PR #8, #9 머지
  2026-08-05  feat: 쿨러 스펙 필터(제품 종류 5종 + 지원 소켓 4종) 추가
              (backend v0.7 / frontend v0.9.1) — 8개 카테고리 전부
              스펙 필터 보유

Phase 5: Flutter 이식 (예정)
         웹에서 검증된 API 그대로 재사용, 화면 구조 기계적 이식

Phase 6: AI 라우터 연동 테스트 (예정)
         Claude 등 에이전트가 실제로 엔드포인트를 tool처럼 호출해보고
         응답 파싱 검증
```
