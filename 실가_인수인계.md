# 실가 인수인계 문서
업데이트: 2026-08-06 | 문서 체계: 3파일 분리 (2026-08-03 신설)
※ 2026-08-03 최초 작성 — 목업 2건(랜딩/앱셸) + 데이터소스 후보 조사까지 진행,
  같은 날 이어서 danawa-py 실동작 검증 + 패치까지 완료 (아래 참조)

---

## 오늘(2026-08-06) SSD 폼팩터 스펙 필터 추가 — 다음 세션 시작 지점
```
PR #11(검색 UX 개편)이 이미 main에 머지된 상태로 세션 시작(브랜치
claude/new-session-xw491j, 커밋/push 확인 완료 — 아래 직전 섹션 "다음에
이어갈 것"의 확인 항목 해소됨). 사용자가 "스펙 필터 확장 → SSD 폼팩터"를
선택해서 진행. backend v0.8 / frontend v0.10.1.

[✓] SSD_FORMFACTOR_ATTRIBUTES 신규(속성코드 14695 = 폼팩터). 사용자가 다나와
  "SSD" 검색결과 #prodArea outerHTML을 통째로 제공해서 실측(기존 패턴 재사용)
  → 실측 8종(M.2 2280/2242/2230/22110, 6.4cm(2.5형), Mini SATA(mSATA),
    PCIe 카드, 기타) 중 개인 조립 PC 범위 밖인 M.2 22110(서버/엔터프라이즈),
    mSATA(구형 노트북용), PCIe 카드(소수 애드인 카드 규격), 기타(불명확)는
    기존 트리밍 원칙대로 제외 — M.2 2280/2242/2230 + 2.5인치(SATA) 4종만 채택
  → /search의 `formfactor` 파라미터를 재사용(기존엔 메인보드/케이스 전용) —
    카테고리별로 완전히 다른 속성코드/딕셔너리를 쓰는 기존 방식(메인보드
    506 vs 케이스 6196) 그대로, SSD도 자기 딕셔너리(SSD_FORMFACTOR_ATTRIBUTES)로
    분리 추가. 새 쿼리파라미터 신설 아님 — 기존 파라미터의 적용 category만 확장
  → SSD는 이제 interface(기존)+formfactor(신규) 스펙 파라미터 2개 보유 —
    동시 지정 시 interface 우선 적용(`SSD_INTERFACE_ATTRIBUTES.get(interface)
    or SSD_FORMFACTOR_ATTRIBUTES.get(formfactor)`, main.py::search()).
    우선순위 기준은 명확한 원칙이 있다기보단 SSD 세대/성능을 더 직접
    나타내는 쪽을 우선한 결정(다른 2-스펙 카테고리와 마찬가지로 임의 결정)
  → 프론트: frontend/src/lib/specFilters.ts의 SSD 항목에 formfactor select
    추가(SSD_FORMFACTOR_OPTIONS). PartRow/SearchPage 둘 다 specDefs 배열
    기반 범용 렌더링이라 컴포넌트 코드 변경 없이 select 2개로 자동 확장됨
    (자동완성 드롭다운 우측 오프셋도 specDefs.length 기반이라 자동 대응 —
    GPU/메인보드/쿨러가 이미 2-스펙이라 검증된 패턴 재사용)
  → 검증: npm run typecheck 통과. mock 백엔드(danawa 함수 monkeypatch,
    attribute값별로 다른 픽스처 반환) + Vite dev + Playwright로 실측 —
    (1) /search?category=SSD&formfactor=... 쿼리 자체가 백엔드에서 올바르게
    필터링되는지 curl로 4종 전부 확인 + interface 동시 지정 시 interface
    우선 확인 + 다른 category에서 무시되는지 확인 (2) SearchPage에서 SSD
    탭 select 2개 렌더링 → 폼팩터 select로 "M.2 2280" 선택 → 검색 결과가
    실제로 필터링된 데이터만 나오는지 화면에서 확인 (3) BuildCreatePage
    (PartRow)의 SSD 행도 select 2개 정상 렌더링 + 자동완성 드롭다운이
    select들과 안 겹치는지 스크린샷으로 확인. pageerror 0건

다음에 이어갈 것:
  → 스펙 필터 확장 후보 남은 것: GPU 길이, 쿨러 냉각 방식(공랭/수랭) — 매번
    해당 카테고리 필터 사이드바 실측 필요(다나와 접근 차단 환경이라 사용자가
    #prodArea outerHTML로 제공하는 방식 계속 필요)
  → 그 외 미결 사항은 그 아래 2026-08-06 섹션("검색 UX 개편") 및
    "수정 예정 사항" 목록 참조(CORS 좁히기/APScheduler 등, 전부 보류 중)
```

---

## 오늘(2026-08-06) 검색 UX 개편 + 빌드 카드 배지 재배치
```
직전 세션(디자인 다듬기, v0.9.2)에서 컴포넌트 구조 변경이 필요해 보류했던
디자인 패치노트 2건을 사용자 확인 후 처리(frontend v0.10).

[✓] 빌드 카드 판정 배지 좌상단 겹침 배치
  → 기존: 카드 안쪽 상단에 인라인으로 놓인 태그(margin-bottom으로 제목과
    분리). 변경: 카드 hairline 위쪽에 살짝 걸치는 탭 형태로 이동
    (.build-card{position:relative}, .bc-tag{position:absolute;
    top:-13px;left:-1px}). 고가/저가는 잉크 완전 채움, 적정은 아웃라인만
  → 사용자가 "카드 유지 + 배지만 좌상단 겹침" 선택 — 홈/빌드목록이 공유하는
    .build-card 그리드 자체는 안 건드림(리스트형 전환 선택지는 기각)

[✓] 검색 화면(SearchPage) 카테고리+견적 카트 플로우로 전면 재구성
  → 사용자가 "전체 적용 — 새 검색+카트 플로우로" 선택. 기존엔 카테고리
    없이 키워드만으로 검색하는 단순 목록이었음(스펙 필터 자체가 아예
    없었음 — PartRow에만 있었음)
  → 새 구조: 카테고리 탭 8개(CATEGORIES 공유) → 검색창+스펙필터(카테고리별
    CATEGORY_SPEC_FILTERS 재사용) → 정렬탭(인기순 API순서 그대로/가격순은
    프론트 재정렬) → 결과 리스트(행마다 "담기" 버튼) → 우측 견적 카트
    패널(카테고리별 슬롯 8개, 채워진 슬롯 클릭시 제거, 빌드 이름 입력 +
    "이 구성으로 빌드 만들기" 버튼)
  → "빌드 만들기" 버튼은 기존 POST /builds(api.createBuild) 그대로 재사용
    — 새 엔드포인트 없음, BuildCreatePage와 마찬가지로 성공 시 새 빌드
    상세로 navigate
  → CATEGORIES + CATEGORY_SPEC_FILTERS(스펙 select 정의)가 기존엔
    PartRow.tsx 안에 갇혀 있었는데, SearchPage도 똑같은 정의가 필요해져서
    frontend/src/lib/specFilters.ts로 추출 — PartRow/BuildCreatePage/
    SearchPage 3곳이 이제 이 파일 하나를 단일 소스로 공유(main.py 주석의
    "키가 일치해야 함" 경고를 코드 레벨로 강제하게 됨)
  → 검증: npx tsc --noEmit 통과, mock 백엔드+Playwright로 카테고리 전환
    (CPU→GPU→쿨러, 스펙필터 자동 교체 확인)/스펙필터 지정 검색/담기→카트
    반영/다른 카테고리 담아도 기존 카트 유지/정렬 탭 전환/실제
    저장→빌드상세 이동까지 end-to-end 확인(CPU+GPU 담아 저장 → 총액
    162.8만원 = 62.9+99.9 정확히 일치). 8개 라우트 전부 pageerror 0건,
    BuildCreatePage(8행)·StatsPage(1행) part-row 개수로 specFilters.ts
    추출이 기존 두 화면을 안 깨뜨렸는지도 확인

[동작특성] 검색 화면 카트와 빌드생성(BuildCreatePage) 화면은 완전히
  독립된 두 진입 경로 — 상태를 공유하지 않음(둘 다 각자 로컬 state).
  검색에서 담다가 새로고침하면 카트 날아감(BuildCreatePage의 PartRow
  선택과 동일한 특성, 원래도 그랬음 — 새로 생긴 문제 아님)

다음에 이어갈 것:
  → 커밋/push 완료 여부 확인 필요(다음 세션에서 git log로 확인)
  → 디자인 패치노트 나머지 항목(제품 종류/버튼 disabled 스타일 등)은
    이미 v0.9.2에서 처리 완료, 이번 v0.10으로 패치노트 전체 항목 소진
```

---

## 오늘(2026-08-05) 쿨러 스펙 필터 추가
```
스펙 필터 확장의 마지막 남은 카테고리(쿨러) 완료 — 이걸로 8개 카테고리 중
스펙 필터가 없는 곳은 없어짐. backend v0.7 / frontend v0.9.1.

채택: 제품 종류(5종) + 지원 소켓(4종), select 2개(기존 GPU·메인보드와 동일).
  → COOLER_TYPE_ATTRIBUTES: CPU 쿨러 / 시스템 쿨러 / VGA 쿨러 /
    M.2 SSD 쿨러 / 써멀그리스 (속성코드 687). 다나와 "쿨러/튜닝" 카테고리엔
    케이스팬·써멀그리스·조명기기·VGA 지지대가 다 섞여 있어서 category
    필터만으론 CPU 쿨러가 안 걸러짐 — 이 카테고리에 특히 필요했던 필터
  → COOLER_SOCKET_ATTRIBUTES: AM5/AM4/LGA1851/LGA1700. 쿨러는 소켓 필터가
    인텔(6805)/AMD(6806) 두 그룹으로 갈려 있어 값에 따라 속성코드까지
    달라지는데, socket 파라미터 하나 + 기존 4개 값 계약은 그대로 유지하려고
    한 딕셔너리로 합침. 프론트도 SOCKET_OPTIONS 그대로 재사용
  → AM5 코드가 CPU/메인보드/쿨러 셋 다 다름(41-/500-/6806-) — 카테고리 간
    attribute 재사용 불가 원칙 재확인
  → 미채택: 냉각 방식(공랭/수랭, 속성코드 315758). 검색어로 대체 가능하고,
    어차피 같은 카테고리 스펙끼리는 상호배타라 3개를 넣어도 동시 사용 불가
  → 상세(실측값 전체·제외 근거·검증 3단계)는 실가_HISTORY.md 마지막 항목 참조

이번 세션 환경도 danawa.com 프록시 차단 지속(CONNECT 403) — 사용자가 "CPU
쿨러" 검색 결과 #prodArea outerHTML을 통째로 제공해서 진행. 필터 사이드바 +
상품목록 40건이 다 들어있어서 attribute 코드 확보(체크박스 480개 파싱)와
카테고리 사후 필터링 검증을 한 파일로 같이 할 수 있었음 — 다음에 스펙을 더
확장할 때도 이 형태(#prodArea outerHTML)로 받는 게 제일 효율적임

다음에 이어갈 것:
  → 스펙 필터는 여기서 일단락. 더 확장한다면 쿨러 냉각 방식(위 미채택),
    SSD 폼팩터, GPU 길이 정도가 후보 — 매번 그 카테고리 필터 사이드바
    실측 필요
  → 부수 발견(미수정): 케이스 행 select placeholder "지원 폼팩터 전체"가
    116px 폭에서 "지원 폼팩터 전"으로 잘림. PR #7 때부터 있던 것,
    기능 영향 없음
```

---

## 오늘(2026-08-05) 프론트 디자인 전면 교체 + 스펙시트 고도화
```
2차(스펙시트 고도화, 커밋 대기 중) — 1차 교체는 색/폰트/보더만 바꿨고
참조 디자인의 구조적 요소가 대부분 미사용이었어서, 그걸 실제로 이식하고
빌드 사이트(PCPartPicker) 관례를 얹음:
  → 빌드 상세를 실제 견적서 구조로 재구성: 4칸 스탯 스트립 / 큰 실측 합계 /
    실측↔판매가 대비 + 차액 배지 / 부품을 기능 그룹(연산부·그래픽·
    메모리저장·섀시전원)으로 묶은 스펙 행 + 총액 대비 비중 바 / 소계
  → 이동평균 무효로 즉시가 대체된 부품을 행 단위로 표시(기존엔 빌드
    전체 신뢰도 한 줄로만 알 수 있었음)
  → 빌드 생성 화면에 sticky 요약 바("구성 N/8" + 진행 틱 + 러닝 총액) —
    이걸 위해 SelectedPart에 raw price 필드 추가
  → 가격 정렬용 tabular-nums 전면 적용, 인쇄(견적서 출력) 스타일 추가
  → 미채택: 소비전력 추정(PCPartPicker 핵심 기능이나 부품별 TDP 데이터가
    스크래퍼에 없음 — 하려면 백엔드 작업 필요)
  → 상세는 실가_HISTORY.md 2026-08-05 마지막 항목 참조

1차(커밋 db61895, push 완료) — PR #7(카테고리·스펙 필터) 머지 확인 후,
사용자가 제공한 참조 디자인(에디토리얼 빌드 명세서 HTML, 종이/잉크 톤)으로
프론트 전체 톤을 교체함. 브랜치 claude/danawa-category-spec-filters-a04sod
(PR은 사용자가 직접 관리 중, 이 세션에서 PR 생성 안 함).

이번 교체 요약(자세한 팔레트/규칙은 REFERENCE.md #디자인-토큰 참조):
  → 이전 다크+시안/마젠타/앰버 네온 톤(Black Han Sans+JetBrains Mono) 폐기,
    종이(paper)/잉크(ink) 모노크롬 에디토리얼로 전면 교체 — accent color
    없음, 상태 구분은 ▲(고가)/▼(저가)/—(적정가) 기호로
  → 폰트: Pretendard(헤드라인 포함 전체) + IBM Plex Mono(데이터/코드).
    index.html의 구글폰트 링크 교체 완료(Black Han Sans 제거,
    IBM Plex Mono 추가)
  → border-radius/box-shadow 글로우 전면 제거, hairline(1px) 보더로만
    위계 표현. frontend/src/styles/global.css 전체를 다시 씀(546줄 규모
    전면 재작성, 클래스명은 유지해서 TSX는 대부분 안 건드림)
  → 빌드 상세(BuildDetailPage.tsx)의 판정 게이지를 SVG 반원+니들에서
    flat 수평 바(.gauge-track/.gauge-zone/.gauge-marker)로 재설계.
    diff_percent를 ±30%로 클램프해 바 위 0~100% 위치에 매핑,
    VERDICT_THRESHOLD_PERCENT(±5%, services/verdict.py)를 음영 구간으로
    표시. needleAngle() → markerPosition()으로 함수도 교체
  → 통계 탭(StatsPage.tsx) 가격 히스토리 차트: 글로우 필터+그라디언트
    라인 제거, flat ink 라인+옅은 회색 fill로 교체
  → 실측 발견 버그: build-grid에 쓴 "그리드 라인 트릭"(컨테이너
    background+gap:1px)이 아이템 개수가 3의 배수가 아닐 때 빈 셀에
    컨테이너 배경색이 그대로 노출되는 문제 발견 → 개별 카드에 자체
    border를 주는 방식으로 수정. flex-column 리스트(search-results,
    recent-list)는 이 문제 없어서 트릭 그대로 유지 — REFERENCE.md에
    이 함정 기록해둠
  → 검증: mock danawa 함수 + 임시 SQLite로 실제 빌드/즐겨찾기/히스토리
    데이터를 채워서 Playwright로 9개 화면(홈/검색/빌드목록/빌드생성/
    빌드상세(게이지 포함)/즐겨찾기/최근기록/통계(차트 포함)/설정) 전부
    실브라우저 확인. npm run typecheck 통과

다음에 이어갈 것:
  → 사용자가 Codespaces에서 직접 눈으로 확인 예정 — 이상 있으면 후속 조치
  → [완료] 쿨러 스펙 필터는 2026-08-05 이어진 세션에서 구현 완료
    (맨 위 섹션 참조)

※ 이 섹션의 "커밋 대기 중"은 이후 해소됨 — 2차(스펙시트)까지 커밋 6c226bf로
  들어갔고 PR #9로 머지 완료
```

---

## 오늘(2026-08-05) /search 카테고리·스펙 필터 확장 — PR #7
```
/search에 카테고리 필터 + 카테고리별 스펙 필터 구현 완료. 브랜치
claude/danawa-scraper-filters-bryp6q, PR #6(카테고리 필터+GPU 메모리+CPU
소켓+폴리시 2건, 커밋 eae25c4~16e5ef5)은 이미 머지·종료됨. 그 이후
GPU 칩셋 제조사(1624454)까지 커밋+push 완료, 이어서 진행한 메인보드
소켓/폼팩터·RAM 규격·케이스 지원폼팩터·파워 정격출력·SSD 인터페이스
6종은 아직 커밋 전(다음 "커밋하자" 지시 대기) — 상세 이력은
실가_HISTORY.md 2026-08-05 항목들(여러 개로 나뉘어 append됨) 참조,
여기는 최종 상태만 요약.

핵심 구현 방식(재구현/확장 시 반드시 참고):
  → category 필터: 요청 URL에 새 파라미터 추가하는 방식이 아님. 상품 li
    안의 input#productItem_categoryInfo_{code} 값(예: "PC 주요
    부품_그래픽카드")을 응답 파싱 단계에서 대조하는 사후 필터링
    (danawa.get_product_codes(category_label=...)). CATEGORY_LABELS 8개
    라벨 전부 실제 상품 li HTML로 직접 검증 완료(간접 검증 남은 것 없음)
  → 스펙 필터(memory_gb/chipset/socket/formfactor/ram_type/wattage/
    interface): 다나와 상세검색 필터 체크박스 클릭 시 URL에 실제로 붙는
    "{속성코드}-{값코드}-OR"(케이스만 -AND, 단일값 선택엔 동작 차이 없음)
    문자열을 danawa.get_product_codes(attribute=...)로 요청 URL에 그대로
    전달 — 이건 다나와 서버측 필터라 사후 필터링 아님
  → **카테고리 간 attribute 코드는 절대 재사용 불가** — 같은 물리적
    스펙(예: AM5 소켓)이라도 카테고리마다 다나와 내부 코드 자체가 다름
    (CPU 카테고리 AM5=41-801631-OR, 메인보드 카테고리 AM5=500-801682-OR).
    새 카테고리/스펙으로 확장할 때마다 매번 그 카테고리 자신의 필터
    사이드바에서 다시 확보해야 함(대표 검색어로 검색 → 필터 체크박스
    아무거나 클릭 → 바뀐 URL의 attribute= 값 확인, 또는 페이지 HTML의
    체크박스 value 속성 직접 읽기)
  → 같은 카테고리 안에 스펙 필터가 2개인 경우(GPU: memory_gb+chipset,
    메인보드: socket+formfactor) **동시 적용 불가** — get_product_codes의
    attribute 인자가 값 하나만 받음(다중 attribute 결합 규칙 미검증).
    백엔드는 `A.get(x) or B.get(y)` 체이닝으로 우선순위 처리, 프론트
    (PartRow)는 같은 카테고리의 스펙 select를 상호 배타로 구현(하나
    고르면 다른 하나 자동 해제) — 자세한 우선순위는 REFERENCE.md
    #엔드포인트-설계 표 참조
  → 데이터 트리밍 원칙: 워크스테이션/서버/구형/데이터센터급 값(예: GPU
    94~96GB, CPU/메인보드 sWRX8·sTRX4·SP3 등 구형 소켓)은 실측은 됐어도
    개인 조립 PC 범위 밖이라 매핑 딕셔너리에서 의도적으로 제외 — 신규
    스펙 추가 시에도 같은 원칙 적용

현재 CATEGORY_LABELS(카테고리 필터, 8개 전부) + 스펙 필터 매핑
(backend/app/main.py):
  GPU      → memory_gb(GPU_MEMORY_ATTRIBUTES, 11개), chipset(GPU_CHIPSET_ATTRIBUTES, NVIDIA/AMD/Intel)
  CPU      → socket(CPU_SOCKET_ATTRIBUTES, AM5/AM4/LGA1851/LGA1700)
  메인보드 → socket(MAINBOARD_SOCKET_ATTRIBUTES, 값은 CPU와 다름), formfactor(MAINBOARD_FORMFACTOR_ATTRIBUTES, ATX/M-ATX/ITX/E-ATX)
  케이스   → formfactor(CASE_FORMFACTOR_ATTRIBUTES, "장착 가능한 보드 크기" 의미 — 메인보드 폼팩터와 라벨은 같지만 코드는 다름)
  RAM      → ram_type(RAM_TYPE_ATTRIBUTES, DDR5/DDR4)
  파워     → wattage(PSU_WATTAGE_ATTRIBUTES, "450W~499W"~"1000W~1299W" 7구간)
  SSD      → interface(SSD_INTERFACE_ATTRIBUTES, SATA3/PCIe3.0x4/PCIe4.0x4/PCIe5.0x4)
  쿨러     → 카테고리 필터만(스펙 필터 없음)

프론트(frontend/src/components/PartRow.tsx): CATEGORY_SPEC_FILTERS 설정
객체로 카테고리→스펙 select 목록을 선언적으로 관리(select 반복 렌더링,
상호배타는 단일 specValue 상태로 자연스럽게 보장됨 — 여러 개의 boolean
필드 대신 {key, value} 하나만 유지). api.ts::api.search(q, category?,
spec?)의 spec은 SearchSpecParams 객체(URLSearchParams로 쿼리 조립) —
포지셔널 인자가 계속 늘어나는 걸 막으려고 이번에 리팩터함. select 폭은
116px(긴 라벨 "800W~899W" 대응), 자동완성 드롭다운은 그 행에 보이는
select 개수(specDefs.length) 기반 인라인 스타일로 겹침 방지.

검증 방식(전부 이 패턴 반복 — 다음에도 재사용): ① 사용자가 제공한 실제
danawa 상품목록/필터 HTML을 requests.get mock 응답으로 고정해 백엔드
필터링 로직 오프라인 검증 ② FastAPI TestClient로 쿼리파라미터 조합별
attribute 전달 경로 확인 ③ mock 백엔드(danawa 함수 monkeypatch) +
Vite dev 서버를 Playwright(사전 설치 Chromium)로 실제 조작해서 UI까지
확인 — 이 과정에서 실제 CSS 버그 2건 발견+수정함(select appearance
리셋 누락으로 폭 무시되던 문제, 자동완성 드롭다운이 select를 덮던 문제)

다음에 이어갈 것:
  → [완료] 위 6종 커밋+push 완료(ab60a3e, f31c295), PR #7 생성·머지 완료.
    이후 PR #8(UI 톤 다듬기)·PR #9(디자인 교체+스펙시트)까지 머지됨
  → [완료] 쿨러 지원 소켓 — 2026-08-05 이어진 세션에서 제품 종류와 함께
    구현 완료(맨 위 섹션 참조)
  → 더 확장하려면(예: SSD 폼팩터) 매번 그 카테고리 필터 사이드바 실측
    필요 — 이 환경은 danawa.com 프록시 차단 지속이라 사용자가 로컬
    브라우저에서 HTML 복사해서 전달하는 방식 계속 필요
```

---

## 오늘(2026-08-04) 세션 마무리
```
PR #3 main 머지 완료 (2026-08-04) — 아래 1~5번이 main에 반영됨. 6~7번은
같은 날 이어서 작업, 아직 커밋/PR 전(다음 "커밋 하자"/"PR 하자" 지시 대기).
브랜치 claude/frontend-moving-average-integration-qkazx9는 머지된 main
기준으로 리셋해둠(과거 커밋 위에 새로 쌓지 않도록).

이번 세션에 완료(1~5는 PR #3, 커밋 순서대로):
  1. verdict 이동평균 프론트 연동 — 설정 탭 드롭다운 + localStorage (v0.2)
  2. _fetch_lowest_price danawa 연결 장애 처리 누락 수정 (v0.4.2)
  3. category/cash_price 필드 스크래퍼 구현 (v0.4.3)
  4. 통계 탭 가격 히스토리 차트 구현 (frontend v0.3)
  5. 홈 탭 최근빌드 대시보드 구현 (frontend v0.4)
  6. 최근기록 탭 — 최근 조회한 부품 구현 (frontend v0.5) — 커밋/push 대기
  7. 즐겨찾기 탭 — 관심 상품 북마크 구현, favorites DB 테이블 신규
     (backend v0.4.4, frontend v0.6, 아래 "우선순위 낮음" 섹션 참조) —
     커밋/push 대기

사이드바 7탭 전부 실데이터 연동 완료 — PlaceholderPage 컴포넌트 자체를
삭제함(더 이상 아무 데서도 안 씀).

이어서(같은 날, v13) 완료:
  → frontend .env 기반 API_BASE 문서화 완료(.env.example 신규 +
    README.md 보강, "현재 구현 상태" 표도 스테일해서 같이 최신화)
  → scripts/e2e_smoke_test.py에 홈/통계/최근기록/즐겨찾기 4개 화면 스텝
    추가 완료(기존 4스텝 + 신규 8스텝, 총 12스텝) — mock 백엔드 +
    Playwright로 전체 흐름 실제 실행해서 통과 확인(아래 "우선순위 높음"
    섹션 v13 참조)
  → 커밋/push 대기 (다음 "커밋 하자" 지시 대기)

남은 것(순서 무관, 모두 낮은 난이도/보류):
  → 배포 시 CORS allow_origins=["*"] 좁히기는 실제 배포 계획이 생길 때까지
    계속 보류(인수인계.md에 명시된 기존 결정 유지)

이 세션 환경 특이사항 (다음 세션에도 반복될 수 있음):
  → 프록시 정책이 danawa.com 전체를 막고 있어(curl/WebFetch 둘 다 403)
    라이브 다나와 검증이 이 환경에서는 불가능함 — 사용자가 로컬 브라우저에서
    HTML을 직접 복사해서 붙여넣어주는 방식으로 실측 진행함(category/
    cash_price 구현 때 실제로 이렇게 함). 같은 상황이면 이 방법 재사용
  → 프론트 기능 검증은 danawa.* 함수를 python mock으로 교체한 로컬
    백엔드 + Playwright 조합으로 진행 (이번 세션 통계/홈/최근기록/즐겨찾기
    탭 검증에 전부 사용, 재사용 가능한 패턴)
  → 즐겨찾기 탭 구현 중 이 mock+Playwright 검증 과정에서 실제 프론트
    버그 하나 발견+수정함(PartRow를 selected=null 고정으로 재사용하면
    두 번째 검색부터 자동완성이 안 뜸 — 아래 "결정 완료"/우선순위 낮음
    섹션 참조). mock 검증이 단순 확인 이상으로 실제로 버그를 잡아낸 사례
```

---

## 읽기 순서 안내
```
① 실가_인수인계.md (이 파일)  — 세션 시작 시 필수. 현재 상태/수정예정/미결사항
② 실가_REFERENCE.md           — 규칙/구조/절차. 작업과 관련된 섹션만 필요 시 확인
③ 실가_HISTORY.md             — 변경 이력 (append-only). 특정 시점 이력 추적 시에만 확인

문서 갱신 규칙:
  코드 작성/수정 시 → 이 파일(상태/수정예정) 갱신 + HISTORY.md append
  REFERENCE.md는 규칙/구조 자체가 바뀔 때만
```

---

## 파일별 최신 버전
```
app-shell-mockup.html    v0 (정적 목업, 유지 — 프론트 실 구현체(frontend/)가
                             이 디자인을 그대로 이식해서 대체함)
danawa_patched.py        편입 완료 → backend/app/services/danawa.py (v0.2).
                             get_price_variance 원본과 통합된 단일 모듈.
                             (git 리포: 커밋 4개, v0.2~v0.4 + docs)
backend/                  v0.8 (2026-08-06. SSD_FORMFACTOR_ATTRIBUTES 신규 —
                             기존 formfactor 파라미터를 SSD category에도
                             적용(M.2 2280/2242/2230/2.5인치). 새 파라미터
                             신설은 아니지만 파라미터의 유효 적용범위가
                             늘어난 계약 변경이라 두 번째 자리 v0.7→v0.8)
                          v0.7 (2026-08-05. v0.6에서 쿨러 스펙 필터 2종
                             (cooler_type 신규 파라미터 + 쿨러 전용 socket
                             매핑) 추가 — 쿼리파라미터 신설이라 계약 변경,
                             두 번째 자리 v0.6→v0.7. 아래는 v0.6까지의 내역)
                          v0.6 (danawa 연결 장애 처리 결함 수정 + category·
                             cash_price 필드 스크래퍼 구현 + favorites
                             테이블/엔드포인트 신규 + /search 카테고리 필터
                             + 카테고리별 스펙 필터 7종(GPU 메모리/칩셋,
                             CPU·메인보드 소켓, 메인보드·케이스 폼팩터,
                             RAM 규격, 파워 출력, SSD 인터페이스) 신규 —
                             2026-08-05, API 계약 변경 누적이라 두 번째
                             자리 v0.5→v0.6 재차 올림)
frontend/                 v0.10.1 (2026-08-06. SSD 스펙 필터에 formfactor
                             select 추가(specFilters.ts SSD 항목에 항목 하나
                             추가한 것뿐 — PartRow/SearchPage 컴포넌트 로직은
                             무변경, 둘 다 specDefs 배열 기반 범용 렌더링이라
                             자동 확장됨). 세 번째 자리)
                          v0.10 (2026-08-06. 보류했던 디자인 패치노트 2건
                             처리 — (1) 빌드 카드 판정 배지를 상단 인라인
                             태그에서 카드 좌상단 hairline에 걸치는 탭
                             형태로 재배치(.build-card position:relative +
                             .bc-tag absolute, 카드 그리드 구조는 유지),
                             (2) 검색 화면을 카테고리 탭 + 스펙필터 +
                             정렬탭 + 견적 카트 2단 레이아웃으로 전면
                             재구성(SearchPage.tsx 재작성) — 검색하며 여러
                             카테고리를 담아 그 자리에서 빌드로 저장하는
                             새 진입 경로 신설(기존 POST /builds 그대로
                             재사용, 새 엔드포인트 없음). PartRow/
                             BuildCreatePage와 공유하던 CATEGORIES·
                             CATEGORY_SPEC_FILTERS를 lib/specFilters.ts로
                             추출해 3곳(PartRow/BuildCreatePage/SearchPage)
                             단일 소스화. 화면 구성이 실질적으로 바뀌어
                             두 번째 자리 올림 — 백엔드 API 계약 변경 없음.
                             상세는 실가_HISTORY.md 참조)
                          v0.9.2 (2026-08-06. 사용자가 claude.ai에서 다듬어온
                             디자인 패치노트 반영 — 버튼 위계(secondary 추가),
                             .part-spec-filter 116→144px 잘림 수정, 게이지
                             트랙 잉크 반전+퍼센트 라벨, .sum 잉크 반전,
                             부품 그룹 2px 보더 컨테이너, 탑바 인장+브레드크럼.
                             API 계약 변경 없음, CSS/마크업만 — 세 번째 자리)
                          v0.9.1 (2026-08-05. 쿨러 스펙 필터 select 2개 추가.
                             CATEGORY_SPEC_FILTERS 설정 객체에 항목 하나
                             추가한 것뿐이라 컴포넌트 로직 무변경 —
                             세 번째 자리)
                          v0.9 (2026-08-05. v0.8.1에서 다크네온→종이/잉크
                             모노크롬 에디토리얼로 톤 전면 교체, 이어서
                             v0.9에서 빌드 상세를 견적서 구조로 재구성 +
                             빌드 생성 sticky 러닝 총액 + 인쇄 스타일 추가.
                             화면 구성 자체가 바뀌어 두 번째 자리 올림
                             — 백엔드 API 계약 변경은 없음)
```
**GitHub 원격 리포 연동 완료** — origin이 `limfighter/silga`로 설정돼 있고
이번 세션 작업 전부 push까지 끝남(브랜치:
`claude/frontend-moving-average-integration-qkazx9`, PR 아직 미생성).
실행 환경(컨테이너)이 세션마다 리셋돼도 git 리포에 커밋+push만 해두면
다음 세션에서 그대로 이어받을 수 있음 — 예전엔 zip 업로드가 필요했지만
더 이상 해당 없음.

---

## 현재까지 확정된 사항 (요약 — 상세는 REFERENCE.md)
```
- 개발 순서: 웹 먼저 → API 명세 확정 → Flutter 이식 (디버깅 편의 이유로 결정)
- 백엔드: FastAPI
- 데이터소스 1순위: danawa-py 기반, 단 원본 그대로는 못 씀 — 자체 패치본
  (danawa_patched.py) 사용 확정. 근거는 아래 "검증 안 된 항목" 결과 참조
- 화면 구조: 사이드바 7탭 확정, 빌드 탭 3단계 흐름(목록/생성/상세) 목업 완료
- API 계약 초안 확정 (REFERENCE.md #엔드포인트-설계) — 프론트 목업이 이 계약 전제로 만들어짐
- 디자인 토큰 확정 (2026-08-05 개정 — 종이/잉크 모노크롬 에디토리얼,
  Pretendard+IBM Plex Mono. 이전 다크+시안/마젠타/앰버 네온+Black Han Sans는
  폐기. 상세는 REFERENCE.md #디자인-토큰 참조)
- 프로젝트 정식 이름 — **실가**로 확정 (2026-08-03, 이전 임시 코드네임: PPE)
```

---

## 검증 안 된 항목 (코드 작성 전 반드시 확인)
```
[x] ★최우선 — danawa-py 실제 호출 테스트 (2026-08-03 완료)
    → 3개 함수 모두 예외 없이 호출은 됐으나, 2개 함수는 다나와 측 HTML/DOM
      구조 변경으로 필드 일부가 비어서 나옴(파싱 실패). 상세는 HISTORY.md
      2026-08-03 v1 항목 참조. 요약:
        - get_product_codes: 검색어 URL 인코딩 누락 + 카테고리별 li 클래스
          불일치(GPU만 우연히 동작, CPU/RAM/SSD 등은 0건)로 패치 필요 → 완료
        - get_product: 최저가 섹션 클래스가 lowest_area → price-summary로
          변경되어 lowest_price/prices가 항상 빈 값으로 나옴 → 패치 완료,
          쇼핑몰명 + variants(정품/벌크/해외구매) 필드까지 확장
        - get_price_variance: 원본 그대로 정상 동작, 패치 불필요
    → 패치본: danawa_patched.py (get_product_codes, get_product 2개 함수만
      재작성 포함. get_price_variance는 원본 danawa.py 그대로 import해서 사용)

[x] danawa_patched.py 실가 프로젝트 정식 경로 편입 → 완료(체크 누락 상태로
    방치돼 있던 걸 2026-08-05 세션에서 발견해서 정정). backend/app/services/
    danawa.py로 vendoring 완료된 지 오래됨 — 파일 상단 도크스트링에 출처
    명시돼 있고, 이후 세션들에서 이 파일에 계속 패치를 쌓아왔음(category_label/
    attribute 인자 등, 실가_HISTORY.md 참조)
    → PyPI 미등록 + setup.py 없음 확인됨 → pip install 불가, 파일 자체를
      프로젝트에 vendoring(복사)하는 방식으로 편입(git submodule 등 X)

[ ] 다나와 공식 오픈API(api.danawa.com) 생사 확인
    → robots.txt로 자동 조회 불가 확인됨 — 브라우저로 직접 접속해
      키 발급 페이지 살아있는지, "가격정보" API가 실제 공개 범위에
      포함되는지 사람이 직접 확인 필요
    → 살아있고 가격 데이터 포함이면 danawa-py보다 우선 채택 검토

[ ] danawa.com robots.txt 상세 확인
    → 전체 도메인 차단만 확인됨(자동fetch 실패), 세부 Disallow 경로는
      브라우저/curl로 직접 조회 안 함 — 크롤러 설계 전 확인 권장
```

---

## 결정 완료 (2026-08-03, 같은 세션 내 논의)
```
[x] API 계약(/product/{code}) 응답에 variants 필드 포함 여부 → 포함 확정
    → /product/{code} 응답에 variants(정품/벌크/해외구매 등 유형별 최저가
      +몰수+pcode) 필드로 노출. 단 참고 정보 한정
    → /estimate, /compare(실측 합계·판정 게이지 등 핵심 계산 로직)에는
      variants 절대 미반영 — 해외구매는 통관/배송 지연(3~4주)/AS 불가 등
      국내 구매와 리스크가 근본적으로 달라 판정 로직 오염 우려
    → 프론트(빌드 상세 화면): breakdown 리스트에 "해외구매 참고가 ~원"
      부가 라인으로만 표시, 실측 합계/판정 게이지 계산에서는 제외
    → REFERENCE.md #엔드포인트-설계에 반영 완료 (API 계약 변경, 버전
      두 번째 자리 영향 — 실 구현 시 v0.2 표기)

[x] danawa_patched.py의 prices 리스트 마지막 항목 신뢰도 처리 → 원칙 확정
    → 실측 합계/판정 계산은 항상 lowest_price 필드(다나와 공식 최저가,
      hidden input 기반) 단일 값만 사용
    → prices 리스트(판매처별 가격)는 화면 표시(판매처 리스트 UI) 전용,
      이 리스트에서 직접 min() 계산해서 최저가로 쓰는 코드는 작성 금지
    → 근거: (1) 리스트 끝에 다나와가 최저가 산정에서 제외한 판매처가
      섞여 신뢰도 낮음 (2) 우리가 스크래핑하는 리스트 자체가 상위 일부
      (약 10건)만 잘려 나와 있어 전체 최저가와 다를 수 있음(불완전)
    → REFERENCE.md #엔드포인트-설계 설계 원칙에 반영 완료

[x] 저장소 구조 → backend/ + frontend/ 2개로 확정 (API는 별도 폴더 아님)
    → API는 backend(FastAPI) 안에 포함된 개념, 별도로 안 쪼갬
    → Flutter 이식 시점(Phase 5)에 flutter/ 추가되어 3개 체제로 전환 예정,
      지금은 2개만 만들면 됨
    → REFERENCE.md #저장소-구조에 반영 완료

[x] DB 스키마 → products / builds / build_items 3개 테이블로 확정,
    price_history는 만들지 않기로 결정 (오버엔지니어링 판단)
    → 개별 부품 가격 추이·빌드 총액 히스토리 둘 다 danawa의
      get_price_variance()를 그때그때 호출해서 계산 가능, 별도 축적
      테이블/스케줄러 불필요하다고 판단
    → APScheduler 우선순위는 그대로 낮음 유지(지정가 알림 등 실제 주기
      감시가 필요해질 때 재검토)
    → REFERENCE.md #DB-스키마에 반영 완료

[x] 배포 플랫폼 후보 갱신 → 결정 자체는 여전히 미룸(원격 접근 필요해질
    때 결정), 단 후보군에 GCP(Compute Engine VM / Cloud Run) 추가 검토
    → VM: 지금 스택(FastAPI+SQLite 파일) 무수정 배포 가능, 확장은 수동
    → Cloud Run: SQLite를 GCS FUSE로 마운트해야 해서 단일 인스턴스 고정
      필요 — 결국 SQLite 쓰는 한 서버리스 자동확장 이점을 못 씀. 진짜
      확장 필요해지면 Postgres 이전 + 멀티 인스턴스가 정공법
    → Cloudflare(Workers)는 후보에서 제외 확정 — Python이 Pyodide 경유라
      FastAPI가 그대로 안 올라가고 SQLite도 D1/Durable Objects로 갈아타야
      해서 스택 재설계 수준의 변경이 필요함
    → REFERENCE.md #기술-스택 배포 항목에 반영 완료
```

---

## 결정 완료 (2026-08-04, verdict 이동평균 도입 논의 — 설계만 확정, 구현 전)
```
배경: verdict가 조회 시점의 스팟 스크래핑 값(순간 lowest_price) 하나로만
  판정돼서, 부품이 일시 특가/재고 급변으로 순간적으로 폭락·폭등하면 같은
  빌드인데도 조회 타이밍에 따라 고가↔저가↔적정가로 판정이 오락가락하는
  문제 발견 (예: 평소 10만원 부품이 물량 이슈로 하루 5만원 됐다가 복귀하는 케이스)

[x] estimate_total(총 견적가, /estimate 응답의 total_price)은 즉시가(현재
    최저가) 그대로 유지 — "지금 당장 사면 얼마"라는 원래 의미를 보존하기
    위해 이동평균 도입 대상에서 제외

[x] verdict 판정 "기준가"만 부품별 이동평균으로 계산하도록 분리
    → 기존 get_price_variance()를 재사용(별도 신규 스크래핑 함수 불필요),
      1/3/6/12개월 daily 시계열에서 최근 N일만 슬라이싱해서 평균
    → N(이동평균 기간)은 7일/14일/30일 중 사용자가 선택, 기본값 14일
    → 선택값은 설정 탭에 드롭다운으로 노출, localStorage에 저장(용량/영속성
      문제 없음 확인 — origin당 수 MB 할당되는데 문자열 하나만 저장), DB
      테이블 신설 안 함(과설계 지양 원칙 유지)
    → compare류 엔드포인트는 ma_window 쿼리 파라미터로 받음 (기본 14)

[x] 이동평균 유효성 기준: "엄격" 채택
    → 선택한 기간(N일)만큼 daily 데이터가 정확히 다 있어야 그 부품의
      이동평균이 유효. 예: 30일선인데 신상품이라 20일치 데이터뿐이면 그
      부품은 이동평균 무효 처리 (완화된 "절반 이상이면 계산" 안은 기각)

[x] 부품별 이동평균 무효 시 fallback 규칙 확정 (중요 — 최초안이었던
    "부품 하나라도 무효면 빌드 전체 판정 null" 원칙을 뒤집음)
    → 이동평균 무효인 부품은 그 부품만 즉시가(현재 lowest_price)로 대체해서
      합산 계속 진행. 빌드 전체를 null로 날리지 않음
    → 대신 응답에 신뢰도를 투명하게 노출: verdict_confidence("high"/"low",
      fallback 섞이면 low) + 부품별 어떤 값을 썼는지 breakdown(source:
      "ma" | "current_fallback")
    → 이 방식이 "전체 null" 방식보다 오히려 구현이 단순함(조기 종료 분기
      불필요, 부품마다 이동평균 있으면 그거 없으면 즉시가 골라 합산하면 끝)
      + API 호출도 추가 안 됨(fallback 판단에 필요한 값들을 이미 부품별로
      조회하는 중이라) + "판정 불가" 빈 상태 UI를 거의 안 만들어도 됨
      (전 부품 스크래핑 실패 같은 진짜 예외 상황에서만 null 발생)

[x] verdict 임계값(VERDICT_THRESHOLD_PERCENT, ±5%)은 그대로 유지
    → 고가/저가 비대칭 임계값(예: 고가 +5%, 저가 -10~15%)도 검토했으나
      이번엔 채택 안 하고 대칭 ±5% 유지로 결론

[x] GET /builds(목록) 성능/일관성 대응 — (build_id, ma_window) 키로 TTL
    5분 메모리 캐시 적용 확정 (DB 저장 아님, 프로세스 인메모리 캐시)
    → 이동평균 계산 도입으로 부품당 스크래핑 호출이 2배(get_product +
      get_price_variance)가 되는데, 목록 화면이 저장된 빌드 전체를 매번
      순회 재계산하는 구조(REFERENCE.md 기존 설계)라 체감 지연 우려 발견
    → 캐시로 "목록→상세→목록" 같은 흔한 네비게이션에서 재스크래핑 방지 +
      목록/상세 판정 불일치(캐시 미스 타이밍 차이로 다른 값 뜨는 문제) 방지
    → 캐시 대상은 이동평균 파트(verdict_basis_price 등)만, 즉시가
      total_price는 캐시 안 하고 매번 새로 조회(기존 "가격 자체 축적 금지"
      원칙과 최대한 안 부딪히게)
    → POST /build/compare(저장 전 미리보기, build_id 없음), GET
      /product/{code}/compare(단일 상품, 반복 조회 문제 없음)는 캐시
      대상에서 제외 확정

[x] REFERENCE.md #엔드포인트-설계 스키마 갱신 완료 (verdict_basis_price,
    verdict_basis_price_formatted, verdict_confidence, verdict_basis_breakdown,
    ma_window 필드 반영, "verdict 판정 기준가(basis_price) — 이동평균 도입"
    섹션 신설)

[x] 백엔드 코드 구현 완료 (2026-08-04)
    → services/verdict.py: compute_ma_price() 신규(엄격 원칙 + full_date
      명시 정렬), calc_verdict()는 파라미터명만 basis_price로 정리(로직 동일)
    → main.py: _fetch_ma_price(), _compute_verdict_basis()(fallback+신뢰도),
      _get_cached_verdict_basis()(TTL 5분 캐시) 신규, /product/{code}/compare
      · /build/compare · GET /builds · GET /builds/{id} 4개 엔드포인트에
      ma_window 파라미터 + verdict_basis_* 필드 반영
    → schemas/compare.py: VerdictBasisItem 신규, schemas/estimate.py·build.py에
      필드 추가
    → 개발 중 발견: ma_window를 Literal[7,14,30] 타입으로 쿼리파라미터에
      바로 쓰면 fastapi 0.141/pydantic 2.13 조합에서 쿼리스트링("14")을
      int로 캐스팅 안 하고 422 내버리는 문제 실측 확인 → int + 수동 검증
      (/history의 months 검증과 동일 패턴)으로 변경해서 해결
    → mock으로 이동평균 정상/fallback/캐시 동작 전부 로컬 검증 완료
      (FastAPI TestClient + danawa 함수 patch)
    → [x] 라이브 다나와 검증 완료 (2026-08-04, 사용자 로컬 환경) — 실측
      결과 full_date 포맷은 "YY-MM-DD"(예: "26-05-12")로 정렬 로직은
      문제없었으나, **다나와가 daily가 아니라 주 단위로 데이터를 줌**(1개월치
      조회해도 포인트 4개 안팎)이 확인돼 "정확히 N개 항목 있어야 유효"였던
      원래 엄격 원칙이 항상 실패하는 치명적 문제 발견 → compute_ma_price()를
      "날짜 기준 최근 window일 이내 데이터 + 히스토리가 window일 전체를
      커버해야 유효"로 재설계, _fetch_ma_price도 get_price_variance(code, 1)
      대신 (code, 3)으로 변경(1개월치만으론 30일 커버리지 체크가 부족해서).
      재검증 완료 (실가_HISTORY.md 2026-08-04 v5 참조)
    → [x] 프론트 연동 완료 (같은 날, PR #2 머지 후 이어서) — 설정 탭
      드롭다운 + localStorage(useMaWindow 훅), BuildListPage/
      BuildDetailPage가 이 값을 읽어 GET /builds, GET /builds/{id}에
      ma_window로 실어 보냄. BuildCreatePage는 POST /builds가 애초
      ma_window를 받지 않아(생성 직후엔 verdict가 항상 null) API 호출
      변경은 없고 안내 문구만 추가. SearchPage는 비교(compare) 기능
      자체가 없어 변경 없음 (실가_HISTORY.md 2026-08-04 v6 참조)
    → [x] danawa 연결 장애 처리 결함 수정 완료 (같은 날, 이어서) — 아래
      "수정 예정 사항" 참조
```

---

## 수정 예정 사항

### 🟡 우선순위 높음 — 전부 완료 (2026-08-03 Phase 3~4 세션)
```
[x] 저장소 구조 실제 생성 (backend/ + frontend/) → 완료, git 리포도 형성됨
    (커밋 4개: v0.2 백엔드 스켈레톤 / v0.3 빌드CRUD / v0.4 프론트 / docs)

[x] danawa_patched.py를 backend/app/services/danawa.py로 편입 + 원본
    danawa.py의 get_price_variance 함수와 합쳐서 단일 모듈로 정리 → 완료
    (GitHub MineEric64/danawa-py 원본에서 get_price_variance 이식, 나머지 두
    함수는 패치본 그대로)

[x] FastAPI 스켈레톤 + /search, /product/{code} 두 엔드포인트 구현 → 완료,
    실제 다나와 라이브 호출로 검증 완료

[x] DB 스키마 실제 구현 → 완료
    → products / builds / build_items 3개 테이블, SQLAlchemy 모델 작성 완료
    → SQLite(WAL) 확정. 단, SQLAlchemy의 DateTime(timezone=True)가 SQLite
      다이얼렉트에서 오프셋을 저장/복원하지 못하는 버그 발견 — "타임존 미표기
      금지" 체크리스트 원칙과 충돌해서 KSTDateTime 커스텀 TypeDecorator로
      해결함(timezone_utils.py). 앞으로 datetime 컬럼은 반드시 이걸 사용할 것

[x] /product/{code}/history, /estimate, /compare 엔드포인트 구현 → 완료
    → /compare는 문서 계약(단일상품 기준)과 silga-mockup.html 예시(빌드전체
      기준)가 스펙 불일치였음 → 확인 후 단일상품용(원 계약 유지) + 빌드전체용
      신규(POST /build/compare) 둘 다 구현. #엔드포인트-설계에 반영 완료
    → 빌드 저장/조회 CRUD(POST/GET /builds, GET /builds/{id})도 신규 추가
      — DB 스키마는 있었는데 채워넣는 엔드포인트가 누락돼 있었음
    → /product/{code} 응답에 in_stock 필드 추가(원 계약 외) — "일시 품절"과
      "스크래핑 실패"를 구분 못 하던 문제 발견해서 해결 (실측: PALIT
      RTX5070Ti 76465883이 일시 품절 상태였음)
    → verdict 판정 임계값은 REFERENCE.md에 수치가 없어 ±5%로 가정하고 구현
      (services/verdict.py 상수 분리, 실사용해보고 조정 필요 — 결정 필요 항목 참조)

[x] 검색 탭 프론트 실데이터 연동 → 완료 (Vite+React+TS+TanStack Query 스캐폴딩)

[x] 빌드 탭 "생성" 화면의 부품 검색 입력에 실제 자동완성/드롭다운 연결 → 완료
    → 디바운스 500ms 적용 (매너 크롤링 원칙 고려)
    → 검색 결과 매칭 느슨한 문제(위에 적힌 현상)는 그대로 남아있음 — 카테고리
      필터링은 아직 안 걸었음, 자동완성에서 관련없는 상품이 섞일 수 있음
    → Playwright E2E 스모크 테스트로 검색→생성→상세→목록 전체 흐름 실브라우저
      검증 완료 (scripts/e2e_smoke_test.py)
    → 버그 수정: 자동완성 로딩 placeholder("검색 중...")가 클릭을 가로채는
      레이스 컨디션 발견 → pointer-events: none으로 해결
```

### 🟡 우선순위 높음 (신규 — Phase 3~4 완료 후 생긴 항목)
```
[x] verdict 판정 임계값(±5% 가정값) 확정 논의 → ±5% 그대로 유지하기로 확정
    (2026-08-04). 대신 판정 "기준가" 계산 방식 자체를 이동평균 도입으로
    개선하기로 함 — 설계 확정, 아래 신규 항목 참조

[x] verdict 이동평균 기준가 도입 — 백엔드 구현 완료 (2026-08-04)
    → estimate_total(즉시가)과 verdict 기준가(이동평균) 분리, ma_window
      쿼리파라미터(7/14/30, 기본 14), 부품별 이동평균 무효 시 즉시가
      fallback + verdict_confidence 플래그, GET /builds 목록용 TTL 5분
      메모리 캐시 — 전부 구현+로컬 mock 테스트 완료, 위 "결정 완료" 섹션 참조
    → [x] 라이브 다나와 검증 완료 (사용자 로컬 환경, 2026-08-04) — 다나와가
      주 단위로 데이터를 주는 걸 발견해서 compute_ma_price() 재설계함
      (위 "결정 완료"/실가_HISTORY.md v5 참조)
    → [x] 프론트 연동 완료 (2026-08-04, 같은 날 이어서) — 설정 탭
      ma_window 드롭다운 + localStorage, BuildList/BuildDetail 연동
      (실가_HISTORY.md v6 참조)

[x] _fetch_lowest_price() danawa 연결 장애 처리 누락 → 수정 완료 (2026-08-04,
    프론트 연동 세션 이어서)
    → 원래 결함: main.py의 _fetch_lowest_price()가 danawa.get_product()
      호출을 try/except로 안 감싸서, requests.RequestException(네트워크
      장애)이 나면 CLAUDE.md 원칙(스크래퍼 장애→503, 정상 품절/파싱실패→404)을
      못 지키고 500 unhandled exception으로 죽었음. 이 환경 프록시가
      danawa.com을 막아서 우연히 재현됨(ProxyError) — 실제 운영에서도
      타임아웃/DNS 실패 등 같은 예외 클래스로 재현 가능한 진짜 결함으로 판단
    → 결정: "엔드포인트별 기존 선례 따르기" 채택 — 새 정책을 만들지 않고
      저장소에 이미 있던 두 선례를 그대로 적용
        (a) 단일 상품 조회(GET /product/{code}/compare)는 GET
            /product/{code}(get_product_detail)와 동일하게 즉시 503
        (b) 다중 부품 합산(_compute_estimate 공유 — POST /estimate, POST
            /build/compare, GET /builds, GET /builds/{id})은 POST
            /builds(create_build)·_fetch_ma_price의 기존 부품별 fallback
            관례를 따라 항목별로 삼켜서 그 부품만 title/price=None 처리,
            나머지 부품은 계속 진행
    → 구현: compare_single_product에 try/except RequestException → 503
      추가, _compute_estimate 루프 내부에 try/except RequestException →
      (None, None) 추가 (main.py)
    → 검증: 이 환경(danawa 접근 차단)에서 실측 — GET
      /product/{code}/compare는 503 확인, GET /builds·GET /builds/{id}·
      POST /estimate는 200으로 정상 응답(가격 정보 없는 부품만
      title/price null)하고 500 크래시 없음 확인. POST /build/compare는
      부품 전부 가격 조회 실패 시 기존 로직대로 404("부품 가격을 하나도
      찾지 못해 판정 불가") — 이건 원래 있던 정상 동작이라 회귀 아님

[x] category, cash_price 필드 스크래퍼 구현 → 완료 (2026-08-04)
    → 이 세션 환경은 danawa.com 접근이 프록시 정책으로 막혀 있어 사용자가
      로컬 브라우저에서 실제 상품 페이지 HTML(GPU: PALIT RTX5070Ti
      76465883, CPU: AMD 9800X3D 70531547)을 직접 떠서 붙여넣어줌 — 그
      실측 HTML 기준으로 구현
    → category: div.location_wrap의 중첩 breadcrumb DOM을 직접 파싱하는
      대신, 페이지 하단 인라인 <script>의 oGlobalSetting.sUICategoryName
      값 사용 ("컴퓨터/노트북/조립PC &gt; 주요부품 &gt; 그래픽카드(GPU)"
      형태로 이미 정리된 단일 문자열, HTML 엔티티만 unescape). GPU/CPU
      두 카테고리 페이지 모두에서 동일 패턴 확인
    → cash_price: 애초 가정했던 "카드가/현금가 비교"(노트북 전용 기능)가
      아니라, 쇼핑몰별 최저가 목록 중 현금결제 전용(badge__cash) 판매처의
      최저가였음 — GPU 페이지엔 없었고(해당 상품이 일시 품절), CPU
      페이지엔 있었음(현금최저가 630,000원, 최저가 681,360원과 다른 값).
      og:description 메타태그에 다나와가 이미 계산해 넣어주는
      "현금최저가: N원" 값을 파싱 — 우리가 스크래핑하는 쇼핑몰별 가격
      목록 자체가 상위 약 10건만 잘려 있어 직접 min() 계산하면 절단
      구간 밖의 진짜 최저가를 놓칠 수 있어서, 다나와 계산값을 그대로 신뢰
    → backend/app/services/danawa.py::get_product()에 두 정규식 추가,
      main.py::get_product_detail()/schemas/product.py TODO 주석 제거하고
      실제 값 연결. 값이 없는 상품(현금결제 전용 판매처가 아예 없는 등)은
      필드가 None인 게 정상 — 파싱 실패 아님
    → 정규식 자체는 사용자가 준 실제 HTML 조각으로 직접 검증 완료.
      전체 페이지 단위 재현(mock 서버로 왕복)까지는 안 함 — 조각 검증으로
      충분하다고 판단(패턴이 각 페이지에 정확히 1회씩만 등장하는 것도
      육안 확인함)

[x] 즐겨찾기 탭 — 관심 상품 북마크 구현 완료, 사이드바 7탭 전부 실데이터
    연동 완료 (2026-08-04, 아래 "우선순위 낮음" 섹션 참조)

[x] frontend .env 기반 API_BASE 설정 문서화 → 완료 (2026-08-04)
    → .gitignore에 .env/.env.local 추가, frontend/.env.example 신규,
      README.md .env 섹션 보강 + "현재 구현 상태" 표 최신화(2026-08-03
      v0.4 시점 그대로 방치돼 있던 걸 발견 — 7탭 전부 연동 완료로 정정)

[ ] 배포 시 CORS allow_origins=["*"] → 실제 배포 도메인으로 좁히기
    (지금은 로컬 전용 개발이라 전체 허용으로 둠 — 실제 배포 계획 생기면 진행,
    이번 세션에도 보류 확정)
```

### 🔵 우선순위 낮음
```
[x] 홈 탭 대시보드 내용 설계+구현 완료 (2026-08-04)
    → 사용자와 논의해서 범위 확정: "최근 빌드 요약 + 빠른 액션"만 채택,
      "관심부품 변동" 후보는 즐겨찾기 기능 자체가 아직 없어서 보여줄
      데이터가 없다는 이유로 이번엔 제외 — 새 백엔드/DB 작업 없이 기존
      GET /builds만으로 끝낼 수 있는 범위로 좁힘
    → frontend/src/pages/HomePage.tsx 신규. 상단에 "새 빌드 만들기"/
      "부품 검색" 바로가기 버튼 2개, 아래에 최근 빌드 4개(created_at
      내림차순 정렬, 클라이언트에서 정렬 — 백엔드가 정렬 순서를 보장 안
      해서) 카드로 표시, 5개 이상이면 "전체보기"로 /build 이동. 빌드가
      하나도 없으면 "새 빌드를 만들어서 가격을 추적해보세요" 빈 상태
    → BuildListPage와 같은 build-card/bc-tag 스타일 재사용(새 CSS는
      버튼 줄(.home-actions) 하나만 추가)
    → Playwright로 빈 상태(빌드 0개)/채워진 상태(5개 만들어서 최근 4개만
      나오는지) 둘 다 실브라우저 검증, 카드 클릭→상세 이동/전체보기→
      목록 이동 둘 다 확인

[x] 통계 탭 — silga-mockup.html #history 섹션의 오실로스코프풍 라인차트를
    앱 셸로 이식 완료 (2026-08-04)
    → frontend/src/pages/StatsPage.tsx 신규. PartRow(빌드 생성 화면과 동일
      컴포넌트) 재사용해서 부품 검색 → 선택하면 GET
      /product/{code}/history?months={1|3|6|12} 호출, 1/3/6/12개월 탭
      전환 가능
    → 기존 목업 SVG(그래디언트 fill + glow filter + 그리드 라인)를
      그대로 이식하되, prices 배열에서 매번 polyline 좌표 계산하도록
      동적화. 최저/최고는 응답의 min/max 필드 그대로 사용(prices에서
      직접 min() 계산 금지 원칙과는 별개 필드라 무관), "현재"는
      검색 시점의 실측 lowest_price(PartRow가 이미 들고 있는 값) 사용 —
      history의 마지막 포인트는 주 단위라 최대 며칠 전 값일 수 있어
      "현재"로 쓰지 않음
    → 차트 범례의 날짜 범위 표시는 prices[].date(포맷 미확정) 대신
      full_date("YY-MM-DD", 실측 확인된 포맷)를 우선 사용해서
      "20YY.MM.DD" 형태로 표시 — date만 쓰면 1년 이상 차이 나는 두
      시점이 같은 월.일로 보여 혼동될 수 있어서(12개월 조회 시 실제
      겪은 문제, 개발 중 mock 데이터로 발견)
    → 이 세션 환경은 danawa.com 접근이 막혀 있어 danawa.get_product_codes/
      get_product/get_price_variance를 mock으로 교체해 검색→선택→차트
      렌더링 전체 흐름을 Playwright로 실브라우저 검증 (1개월/12개월 등
      데이터 포인트 개수 다른 케이스 포함)
    → 버그 하나 발견+수정: 부품 선택창(.stats-picker)에 overflow:hidden을
      넣었더니 자동완성 드롭다운이 컨테이너 아래로 잘려서 안 보이는 문제
      확인 → overflow:hidden 제거로 해결 (빌드 생성 화면의 .part-rows는
      여러 행이 이어져 있어 드롭다운이 컨테이너 안쪽에 들어가는 우연 덕에
      같은 문제가 안 드러났던 것으로 추정 — 그쪽은 아직 안 건드림)

[x] 최근기록 탭 — 최근 조회한 부품 구현 완료 (2026-08-04)
    → 사용자와 논의해서 범위 확정: "최근 조회한 부품(상품)" 채택
      (danawa 자체 헤더의 "최근 본 상품" 기능과 같은 맥락). "최근 검색어"
      /"최근 조회한 빌드"(홈 탭 최근빌드와 거의 같은 내용이 될 것) 후보는
      기각
    → 저장 위치: ma_window 설정과 동일하게 localStorage만 사용
      (silga:recent_products 키) — DB/백엔드 작업 전혀 없음. "기록"이라는
      단어 때문에 서버 저장을 먼저 떠올리기 쉬운데, 이 프로젝트는
      인증/로그인이 없어 "이 브라우저에서"라는 전제가 항상 깔려 있으므로
      서버에 남길 이유가 딱히 없다고 판단
    → 어디서 "조회"로 칠지가 관건이었음 — 이 앱엔 독립된 "상품 상세
      페이지"가 없어서(검색 결과 클릭해도 이동 안 함) 부품을 실제로 들여다
      보는 지점은 두 곳: (1) 통계 탭에서 부품 검색해 가격 히스토리 볼 때,
      (2) 빌드 생성 화면에서 부품을 고를 때. 둘 다 "조회"로 간주해서 두
      지점 모두에서 기록하기로 함(danawa 자체도 의도와 무관하게 방문한
      모든 상품 페이지를 "최근 본 상품"에 넣는 것과 같은 방식)
    → frontend/src/lib/recentProducts.ts 신규 — getRecentProducts/
      addRecentProduct(중복 시 맨 앞으로 이동)/removeRecentProduct/
      clearRecentProducts, 최대 20개 유지
    → StatsPage.tsx·BuildCreatePage.tsx의 PartRow onSelect 콜백에
      addRecentProduct(part) 한 줄씩 추가해서 계측
    → frontend/src/pages/RecentHistoryPage.tsx 신규 — 목록(시간 내림차순),
      개별 삭제(×), 전체 지우기, 빈 상태. 각 행 클릭 시 /stats로
      이동하면서 react-router의 location.state로 해당 부품을 넘겨
      StatsPage가 재검색 없이 바로 그 부품의 차트를 띄우도록 연결
      (StatsPage가 useLocation().state를 초기 선택값으로 사용하도록 확장)
    → 이 세션 환경은 danawa.com이 막혀 있어 mock 백엔드로 서로 다른 부품
      2개를 통계 탭에서 순서대로 선택 → 최근기록에 최신순으로 뜨는지,
      항목 클릭 시 통계 탭에 해당 부품이 바로 로드되는지, 개별 삭제/전체
      삭제 둘 다 Playwright로 실브라우저 검증 완료

[x] 즐겨찾기 탭 — "관심 상품 북마크" 구현 완료 (2026-08-04)
    → favorites DB 테이블 신규(id PK, product_code FK, created_at,
      product_code 유니크 제약) — 가격은 저장 안 함, 다른 테이블과 동일
      원칙(REFERENCE.md #DB-스키마 갱신 완료)
    → 백엔드: POST /favorites(idempotent — 이미 있으면 기존 항목 그대로
      반환, danawa.get_product() 한 번만 호출해서 존재확인+products
      캐시 upsert+응답용 즉시가까지 처리해 중복 스크래핑 방지), GET
      /favorites(목록, 항목별 실시간 최저가, 연결 장애 시 부품별
      fallback), DELETE /favorites/{code}(204, 없으면 404). 단일상품
      조회(POST)는 즉시 503, 다중부품 목록(GET)은 항목별 fallback —
      기존에 확립한 두 관례 그대로 적용
    → 겸사겸사 발견: _cache_product()가 category 파라미터를 아예 안 받아서
      products 테이블의 category 컬럼이 스크래퍼 구현(오늘 앞서 완료) 이후에도
      계속 비어있던 걸 발견 → _cache_product 시그니처에 category 추가,
      create_build/add_favorite 두 호출부 모두 수정. Product 모델의 스테일한
      TODO 주석("스크래퍼 아직 미제공")도 같이 정리
    → 프론트: frontend/src/pages/FavoritesPage.tsx 신규 — 상단에 PartRow로
      검색→선택 즉시 POST /favorites(추가), 아래 목록(recent-list 스타일
      재사용)에서 클릭 시 최근기록과 동일하게 /stats로 이동+즉시 로드,
      개별 제거(DELETE), 빈 상태
    → 버그 발견+수정: PartRow를 selected=null 고정으로 반복 재사용하는
      패턴(즐겨찾기 화면 특유 — "선택 즉시 추가하고 계속 검색 상태 유지")
      에서, PartRow.handlePick()이 선택 시 setFocused(false)를 호출하는데
      즐겨찾기 화면은 selected가 항상 null이라 .part-selected 클릭으로
      focused를 되돌리는 경로 자체가 없어서, 한 번 선택한 뒤 같은 input에
      바로 타이핑해도 자동완성이 다시 안 뜨는 문제 발견(DOM은 계속
      focus돼 있어서 안 보이는 버그였음 — Playwright로 활성 엘리먼트까지
      직접 확인해서 원인 특정). PartRow 공용 컴포넌트 자체는 안 건드리고,
      FavoritesPage에서 선택 성공마다 key를 바꿔 PartRow를 강제
      remount하는 방식으로 우회 해결
    → 검증: mock 백엔드로 POST(idempotent 포함)/GET/DELETE 전부 curl로,
      프론트는 검색→추가(2개, 중복추가 무시 확인)→클릭시 통계 탭 이동+
      로드→제거까지 Playwright로 실브라우저 검증

[x] scripts/e2e_smoke_test.py — 홈/통계/최근기록/즐겨찾기 4개 화면
    스텝 추가 완료 (2026-08-04)
    → 기존 검색→빌드생성→상세→목록(4스텝)에 홈 카드 클릭, 통계 탭
      검색+차트+월 탭 전환, 최근기록 계측 확인+클릭 이동, 즐겨찾기
      추가/제거 8스텝 추가(총 12스텝)
    → 이 환경은 danawa.com 접근이 막혀 있어 스크립트를 라이브로 못
      돌림 — danawa 3개 함수를 mock으로 교체한 로컬 백엔드를 세워
      확장된 스크립트 전체를 실제 Playwright(사전 설치된 Chromium)로
      끝까지 실행해 12스텝 전부 통과 확인. 셀렉터/플로우 순서 결함은
      이 과정에서 걸러졌고, 다나와 실제 HTML 파싱 자체는 기존 검증된
      danawa.py 로직을 그대로 신뢰(이번 검증 범위 아님)
    → 상세는 실가_HISTORY.md 2026-08-04 v13 참조

[ ] 알림(지정가 알림) 기능 — Flutter 이식 이후 FCM으로 적용 예정,
    웹 단계에서는 보류 확정

[ ] APScheduler 크롤링 배치 설계 (몇 시간 주기로 갱신할지 미정)

[x] 프로젝트 정식 이름 확정 → 실가로 확정, PPE 코드네임 일괄 치환 완료 (2026-08-03)
```

---

## 알려진 미결사항 / 동작 특성
```
[미결] 목업 파일 2개 중 실가(silga-mockup.html)는 랜딩형 마케팅 페이지로,
  현재 채택된 앱 셸(app-shell-mockup.html)과 레이아웃 사상이 다름
  → 판정 게이지/breakdown 컴포넌트, 가격 히스토리 오실로스코프 차트는
    앱 셸 쪽(통계 탭, 2026-08-04)으로 이식 완료. 나머지(히어로, API
    터미널 섹션)는 폐기 또는 별도 마케팅 페이지 용도로 보류 (미정)

[동작특성] app-shell-mockup.html은 순수 정적 목업
  → 사이드바 토글/탭 전환/빌드 3단계 흐름 전환만 JS로 동작, 실제 데이터
    fetch 없음. 모든 부품명/가격은 하드코딩된 예시값 (2026-08-01 대화
    시점의 실제 다나와 조사값 재사용)

[동작특성] GTHV 문서 체계와의 차이
  → GTHV는 UTC 통일, 실가는 KST 통일 (다나와 자체가 KST 서비스라 변환
    비용 없애는 게 실익 큼) — 문서 작성 시 혼동 주의
  → GTHV의 "실행 중인 프로세스"/"장애 대응"/"주기적 점검" 섹션은 실가
    REFERENCE.md에 아직 없음 — 배포 이후(Phase 3 이후)에 채울 것,
    현재는 로컬 개발 단계라 해당 없음

[동작특성] danawa-py는 다나와 DOM 구조 변경에 매우 취약함이 실증됨
  → 2026-08-03 검증 시점 기준으로 이미 원본 코드의 셀렉터 2곳이 깨져
    있었음(제작 시점 대비 다나와 개편). 커밋 9개뿐이라는 우려가 실제로
    맞아떨어진 사례 — 향후 다나와가 또 개편하면 재점검 필요. 크롤링 실패
    시 명확한 에러를 내는 방어 로직은 FastAPI 단에 구현 완료
    (requests.RequestException → 503 "데이터 소스 연결 실패",
    상품 없음/파싱 실패 → 404 "상품을 찾을 수 없음", 라이브 검증 완료)
  → 추가로 "일시 품절"(가격 정보 자체가 없는 정상 상태)과 "스크래핑
    실패"를 구분 못 하던 문제도 발견해서 in_stock 필드로 해결함
    (2026-08-03, PALIT RTX5070Ti 76465883 실측 사례 — REFERENCE.md 참조)
```

---

## Phase 로드맵
```
Phase 1: 설계/목업 (완료)
  2026-08-03  기획: 조립PC 적정가 판정 아이디어 도출 (참쉬운PC 사례 분석에서 출발)
  2026-08-03  design: 랜딩형 목업(실가) 제작
  2026-08-03  design: 앱 셸 목업(사이드바 7탭 + 빌드 3단계 흐름) 제작, 최종 채택
  2026-08-03  research: 데이터소스 후보 조사 — danawa-py(비공식 API), 
              sammy310/Danawa-Crawler(대체 크롤러), 다나와 공식 API(미검증)
  2026-08-03  docs: 인수인계 3파일 체계 도입 (GTHV 문서 틀 이식)

Phase 2: 데이터소스 검증 (완료)
  2026-08-03  test: danawa-py 3개 함수 실동작 테스트 완료
  2026-08-03  fix: get_product_codes 패치 (URL 인코딩 + 클래스 매칭 완화)
  2026-08-03  fix: get_product 패치 (price-summary 신 구조 대응, 쇼핑몰명/
              variants 필드 확장) → danawa_patched.py
  2026-08-03  chore: danawa_patched.py를 backend/app/services/danawa.py로
              정식 편입, get_price_variance 원본과 통합

Phase 3: 백엔드 API 구현 (완료)
  2026-08-03  feat: FastAPI 스켈레톤 + /search, /product/{code} 구현+검증
  2026-08-03  feat: SQLite(WAL) DB 스키마 구현 (products/builds/build_items),
              KSTDateTime 커스텀 타입으로 타임존 소실 버그 해결
  2026-08-03  feat: /product/{code}/history, /estimate,
              /product/{code}/compare 구현+검증
  2026-08-03  feat: /compare 스펙 불일치 발견 → POST /build/compare 신규 추가
  2026-08-03  feat: 빌드 저장/조회 CRUD 신규 추가 (POST/GET /builds,
              GET /builds/{id})
  2026-08-03  fix: "일시 품절"과 "스크래핑 실패" 구분 안 되는 문제 발견,
              in_stock 필드로 해결
  2026-08-03  chore: git 리포 형성 (커밋 4개, v0.2~v0.4 + docs)

Phase 4: 프론트 실데이터 연동 (핵심 완료, 마무리 항목 남음) ← 현재
  2026-08-03  feat: Vite+React+TS+React Router+TanStack Query 스캐폴딩
  2026-08-03  feat: 검색 탭, 빌드 목록/생성/상세 실데이터 연동 완료
  2026-08-03  test: Playwright E2E 스모크 테스트로 전체 흐름 실브라우저 검증
  2026-08-03  fix: 자동완성 로딩 placeholder 클릭 가로채기 레이스 컨디션 수정
  2026-08-04  feat: verdict 판정 기준가에 이동평균(7/14/30일 선택) 도입,
              부품별 fallback+신뢰도 플래그, GET /builds용 TTL 캐시 (백엔드만,
              라이브 다나와 미검증 — 아래 참조)
  2026-08-04  fix: 다나와 주 단위 데이터 실측 반영해 compute_ma_price
              재설계(v0.4.1), 라이브 검증 완료, PR #2 main 머지
  2026-08-04  feat: verdict 이동평균 프론트 연동(v0.2) — 설정 탭 드롭다운
              + localStorage, BuildList/BuildDetail이 ma_window 실어 보냄
  2026-08-04  fix: _fetch_lowest_price danawa 연결 장애 처리 누락 수정
              (v0.4.2) — 단일상품은 503, 다중부품 합산은 부품별 fallback
  2026-08-04  feat: category(카테고리 브레드크럼)·cash_price(현금최저가)
              필드 스크래퍼 구현(v0.4.3), 사용자가 준 실제 페이지 HTML로 검증
  2026-08-04  feat: 통계 탭 가격 히스토리 차트 구현(v0.3) — silga-mockup.html
              오실로스코프 차트 이식, PartRow 재사용해서 부품 검색 연동
  2026-08-04  feat: 홈 탭 최근빌드 대시보드 구현(v0.4) — GET /builds 재사용,
              최근 4개 빌드 카드 + 빠른 액션 버튼, 새 백엔드 작업 없음
  2026-08-04  feat: 최근기록 탭 구현(v0.5) — localStorage 기반 최근 조회
              부품 목록, 통계/빌드생성 화면 계측, 새 백엔드 작업 없음
  2026-08-04  feat: 즐겨찾기 탭 구현(backend v0.4.4, frontend v0.6) —
              favorites 테이블/엔드포인트 3개 신규, 사이드바 7탭 전부
              실데이터 연동 완료(PlaceholderPage 삭제)
  [x] Phase 4 목표(프론트 실데이터 연동) 달성 — 7탭 전부 완료
  2026-08-04  docs: frontend .env API_BASE 문서화 보강(.env.example 신규,
              README.md 최신화)
  2026-08-04  test: e2e_smoke_test.py에 홈/통계/최근기록/즐겨찾기
              4개 화면 스텝 추가(v13), mock 백엔드로 12스텝 전체 통과 검증
  2026-08-05  feat: /search 카테고리 필터 + 카테고리별 스펙 필터 7종
              (GPU 메모리/칩셋, CPU·메인보드 소켓, 메인보드·케이스 폼팩터,
              RAM 규격, 파워 출력, SSD 인터페이스) — PR #6, #7 머지
  2026-08-05  feat: 프론트 디자인 전면 교체(다크네온→종이/잉크 에디토리얼)
              + 빌드 상세를 견적서 스펙시트로 재구성 — PR #8, #9 머지
  2026-08-05  feat: 쿨러 스펙 필터(제품 종류 5종 + 지원 소켓 4종) 추가
              (backend v0.7 / frontend v0.9.1) — 8개 카테고리 전부
              스펙 필터 보유

Phase 5: Flutter 이식 (예정)
         웹에서 검증된 API 그대로 재사용, 화면 구조 기계적 이식

Phase 6: AI 라우터 연동 테스트 (예정)
         Claude 등 에이전트가 실제로 엔드포인트를 tool처럼 호출해보고
         응답 파싱 검증
```
